(function ($) { "use strict";
/*!
 * jQuery formBuilder: https://formbuilder.online/
 * Version: 3.23.0
 * Author: Kevin Chappell <kevin.b.chappell@gmail.com>
 */
/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ 349:
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(645);
/* harmony import */ var _node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(278);
/* harmony import */ var _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1__);
/* harmony import */ var _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(21);
/* harmony import */ var _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(_node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2__);
// Imports



var ___CSS_LOADER_URL_IMPORT_0___ = new URL(/* asset import */ __webpack_require__(138), __webpack_require__.b);
var ___CSS_LOADER_URL_IMPORT_1___ = new URL(/* asset import */ __webpack_require__(488), __webpack_require__.b);
var ___CSS_LOADER_URL_IMPORT_2___ = new URL(/* asset import */ __webpack_require__(591), __webpack_require__.b);
var ___CSS_LOADER_EXPORT___ = _node_modules_css_loader_dist_runtime_api_js__WEBPACK_IMPORTED_MODULE_1___default()((_node_modules_css_loader_dist_runtime_noSourceMaps_js__WEBPACK_IMPORTED_MODULE_0___default()));
var ___CSS_LOADER_URL_REPLACEMENT_0___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(___CSS_LOADER_URL_IMPORT_0___);
var ___CSS_LOADER_URL_REPLACEMENT_1___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(___CSS_LOADER_URL_IMPORT_1___);
var ___CSS_LOADER_URL_REPLACEMENT_2___ = _node_modules_css_loader_dist_runtime_getUrl_js__WEBPACK_IMPORTED_MODULE_2___default()(___CSS_LOADER_URL_IMPORT_2___);
// Module
___CSS_LOADER_EXPORT___.push([module.id, "@font-face{font-family:\"formbuilder-icons\";src:url(" + ___CSS_LOADER_URL_REPLACEMENT_0___ + ") format(\"woff\")}[class^=formbuilder-icon-]:before,[class*=\" formbuilder-icon-\"]:before{font-family:\"formbuilder-icons\";font-style:normal;font-weight:normal;speak:never;display:inline-block;text-decoration:inherit;width:1em;margin-right:.2em;text-align:center;font-variant:normal;text-transform:none;line-height:1em;margin-left:.2em}.formbuilder-icon-autocomplete:before{content:\"\"}.formbuilder-icon-date:before{content:\"\"}.formbuilder-icon-checkbox:before{content:\"\"}.formbuilder-icon-checkbox-group:before{content:\"\"}.formbuilder-icon-radio-group:before{content:\"\"}.formbuilder-icon-rich-text:before{content:\"\"}.formbuilder-icon-select:before{content:\"\"}.formbuilder-icon-textarea:before{content:\"\"}.formbuilder-icon-text:before{content:\"\"}.formbuilder-icon-pencil:before{content:\"\"}.formbuilder-icon-file:before{content:\"\"}.formbuilder-icon-hidden:before{content:\"\"}.formbuilder-icon-cancel:before{content:\"\"}.formbuilder-icon-button:before{content:\"\"}.formbuilder-icon-header:before{content:\"\"}.formbuilder-icon-paragraph:before{content:\"\"}.formbuilder-icon-number:before{content:\"\"}.formbuilder-icon-copy:before{content:\"\"}.formbuilder-icon-grid:before{content:url(" + ___CSS_LOADER_URL_REPLACEMENT_1___ + ")}.formbuilder-icon-plus:before{content:url(" + ___CSS_LOADER_URL_REPLACEMENT_2___ + ")}.formbuilder-icon-sort-lower:before{content:\"\"}.formbuilder-icon-sort-higher:before{content:\"\"}.form-wrap.form-builder{position:relative;display:flex;flex-direction:row}.form-wrap.form-builder *{box-sizing:border-box}.form-wrap.form-builder.formbuilder-embedded-bootstrap button,.form-wrap.form-builder.formbuilder-embedded-bootstrap input,.form-wrap.form-builder.formbuilder-embedded-bootstrap select,.form-wrap.form-builder.formbuilder-embedded-bootstrap textarea{font-family:inherit;font-size:inherit;line-height:inherit}.form-wrap.form-builder.formbuilder-embedded-bootstrap input{line-height:normal}.form-wrap.form-builder.formbuilder-embedded-bootstrap textarea{overflow:auto}.form-wrap.form-builder.formbuilder-embedded-bootstrap button,.form-wrap.form-builder.formbuilder-embedded-bootstrap input,.form-wrap.form-builder.formbuilder-embedded-bootstrap select,.form-wrap.form-builder.formbuilder-embedded-bootstrap textarea{font-family:inherit;font-size:inherit;line-height:inherit}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group{position:relative;display:inline-block;vertical-align:middle}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn{position:relative;float:left}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:first-child:not(:last-child):not(.dropdown-toggle){border-top-right-radius:0;border-bottom-right-radius:0}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:not(:first-child):not(:last-child):not(.dropdown-toggle){border-radius:0}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .btn+.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .btn+.btn-group,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .btn-group+.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .btn-group+.btn-group{margin-left:-1px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:last-child:not(:first-child),.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.dropdown-toggle:not(:first-child),.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group .form-control:last-child,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-addon:last-child,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-btn:first-child>.btn-group:not(:first-child)>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-btn:first-child>.btn:not(:first-child),.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-btn:last-child>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-btn:last-child>.btn-group>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group .input-group-btn:last-child>.dropdown-toggle{border-top-left-radius:0;border-bottom-left-radius:0}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn.active,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:active,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:focus,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn-group>.btn:hover{z-index:2}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn{display:inline-block;padding:6px 12px;margin-bottom:0;font-size:14px;font-weight:400;line-height:1.42857143;text-align:center;white-space:nowrap;vertical-align:middle;touch-action:manipulation;cursor:pointer;-webkit-user-select:none;-moz-user-select:none;user-select:none;background-image:none;border-radius:4px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn.btn-lg{padding:10px 16px;font-size:18px;line-height:1.3333333;border-radius:6px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn.btn-sm{padding:5px 10px;font-size:12px;line-height:1.5;border-radius:3px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn.btn-xs{padding:1px 5px;font-size:12px;line-height:1.5;border-radius:3px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn.active,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn.btn-active,.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn:active{background-image:none}.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group .form-control:last-child,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-addon:last-child,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn:first-child>.btn-group:not(:first-child)>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn:first-child>.btn:not(:first-child),.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn:last-child>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn:last-child>.btn-group>.btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn:last-child>.dropdown-toggle{border-top-left-radius:0;border-bottom-left-radius:0}.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group .form-control,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-addon,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-btn{display:table-cell}.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-lg>.form-control,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-lg>.input-group-addon,.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group-lg>.input-group-btn>.btn{height:46px;padding:10px 16px;font-size:18px;line-height:1.3333333}.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group{position:relative;display:table;border-collapse:separate}.form-wrap.form-builder.formbuilder-embedded-bootstrap .input-group .form-control{position:relative;z-index:2;float:left;width:100%;margin-bottom:0}.form-wrap.form-builder.formbuilder-embedded-bootstrap .form-control,.form-wrap.form-builder.formbuilder-embedded-bootstrap output{font-size:14px;line-height:1.42857143;display:block}.form-wrap.form-builder.formbuilder-embedded-bootstrap textarea.form-control{height:auto}.form-wrap.form-builder.formbuilder-embedded-bootstrap .form-control{height:34px;display:block;width:100%;padding:6px 12px;font-size:14px;line-height:1.42857143;border-radius:4px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .form-control:focus{outline:0;box-shadow:inset 0 1px 1px rgba(0,0,0,.075),0 0 8px rgba(102,175,233,.6)}.form-wrap.form-builder.formbuilder-embedded-bootstrap .form-group{margin-left:0px;margin-bottom:15px}.form-wrap.form-builder.formbuilder-embedded-bootstrap .btn,.form-wrap.form-builder.formbuilder-embedded-bootstrap .form-control{background-image:none}.form-wrap.form-builder.formbuilder-embedded-bootstrap .pull-right{float:right}.form-wrap.form-builder.formbuilder-embedded-bootstrap .pull-left{float:left}.form-wrap.form-builder .formbuilder-required,.form-wrap.form-builder .required-asterisk{color:#c10000}.form-wrap.form-builder .checkbox-group,.form-wrap.form-builder .radio-group{display:flex;flex-direction:column;gap:8px}.form-wrap.form-builder .checkbox-group.checkbox-group--inline,.form-wrap.form-builder .checkbox-group.radio-group--inline,.form-wrap.form-builder .radio-group.checkbox-group--inline,.form-wrap.form-builder .radio-group.radio-group--inline{flex-direction:row}.form-wrap.form-builder .formbuilder-checkbox-group input[type=checkbox],.form-wrap.form-builder .formbuilder-checkbox-group input[type=radio],.form-wrap.form-builder .formbuilder-radio-group input[type=checkbox],.form-wrap.form-builder .formbuilder-radio-group input[type=radio]{margin:0 4px 0 0}.form-wrap.form-builder .formbuilder-autocomplete-list{background-color:#fff;display:none;list-style:none;padding:0;border:1px solid #ccc;border-width:0 1px 1px;position:absolute;z-index:20;max-height:200px;overflow-y:auto}.form-wrap.form-builder .formbuilder-autocomplete-list li{display:none;cursor:default;padding:5px;margin:0;transition:background-color 200ms ease-in-out}.form-wrap.form-builder .formbuilder-autocomplete-list li:hover,.form-wrap.form-builder .formbuilder-autocomplete-list li.active-option{background-color:rgba(0,0,0,.075)}@keyframes PLACEHOLDER{0%{height:1px}100%{height:15px}}.form-wrap.form-builder .cb-wrap{width:26%;max-width:-moz-fit-content;max-width:fit-content;transition:transform 250ms}.form-wrap.form-builder .cb-wrap.sticky-controls{position:sticky;align-self:flex-start;top:0}.form-wrap.form-builder .cb-wrap h4{margin-top:0;color:#666}@media(max-width: 481px){.form-wrap.form-builder .cb-wrap{width:64px}.form-wrap.form-builder .cb-wrap h4{display:none}}.form-wrap.form-builder .cb-wrap .form-actions{float:right;margin-top:5px}.form-wrap.form-builder .cb-wrap .form-actions button{border:0 none}.form-wrap.form-builder .frmb-control{margin:0;padding:0;border-radius:5px}.form-wrap.form-builder .frmb-control li{cursor:move;list-style:none;margin:0 0 -1px 0;padding:10px;text-align:left;background:#fff;-webkit-user-select:none;-moz-user-select:none;user-select:none;white-space:nowrap;text-overflow:ellipsis;overflow:hidden;box-shadow:inset 0 0 0 1px #c5c5c5}.form-wrap.form-builder .frmb-control li .control-icon{width:16px;height:auto;margin-right:10px;margin-left:.2em;display:inline-block}.form-wrap.form-builder .frmb-control li .control-icon img,.form-wrap.form-builder .frmb-control li .control-icon svg{max-width:100%;height:auto}.form-wrap.form-builder .frmb-control li:first-child{border-radius:5px 5px 0 0;margin-top:0}.form-wrap.form-builder .frmb-control li:last-child{border-radius:0 0 5px 5px}.form-wrap.form-builder .frmb-control li::before{margin-right:10px;font-size:16px}.form-wrap.form-builder .frmb-control li:hover{background-color:#f2f2f2}.form-wrap.form-builder .frmb-control li.ui-sortable-helper{border-radius:5px;transition:box-shadow 250ms;box-shadow:2px 2px 6px 0 #666;border:1px solid #fff}.form-wrap.form-builder .frmb-control li.ui-state-highlight{width:0;overflow:hidden;padding:0;margin:0;border:0 none}.form-wrap.form-builder .frmb-control li.moving{opacity:.6}.form-wrap.form-builder .frmb-control li.formbuilder-separator{background-color:rgba(0,0,0,0);box-shadow:none;padding:0;cursor:default}.form-wrap.form-builder .frmb-control li.formbuilder-separator hr{margin:10px 0}@media(max-width: 481px){.form-wrap.form-builder .frmb-control li::before{font-size:30px}.form-wrap.form-builder .frmb-control li{text-overflow:clip}.form-wrap.form-builder .frmb-control li span{visibility:hidden}.form-wrap.form-builder .frmb-control li span span{visibility:visible;font-size:30px;width:auto !important}}.form-wrap.form-builder .frmb-control.sort-enabled li.ui-state-highlight{box-shadow:none;height:0;width:100%;background:radial-gradient(ellipse at center, rgb(84, 84, 84) 0%, rgba(0, 0, 0, 0) 75%);border:0 none;clip-path:polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);visibility:visible;overflow:hidden;margin:1px 0 3px;animation:PLACEHOLDER 250ms forwards}.controls-left.form-wrap.form-builder .form-actions{float:left}.formbuilder-mobile.form-wrap.form-builder .form-actions{width:100%}.formbuilder-mobile.form-wrap.form-builder .form-actions button{width:100%;font-size:.85em !important;display:block !important;border-radius:0 !important;margin-top:-1px;margin-left:0 !important}.formbuilder-mobile.form-wrap.form-builder .form-actions button:first-child{border-radius:5px 5px 0 0 !important;margin-top:0 !important;border-bottom:0 none}.formbuilder-mobile.form-wrap.form-builder .form-actions button:last-child{border-radius:0 0 5px 5px !important}.form-wrap.form-builder .stage-wrap{flex-grow:1;display:flex;flex-direction:column;position:relative;padding:0;margin:0;width:calc(74% - 5px)}@media(max-width: 481px){.form-wrap.form-builder .stage-wrap{width:calc(100% - 64px)}}.form-wrap.form-builder .stage-wrap.empty{border:3px dashed #ccc;background-color:rgba(255,255,255,.25)}.form-wrap.form-builder .stage-wrap.empty::after{content:attr(data-content);position:absolute;text-align:center;top:50%;left:0;width:100%;margin-top:-1em}.form-wrap.form-builder .frmb{list-style-type:none;min-height:200px;transition:background-color 500ms ease-in-out}.form-wrap.form-builder .frmb .formbuilder-required{color:#c10000}.form-wrap.form-builder .frmb.removing{overflow:hidden}.form-wrap.form-builder .frmb li.form-field:hover{border-color:#66afe9;outline:0;box-shadow:inset 0 1px 1px rgba(0,0,0,.1),0 0 8px rgba(102,175,233,.6)}.form-wrap.form-builder .frmb li.form-field:hover .field-actions{opacity:1}.form-wrap.form-builder .frmb li.form-field:hover li :hover{background:#fefefe}.form-wrap.form-builder .frmb li.form-field{position:relative;padding:6px;clear:both;margin-left:0;margin-bottom:3px;background-color:#fff;transition:background-color 250ms ease-in-out,margin-top 400ms}.form-wrap.form-builder .frmb li.form-field.hidden-field{background-color:rgba(255,255,255,.6)}.form-wrap.form-builder .frmb li.form-field:first-child{border-top-right-radius:5px;border-top-left-radius:5px}.form-wrap.form-builder .frmb li.form-field:first-child .field-actions .btn:last-child{border-radius:0 5px 0 0}.form-wrap.form-builder .frmb li.form-field:last-child{border-bottom-right-radius:5px;border-bottom-left-radius:5px}.form-wrap.form-builder .frmb li.form-field.no-fields label{font-weight:400}@keyframes PLACEHOLDER{0%{height:0}100%{height:15px}}.form-wrap.form-builder .frmb li.form-field.frmb-placeholder,.form-wrap.form-builder .frmb li.form-field.ui-state-highlight{height:0;padding:0;background:radial-gradient(ellipse at center, rgb(84, 84, 84) 0%, rgba(0, 0, 0, 0) 75%);border:0 none;clip-path:polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);visibility:visible;overflow:hidden;margin-bottom:3px;animation:PLACEHOLDER 250ms forwards}.form-wrap.form-builder .frmb li.form-field.moving,.form-wrap.form-builder .frmb li.form-field.ui-sortable-helper{transition:box-shadow 500ms ease-in-out;box-shadow:2px 2px 6px 0 #666;border:1px solid #fff;border-radius:5px}.form-wrap.form-builder .frmb li.form-field.disabled-field{z-index:1;position:relative;overflow:visible}.form-wrap.form-builder .frmb li.form-field.disabled-field:hover .frmb-tt{display:inline-block}.form-wrap.form-builder .frmb li.form-field.disabled-field [type=checkbox]{float:left;margin-right:10px}.form-wrap.form-builder .frmb li.form-field.disabled-field h2{border-bottom:0 none}.form-wrap.form-builder .frmb li.form-field.disabled-field label{font-size:12px;font-weight:400;color:#666}.form-wrap.form-builder .frmb li.form-field.disabled-field .prev-holder{cursor:default;line-height:28px;padding-left:5px}.form-wrap.form-builder .frmb li.form-field .close-field{position:absolute;color:#666;left:50%;bottom:6px;background:#fff;border-top:1px solid #c5c5c5;border-left:1px solid #c5c5c5;border-right:1px solid #c5c5c5;transform:translateX(-50%);padding:0 5px;border-top-right-radius:3px;border-top-left-radius:3px;cursor:pointer;transition:background-color 250ms ease-in-out}.form-wrap.form-builder .frmb li.form-field .close-field:hover{text-decoration:none}.form-wrap.form-builder .frmb li.form-field.header-field h1,.form-wrap.form-builder .frmb li.form-field.header-field h2,.form-wrap.form-builder .frmb li.form-field.header-field h3,.form-wrap.form-builder .frmb li.form-field.header-field h4,.form-wrap.form-builder .frmb li.form-field.header-field h5,.form-wrap.form-builder .frmb li.form-field.header-field h6{word-break:break-word}.form-wrap.form-builder .frmb li.form-field.paragraph-field p{word-break:break-word}.form-wrap.form-builder .frmb li.form-field .field-label{display:inline;overflow-wrap:break-word}.form-wrap.form-builder .frmb li.form-field.button-field h1,.form-wrap.form-builder .frmb li.form-field.button-field h2,.form-wrap.form-builder .frmb li.form-field.button-field h3,.form-wrap.form-builder .frmb li.form-field.button-field p,.form-wrap.form-builder .frmb li.form-field.button-field canvas,.form-wrap.form-builder .frmb li.form-field.button-field output,.form-wrap.form-builder .frmb li.form-field.button-field address,.form-wrap.form-builder .frmb li.form-field.button-field blockquote,.form-wrap.form-builder .frmb li.form-field.button-field .prev-holder,.form-wrap.form-builder .frmb li.form-field.header-field h1,.form-wrap.form-builder .frmb li.form-field.header-field h2,.form-wrap.form-builder .frmb li.form-field.header-field h3,.form-wrap.form-builder .frmb li.form-field.header-field p,.form-wrap.form-builder .frmb li.form-field.header-field canvas,.form-wrap.form-builder .frmb li.form-field.header-field output,.form-wrap.form-builder .frmb li.form-field.header-field address,.form-wrap.form-builder .frmb li.form-field.header-field blockquote,.form-wrap.form-builder .frmb li.form-field.header-field .prev-holder,.form-wrap.form-builder .frmb li.form-field.paragraph-field h1,.form-wrap.form-builder .frmb li.form-field.paragraph-field h2,.form-wrap.form-builder .frmb li.form-field.paragraph-field h3,.form-wrap.form-builder .frmb li.form-field.paragraph-field p,.form-wrap.form-builder .frmb li.form-field.paragraph-field canvas,.form-wrap.form-builder .frmb li.form-field.paragraph-field output,.form-wrap.form-builder .frmb li.form-field.paragraph-field address,.form-wrap.form-builder .frmb li.form-field.paragraph-field blockquote,.form-wrap.form-builder .frmb li.form-field.paragraph-field .prev-holder{margin:0}.form-wrap.form-builder .frmb li.form-field.button-field .field-label,.form-wrap.form-builder .frmb li.form-field.header-field .field-label,.form-wrap.form-builder .frmb li.form-field.paragraph-field .field-label{display:none}.form-wrap.form-builder .frmb li.form-field.button-field.editing .field-label,.form-wrap.form-builder .frmb li.form-field.header-field.editing .field-label,.form-wrap.form-builder .frmb li.form-field.paragraph-field.editing .field-label{display:block}.form-wrap.form-builder .frmb li.form-field.paragraph-field .fld-label{min-height:150px;overflow-y:auto}.form-wrap.form-builder .frmb li.form-field.checkbox-field .field-label{display:none}.form-wrap.form-builder .frmb li.deleting,.form-wrap.form-builder .frmb li.delete:hover,.form-wrap.form-builder .frmb li:hover li.delete:hover{background-color:#fdd}.form-wrap.form-builder .frmb li.deleting .close-field,.form-wrap.form-builder .frmb li.delete:hover .close-field,.form-wrap.form-builder .frmb li:hover li.delete:hover .close-field{background-color:#fdd}.form-wrap.form-builder .frmb li.deleting{z-index:20;pointer-events:none}.form-wrap.form-builder .frmb.disabled-field{padding:0 5px}.form-wrap.form-builder .frmb.disabled-field :hover{border-color:rgba(0,0,0,0)}.form-wrap.form-builder .frmb.disabled-field .form-element{float:none;margin-bottom:10px;overflow:visible;padding:5px 0;position:relative}.form-wrap.form-builder .frmb .frm-holder{display:none}.form-wrap.form-builder .frmb .tooltip{left:20px}.form-wrap.form-builder .frmb .prev-holder{display:block}.form-wrap.form-builder .frmb .prev-holder .form-group{margin:0}.form-wrap.form-builder .frmb .prev-holder .ql-editor{min-height:125px}.form-wrap.form-builder .frmb .prev-holder .form-group>label:not([class=formbuilder-checkbox-label]){display:none}.form-wrap.form-builder .frmb .prev-holder select,.form-wrap.form-builder .frmb .prev-holder input[type=text],.form-wrap.form-builder .frmb .prev-holder textarea,.form-wrap.form-builder .frmb .prev-holder input[type=number]{background-color:#fff;border:1px solid #ccc;box-shadow:inset 0 1px 1px rgba(0,0,0,.075)}.form-wrap.form-builder .frmb .prev-holder input[type=color]{width:60px;padding:2px;display:inline-block}.form-wrap.form-builder .frmb .prev-holder input[type=date]{width:auto}.form-wrap.form-builder .frmb .prev-holder select[multiple]{height:auto}.form-wrap.form-builder .frmb .prev-holder label{font-weight:normal}.form-wrap.form-builder .frmb .prev-holder input[type=number]{width:auto;max-width:100%}.form-wrap.form-builder .frmb .prev-holder input[type=color]{width:60px;padding:2px;display:inline-block}.form-wrap.form-builder .frmb .required-asterisk{display:none}.form-wrap.form-builder .frmb .field-label,.form-wrap.form-builder .frmb .legend{color:#666;margin-bottom:5px;line-height:27px;font-size:16px;font-weight:normal}.form-wrap.form-builder .frmb .disabled-field .field-label{display:block}.form-wrap.form-builder .frmb .other-option:checked+label input{display:inline-block}.form-wrap.form-builder .frmb .other-val{margin-left:5px;display:none}.form-wrap.form-builder .frmb .field-actions{position:absolute;top:0;right:0;opacity:0}.form-wrap.form-builder .frmb .field-actions a::before{margin:0}.form-wrap.form-builder .frmb .field-actions a:hover{text-decoration:none;color:#000}.form-wrap.form-builder .frmb .field-actions .btn{display:inline-block;width:32px;height:32px;padding:0 6px;border-radius:0;border-color:#c5c5c5;background-color:#fff;color:#c5c5c5;line-height:32px;font-size:16px;border-width:0 0 1px 1px}.form-wrap.form-builder .frmb .field-actions .btn:first-child{border-bottom-left-radius:5px}.form-wrap.form-builder .frmb .field-actions .toggle-form:hover{border-color:#ccc;background-color:#65aac6;color:#fff}.form-wrap.form-builder .frmb .field-actions .toggle-form::before{margin:0}.form-wrap.form-builder .frmb .field-actions .copy-button:hover{background-color:#6fc665;color:#fff}.form-wrap.form-builder .frmb .field-actions .del-button:hover{background-color:#c66865;color:#fff}.form-wrap.form-builder .frmb .option-actions{text-align:right;margin-top:10px;width:100%;margin-left:2%}.form-wrap.form-builder .frmb .option-actions button,.form-wrap.form-builder .frmb .option-actions a{background:#fff;padding:5px 10px;border:1px solid #c5c5c5;font-size:14px;border-radius:5px;cursor:default}.form-wrap.form-builder .frmb .sortable-options-wrap{width:81.33333333%;display:inline-block}.form-wrap.form-builder .frmb .sortable-options-wrap label{font-weight:normal}@media(max-width: 481px){.form-wrap.form-builder .frmb .sortable-options-wrap{display:block;width:100%}}.form-wrap.form-builder .frmb .radio-group-field .sortable-options li:nth-child(2) .remove,.form-wrap.form-builder .frmb .select-field .sortable-options li:nth-child(2) .remove{opacity:0;pointer-events:none}.form-wrap.form-builder .frmb .sortable-options{display:inline-block;width:100%;margin-left:2%;background:#c5c5c5;margin-bottom:0;border-radius:2px;list-style:none;padding:0}.form-wrap.form-builder .frmb .sortable-options>li{display:flex !important;flex-wrap:wrap;cursor:move;margin:1px;padding:6px;background-color:#fff}.form-wrap.form-builder .frmb .sortable-options>li:nth-child(1) .remove{opacity:0;pointer-events:none}.form-wrap.form-builder .frmb .sortable-options>li .remove{flex:none;display:flex;align-items:center;justify-content:center;opacity:1;width:18px;font-size:12px;padding:0;color:#c10000;margin-left:3px}.form-wrap.form-builder .frmb .sortable-options>li .remove::before{margin:0}.form-wrap.form-builder .frmb .sortable-options>li .remove:hover{background-color:#c10000 !important;text-decoration:none;color:#fff}.form-wrap.form-builder .frmb .sortable-options .option-selected{margin:0 3px 0 0;flex:none}.form-wrap.form-builder .frmb .sortable-options input[type=text]{flex-grow:1;flex-shrink:0;flex-basis:auto;width:100px;margin:0 3px;float:none}.form-wrap.form-builder .frmb .form-field .form-group{width:100%;clear:left;float:none}.form-wrap.form-builder .frmb .col-md-6 .form-elements,.form-wrap.form-builder .frmb .col-md-8 .form-elements{width:100%}.form-wrap.form-builder .frmb .field-options .add-area .add{clear:both}.form-wrap.form-builder .frmb .style-wrap button.selected{border:1px solid #000;margin-top:0;margin-right:1px;box-shadow:0 0 0 1px #fff inset;padding:1px 5px}.form-wrap.form-builder .frmb .form-elements{padding:10px 5px;background:#f7f7f7;border-radius:3px;margin:0;border:1px solid #c5c5c5}.form-wrap.form-builder .frmb .form-elements .input-wrap,.form-wrap.form-builder .frmb .form-elements .input-group-wrap{width:81.33333333%;margin-left:2%}.form-wrap.form-builder .frmb .form-elements .input-wrap>input[type=checkbox],.form-wrap.form-builder .frmb .form-elements .input-group-wrap>input[type=checkbox]{margin-top:8px}.form-wrap.form-builder .frmb .form-elements .btn-group{margin-left:2%}.form-wrap.form-builder .frmb .form-elements .add{clear:both}.form-wrap.form-builder .frmb .form-elements [contenteditable],.form-wrap.form-builder .frmb .form-elements select[multiple]{height:auto}.form-wrap.form-builder .frmb .form-elements [contenteditable].form-control{display:inline-block}.form-wrap.form-builder .frmb .form-elements [contenteditable].form-control,.form-wrap.form-builder .frmb .form-elements input[type=text],.form-wrap.form-builder .frmb .form-elements input[type=number],.form-wrap.form-builder .frmb .form-elements input[type=date],.form-wrap.form-builder .frmb .form-elements input[type=color],.form-wrap.form-builder .frmb .form-elements textarea,.form-wrap.form-builder .frmb .form-elements select{transition:background 250ms ease-in-out;padding:6px 12px;border:1px solid #c5c5c5;background-color:#fff}@media(max-width: 481px){.form-wrap.form-builder .frmb .form-elements .input-wrap{width:100%;margin-left:0}}.form-wrap.form-builder .frmb .form-elements input[type=number]{width:auto}.col-md-6 .form-wrap.form-builder .frmb .form-elements .false-label,.col-md-8 .form-wrap.form-builder .frmb .form-elements .false-label,.col-md-6 .form-wrap.form-builder .frmb .form-elements label,.col-md-8 .form-wrap.form-builder .frmb .form-elements label{display:block}.form-wrap.form-builder .frmb .form-elements .false-label:first-child,.form-wrap.form-builder .frmb .form-elements label:first-child{width:16.66666667%;padding-top:7px;margin-bottom:0;text-align:right;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;text-transform:capitalize}@media(max-width: 481px){.form-wrap.form-builder .frmb .form-elements .false-label:first-child,.form-wrap.form-builder .frmb .form-elements label:first-child{display:block;width:auto;text-align:left}.form-wrap.form-builder .frmb .form-elements .false-label:first-child.empty-label,.form-wrap.form-builder .frmb .form-elements label:first-child.empty-label{display:none}}.form-wrap.form-builder .frmb .form-elements .false-label.multiple,.form-wrap.form-builder .frmb .form-elements .false-label.required-label,.form-wrap.form-builder .frmb .form-elements .false-label.toggle-label,.form-wrap.form-builder .frmb .form-elements .false-label.roles-label,.form-wrap.form-builder .frmb .form-elements .false-label.other-label,.form-wrap.form-builder .frmb .form-elements label.multiple,.form-wrap.form-builder .frmb .form-elements label.required-label,.form-wrap.form-builder .frmb .form-elements label.toggle-label,.form-wrap.form-builder .frmb .form-elements label.roles-label,.form-wrap.form-builder .frmb .form-elements label.other-label{text-align:left;margin-bottom:-3px;font-weight:400;width:calc(81.3333% - 23px)}.form-wrap.form-builder .frmb .form-elements .input-group-wrap{display:flex;gap:2em}.form-wrap.form-builder .frmb .form-elements .input-group-wrap label{width:auto;display:block;text-align:left}.form-wrap.form-builder .frmb .form-elements .input-group-wrap .form-group{flex-direction:column;width:auto}.form-wrap.form-builder .frmb .form-elements .input-group-wrap .input-wrap{margin:0}.form-wrap.form-builder .frmb .form-elements input.error{border:1px solid #c10000}.form-wrap.form-builder .frmb .form-elements input.fld-maxlength,.form-wrap.form-builder .frmb .form-elements input.fld-rows{width:75px}.form-wrap.form-builder .frmb .form-elements input.field-error{background:#fefefe;border:1px solid #c5c5c5}.form-wrap.form-builder .frmb .form-elements label em{display:block;font-weight:400;font-size:.75em}.form-wrap.form-builder .frmb .form-elements label.maxlength-label{line-height:1em}.form-wrap.form-builder .frmb .form-elements .available-roles{display:none;padding:10px;margin:10px 0;background:#e6e6e6;box-shadow:inset 0 0 2px 0 #b3b3b3}@media(max-width: 481px){.form-wrap.form-builder .frmb .form-elements .available-roles{margin-left:0}}.form-wrap.form-builder .frmb .form-elements .available-roles label{font-weight:400;width:auto;float:none;display:inline}.form-wrap.form-builder .frmb .form-elements .available-roles input{display:inline;top:auto}.form-wrap.form-builder .autocomplete-field .sortable-options .option-selected{display:none}.formbuilder-mobile.form-wrap.form-builder .field-actions{opacity:1}.form-wrap.form-builder .snackbar{visibility:hidden;min-width:250px;margin-left:-125px;background-color:#333;color:#fff;text-align:center;border-radius:2px;padding:16px;position:fixed;z-index:1;left:50%;bottom:30px}.form-wrap.form-builder .snackbar.show{visibility:visible;animation:fadein .5s,fadeout .5s 2.5s}@keyframes fadein{from{bottom:0;opacity:0}to{bottom:30px;opacity:1}}@keyframes fadeout{from{bottom:30px;opacity:1}to{bottom:0;opacity:0}}.form-wrap.form-builder .ui-state-highlight{border-radius:3px;border:1px dashed #0d99f2;background-color:#e5f5f8;width:12px}.form-wrap.form-builder .moveHighlight{border:1px dashed #0d99f2 !important;background-color:#e5f5f8 !important}.form-wrap.form-builder .currentGridModeFieldHighlight{background-color:#e5f5f8 !important}.form-wrap.form-builder .grid-mode-help{background-color:#fff;border-top-left-radius:5px;border-top-right-radius:5px}.form-wrap.form-builder .grid-mode-help-row1{white-space:nowrap;text-overflow:ellipsis;overflow:hidden;max-width:1px}.form-wrap.form-builder .grid-mode-help-row2{white-space:nowrap}.form-wrap.form-builder .colWithInsertButtons{padding-left:0 !important;padding-right:0 !important;flex:95 1 0% !important}.form-wrap.form-builder .rowWrapper{margin-left:0 !important;margin-right:0 !important}.form-wrap.form-builder .rowWrapper:last-child{flex-grow:1}.form-wrap.form-builder .rowWrapper:not(.tempRowWrapper){padding-top:1em;padding-bottom:1em}.form-wrap.form-builder .btnAddControl{border:0;background-color:unset}.form-wrap.form-builder .hoverColumnDropStyle{border:1px dashed #0d99f2;border-radius:3px;background-color:#e5f5f8;width:20px;position:fixed;margin-left:40px}.form-wrap.form-builder .hoverDropStyleInverse{background-color:#0d99f2;border:1px dashed #e5f5f8;min-height:20px}.form-wrap.form-builder .hoverDropStyleInverse .colWrapper{max-width:calc(100% - 40px)}.form-wrap.form-builder .stage-wrap>.hoverDropStyleInverse{width:100%}.form-wrap.form-builder .rowWrapper>.hoverDropStyleInverse{min-width:40px;flex-grow:1}.form-wrap.form-builder .hoverDropStyleInverse:last-child{flex-grow:1}.form-wrap.form-builder .invisibleRowPlaceholder{width:0 !important;position:fixed !important;left:-100px !important}.form-wrap.form-builder ol.options-no-select input[type=radio]{display:none}.form-wrap.form-builder .hidden{display:none !important}.form-wrap.form-builder .flex{display:flex !important}.form-wrap.form-builder *[tooltip]{position:relative}.form-wrap.form-builder *[tooltip]:hover::after{background:rgba(0,0,0,.9);border-radius:5px 5px 5px 0;bottom:23px;color:#fff;content:attr(tooltip);padding:10px 5px;position:absolute;z-index:98;left:2px;width:230px;text-shadow:none;font-size:12px;line-height:1.5em;cursor:default}.form-wrap.form-builder *[tooltip]:hover::before{border:solid;border-color:#222 rgba(0,0,0,0);border-width:6px 6px 0;bottom:17px;content:\"\";left:2px;position:absolute;z-index:99;cursor:default}.form-wrap.form-builder .tooltip-element{visibility:visible;color:#fff;background:#000;width:16px;height:16px;border-radius:8px;display:inline-block;text-align:center;line-height:16px;margin:0 5px;font-size:12px;cursor:default}.form-wrap.form-builder .kc-toggle{display:flex;align-items:center}.form-wrap.form-builder .kc-toggle span{position:relative;width:48px;height:24px;background:#e6e6e6;display:inline-block;border-radius:4px;border:1px solid #ccc;padding:2px;overflow:hidden;margin-right:5px;will-change:transform}.form-wrap.form-builder .kc-toggle span::after,.form-wrap.form-builder .kc-toggle span::before{position:absolute;display:inline-block;top:0}.form-wrap.form-builder .kc-toggle span::after{position:relative;content:\"\";width:50%;height:calc(100% - 2px);left:0;border-radius:3px;background:linear-gradient(to bottom, white 0%, #ccc 100%);border:1px solid #999;transition:transform 100ms;transform:translateX(0)}.form-wrap.form-builder .kc-toggle span::before{border-radius:4px;top:2px;left:2px;content:\"\";width:calc(100% - 4px);height:18px;box-shadow:0 0 1px 1px #b3b3b3 inset;background-color:rgba(0,0,0,0)}.form-wrap.form-builder .kc-toggle input{height:0;overflow:hidden;width:0;opacity:0;pointer-events:none;margin:0}.form-wrap.form-builder .kc-toggle input:checked+span::after{transform:translateX(calc(100% - 4px))}.form-wrap.form-builder .kc-toggle input:checked+span::before{background-color:#6fc665}.form-wrap.form-builder.controls-left{flex-direction:row-reverse}.form-wrap.form-builder::after{content:\"\";display:table;clear:both}.form-wrap.form-builder .cb-wrap,.form-wrap.form-builder .stage-wrap{vertical-align:top}.form-wrap.form-builder .form-elements,.form-wrap.form-builder .multi-row span,.form-wrap.form-builder textarea{display:block}.form-wrap.form-builder .form-group{display:flex;flex-direction:row}.form-wrap.form-builder .form-elements::after,.form-wrap.form-builder .form-group::after{content:\".\";display:block;height:0;clear:both;visibility:hidden}.form-wrap.form-builder .form-elements .field-options div:hover,.form-wrap.form-builder .frmb .legend,.form-wrap.form-builder .frmb .prev-holder{cursor:move}.form-wrap.form-builder .frmb-tt{display:none;position:absolute;top:0;left:0;border:1px solid #262626;background-color:#666;border-radius:5px;padding:5px;color:#fff;z-index:20;text-align:left;font-size:12px;pointer-events:none}.form-wrap.form-builder .frmb-tt::before{border-color:#262626 rgba(0,0,0,0);bottom:-11px}.form-wrap.form-builder .frmb-tt::before,.form-wrap.form-builder .frmb-tt::after{content:\"\";position:absolute;border-style:solid;border-width:10px 10px 0;border-color:#666 rgba(0,0,0,0);display:block;width:0;z-index:1;margin-left:-10px;bottom:-10px;left:20px}.form-wrap.form-builder .frmb-tt a{text-decoration:underline;color:#fff}.form-builder-overlay{position:fixed;top:0;left:0;width:100%;height:100%;background-color:rgba(0,0,0,.5);display:none;z-index:10}.form-builder-overlay.visible{display:block}.form-builder-dialog{position:absolute;border-radius:5px;background:#fff;z-index:20;transform:translate(-50%, -50%);top:0;left:0;padding:10px;box-shadow:0 3px 10px #000;min-width:166px;max-height:80%;overflow-y:scroll}.form-builder-dialog h3{margin-top:0}.form-builder-dialog.data-dialog{width:65%;background-color:#23241f}.form-builder-dialog.data-dialog pre{background:none;border:0 none;box-shadow:none;margin:0;color:#f2f2f2}.form-builder-dialog.positioned{transform:translate(-50%, -100%)}.form-builder-dialog.positioned .button-wrap::before{content:\"\";width:0;height:0;border-left:15px solid rgba(0,0,0,0);border-right:15px solid rgba(0,0,0,0);border-top:10px solid #fff;position:absolute;left:50%;top:100%;transform:translate(-50%, 10px)}.form-builder-dialog .button-wrap{position:relative;margin-top:10px;text-align:right;clear:both}.form-builder-dialog .button-wrap .btn{margin-left:10px}", ""]);
// Exports
/* harmony default export */ __webpack_exports__["default"] = (___CSS_LOADER_EXPORT___);


/***/ }),

/***/ 278:
/***/ (function(module) {

"use strict";


/*
  MIT License http://www.opensource.org/licenses/mit-license.php
  Author Tobias Koppers @sokra
*/
module.exports = function (cssWithMappingToString) {
  var list = [];

  // return the list of modules as css string
  list.toString = function toString() {
    return this.map(function (item) {
      var content = "";
      var needLayer = typeof item[5] !== "undefined";
      if (item[4]) {
        content += "@supports (".concat(item[4], ") {");
      }
      if (item[2]) {
        content += "@media ".concat(item[2], " {");
      }
      if (needLayer) {
        content += "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {");
      }
      content += cssWithMappingToString(item);
      if (needLayer) {
        content += "}";
      }
      if (item[2]) {
        content += "}";
      }
      if (item[4]) {
        content += "}";
      }
      return content;
    }).join("");
  };

  // import a list of modules into the list
  list.i = function i(modules, media, dedupe, supports, layer) {
    if (typeof modules === "string") {
      modules = [[null, modules, undefined]];
    }
    var alreadyImportedModules = {};
    if (dedupe) {
      for (var k = 0; k < this.length; k++) {
        var id = this[k][0];
        if (id != null) {
          alreadyImportedModules[id] = true;
        }
      }
    }
    for (var _k = 0; _k < modules.length; _k++) {
      var item = [].concat(modules[_k]);
      if (dedupe && alreadyImportedModules[item[0]]) {
        continue;
      }
      if (typeof layer !== "undefined") {
        if (typeof item[5] === "undefined") {
          item[5] = layer;
        } else {
          item[1] = "@layer".concat(item[5].length > 0 ? " ".concat(item[5]) : "", " {").concat(item[1], "}");
          item[5] = layer;
        }
      }
      if (media) {
        if (!item[2]) {
          item[2] = media;
        } else {
          item[1] = "@media ".concat(item[2], " {").concat(item[1], "}");
          item[2] = media;
        }
      }
      if (supports) {
        if (!item[4]) {
          item[4] = "".concat(supports);
        } else {
          item[1] = "@supports (".concat(item[4], ") {").concat(item[1], "}");
          item[4] = supports;
        }
      }
      list.push(item);
    }
  };
  return list;
};

/***/ }),

/***/ 21:
/***/ (function(module) {

"use strict";


module.exports = function (url, options) {
  if (!options) {
    options = {};
  }
  if (!url) {
    return url;
  }
  url = String(url.__esModule ? url.default : url);

  // If url is already wrapped in quotes, remove them
  if (/^['"].*['"]$/.test(url)) {
    url = url.slice(1, -1);
  }
  if (options.hash) {
    url += options.hash;
  }

  // Should url be wrapped?
  // See https://drafts.csswg.org/css-values-3/#urls
  if (/["'() \t\n]|(%20)/.test(url) || options.needQuotes) {
    return "\"".concat(url.replace(/"/g, '\\"').replace(/\n/g, "\\n"), "\"");
  }
  return url;
};

/***/ }),

/***/ 645:
/***/ (function(module) {

"use strict";


module.exports = function (i) {
  return i[1];
};

/***/ }),

/***/ 781:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var root = __webpack_require__(433);

/** Built-in value references. */
var Symbol = root.Symbol;

module.exports = Symbol;


/***/ }),

/***/ 148:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var Symbol = __webpack_require__(781),
    getRawTag = __webpack_require__(903),
    objectToString = __webpack_require__(738);

/** `Object#toString` result references. */
var nullTag = '[object Null]',
    undefinedTag = '[object Undefined]';

/** Built-in value references. */
var symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/**
 * The base implementation of `getTag` without fallbacks for buggy environments.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  if (value == null) {
    return value === undefined ? undefinedTag : nullTag;
  }
  return (symToStringTag && symToStringTag in Object(value))
    ? getRawTag(value)
    : objectToString(value);
}

module.exports = baseGetTag;


/***/ }),

/***/ 812:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var trimmedEndIndex = __webpack_require__(140);

/** Used to match leading whitespace. */
var reTrimStart = /^\s+/;

/**
 * The base implementation of `_.trim`.
 *
 * @private
 * @param {string} string The string to trim.
 * @returns {string} Returns the trimmed string.
 */
function baseTrim(string) {
  return string
    ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, '')
    : string;
}

module.exports = baseTrim;


/***/ }),

/***/ 380:
/***/ (function(module) {

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

module.exports = freeGlobal;


/***/ }),

/***/ 903:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var Symbol = __webpack_require__(781);

/** Used for built-in method references. */
var objectProto = Object.prototype;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/** Built-in value references. */
var symToStringTag = Symbol ? Symbol.toStringTag : undefined;

/**
 * A specialized version of `baseGetTag` which ignores `Symbol.toStringTag` values.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the raw `toStringTag`.
 */
function getRawTag(value) {
  var isOwn = hasOwnProperty.call(value, symToStringTag),
      tag = value[symToStringTag];

  try {
    value[symToStringTag] = undefined;
    var unmasked = true;
  } catch (e) {}

  var result = nativeObjectToString.call(value);
  if (unmasked) {
    if (isOwn) {
      value[symToStringTag] = tag;
    } else {
      delete value[symToStringTag];
    }
  }
  return result;
}

module.exports = getRawTag;


/***/ }),

/***/ 738:
/***/ (function(module) {

/** Used for built-in method references. */
var objectProto = Object.prototype;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var nativeObjectToString = objectProto.toString;

/**
 * Converts `value` to a string using `Object.prototype.toString`.
 *
 * @private
 * @param {*} value The value to convert.
 * @returns {string} Returns the converted string.
 */
function objectToString(value) {
  return nativeObjectToString.call(value);
}

module.exports = objectToString;


/***/ }),

/***/ 433:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var freeGlobal = __webpack_require__(380);

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

module.exports = root;


/***/ }),

/***/ 140:
/***/ (function(module) {

/** Used to match a single whitespace character. */
var reWhitespace = /\s/;

/**
 * Used by `_.trim` and `_.trimEnd` to get the index of the last non-whitespace
 * character of `string`.
 *
 * @private
 * @param {string} string The string to inspect.
 * @returns {number} Returns the index of the last non-whitespace character.
 */
function trimmedEndIndex(string) {
  var index = string.length;

  while (index-- && reWhitespace.test(string.charAt(index))) {}
  return index;
}

module.exports = trimmedEndIndex;


/***/ }),

/***/ 177:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var isObject = __webpack_require__(953),
    now = __webpack_require__(664),
    toNumber = __webpack_require__(378);

/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeMax = Math.max,
    nativeMin = Math.min;

/**
 * Creates a debounced function that delays invoking `func` until after `wait`
 * milliseconds have elapsed since the last time the debounced function was
 * invoked. The debounced function comes with a `cancel` method to cancel
 * delayed `func` invocations and a `flush` method to immediately invoke them.
 * Provide `options` to indicate whether `func` should be invoked on the
 * leading and/or trailing edge of the `wait` timeout. The `func` is invoked
 * with the last arguments provided to the debounced function. Subsequent
 * calls to the debounced function return the result of the last `func`
 * invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the debounced function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.debounce` and `_.throttle`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to debounce.
 * @param {number} [wait=0] The number of milliseconds to delay.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=false]
 *  Specify invoking on the leading edge of the timeout.
 * @param {number} [options.maxWait]
 *  The maximum time `func` is allowed to be delayed before it's invoked.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new debounced function.
 * @example
 *
 * // Avoid costly calculations while the window size is in flux.
 * jQuery(window).on('resize', _.debounce(calculateLayout, 150));
 *
 * // Invoke `sendMail` when clicked, debouncing subsequent calls.
 * jQuery(element).on('click', _.debounce(sendMail, 300, {
 *   'leading': true,
 *   'trailing': false
 * }));
 *
 * // Ensure `batchLog` is invoked once after 1 second of debounced calls.
 * var debounced = _.debounce(batchLog, 250, { 'maxWait': 1000 });
 * var source = new EventSource('/stream');
 * jQuery(source).on('message', debounced);
 *
 * // Cancel the trailing debounced invocation.
 * jQuery(window).on('popstate', debounced.cancel);
 */
function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  wait = toNumber(wait) || 0;
  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;

    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any `maxWait` timer.
    lastInvokeTime = time;
    // Start the timer for the trailing edge.
    timerId = setTimeout(timerExpired, wait);
    // Invoke the leading edge.
    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        timeWaiting = wait - timeSinceLastCall;

    return maxing
      ? nativeMin(timeWaiting, maxWait - timeSinceLastInvoke)
      : timeWaiting;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime;

    // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the `maxWait` limit.
    return (lastCallTime === undefined || (timeSinceLastCall >= wait) ||
      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));
  }

  function timerExpired() {
    var time = now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // Restart the timer.
    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    timerId = undefined;

    // Only invoke if we have `lastArgs` which means `func` has been
    // debounced at least once.
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }
    lastInvokeTime = 0;
    lastArgs = lastCallTime = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);

    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        // Handle invocations in a tight loop.
        clearTimeout(timerId);
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}

module.exports = debounce;


/***/ }),

/***/ 953:
/***/ (function(module) {

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return value != null && (type == 'object' || type == 'function');
}

module.exports = isObject;


/***/ }),

/***/ 934:
/***/ (function(module) {

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return value != null && typeof value == 'object';
}

module.exports = isObjectLike;


/***/ }),

/***/ 414:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var baseGetTag = __webpack_require__(148),
    isObjectLike = __webpack_require__(934);

/** `Object#toString` result references. */
var symbolTag = '[object Symbol]';

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && baseGetTag(value) == symbolTag);
}

module.exports = isSymbol;


/***/ }),

/***/ 664:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var root = __webpack_require__(433);

/**
 * Gets the timestamp of the number of milliseconds that have elapsed since
 * the Unix epoch (1 January 1970 00:00:00 UTC).
 *
 * @static
 * @memberOf _
 * @since 2.4.0
 * @category Date
 * @returns {number} Returns the timestamp.
 * @example
 *
 * _.defer(function(stamp) {
 *   console.log(_.now() - stamp);
 * }, _.now());
 * // => Logs the number of milliseconds it took for the deferred invocation.
 */
var now = function() {
  return root.Date.now();
};

module.exports = now;


/***/ }),

/***/ 858:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var debounce = __webpack_require__(177),
    isObject = __webpack_require__(953);

/** Error message constants. */
var FUNC_ERROR_TEXT = 'Expected a function';

/**
 * Creates a throttled function that only invokes `func` at most once per
 * every `wait` milliseconds. The throttled function comes with a `cancel`
 * method to cancel delayed `func` invocations and a `flush` method to
 * immediately invoke them. Provide `options` to indicate whether `func`
 * should be invoked on the leading and/or trailing edge of the `wait`
 * timeout. The `func` is invoked with the last arguments provided to the
 * throttled function. Subsequent calls to the throttled function return the
 * result of the last `func` invocation.
 *
 * **Note:** If `leading` and `trailing` options are `true`, `func` is
 * invoked on the trailing edge of the timeout only if the throttled function
 * is invoked more than once during the `wait` timeout.
 *
 * If `wait` is `0` and `leading` is `false`, `func` invocation is deferred
 * until to the next tick, similar to `setTimeout` with a timeout of `0`.
 *
 * See [David Corbacho's article](https://css-tricks.com/debouncing-throttling-explained-examples/)
 * for details over the differences between `_.throttle` and `_.debounce`.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Function
 * @param {Function} func The function to throttle.
 * @param {number} [wait=0] The number of milliseconds to throttle invocations to.
 * @param {Object} [options={}] The options object.
 * @param {boolean} [options.leading=true]
 *  Specify invoking on the leading edge of the timeout.
 * @param {boolean} [options.trailing=true]
 *  Specify invoking on the trailing edge of the timeout.
 * @returns {Function} Returns the new throttled function.
 * @example
 *
 * // Avoid excessively updating the position while scrolling.
 * jQuery(window).on('scroll', _.throttle(updatePosition, 100));
 *
 * // Invoke `renewToken` when the click event is fired, but not more than once every 5 minutes.
 * var throttled = _.throttle(renewToken, 300000, { 'trailing': false });
 * jQuery(element).on('click', throttled);
 *
 * // Cancel the trailing throttled invocation.
 * jQuery(window).on('popstate', throttled.cancel);
 */
function throttle(func, wait, options) {
  var leading = true,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  if (isObject(options)) {
    leading = 'leading' in options ? !!options.leading : leading;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }
  return debounce(func, wait, {
    'leading': leading,
    'maxWait': wait,
    'trailing': trailing
  });
}

module.exports = throttle;


/***/ }),

/***/ 378:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var baseTrim = __webpack_require__(812),
    isObject = __webpack_require__(953),
    isSymbol = __webpack_require__(414);

/** Used as references for various `Number` constants. */
var NAN = 0 / 0;

/** Used to detect bad signed hexadecimal string values. */
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

/** Used to detect binary string values. */
var reIsBinary = /^0b[01]+$/i;

/** Used to detect octal string values. */
var reIsOctal = /^0o[0-7]+$/i;

/** Built-in method references without a dependency on `root`. */
var freeParseInt = parseInt;

/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = baseTrim(value);
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}

module.exports = toNumber;


/***/ }),

/***/ 252:
/***/ (function(module) {

/*!
 * mi18n - https://github.com/Draggable/mi18n
 * Version: 0.4.7
 * Author: Kevin Chappell <kevin.b.chappell@gmail.com> (http://kevin-chappell.com)
 */
module.exports=function(e){var t={};function n(r){if(t[r])return t[r].exports;var o=t[r]={i:r,l:!1,exports:{}};return e[r].call(o.exports,o,o.exports,n),o.l=!0,o.exports}return n.m=e,n.c=t,n.d=function(e,t,r){n.o(e,t)||Object.defineProperty(e,t,{enumerable:!0,get:r})},n.r=function(e){"undefined"!=typeof Symbol&&Symbol.toStringTag&&Object.defineProperty(e,Symbol.toStringTag,{value:"Module"}),Object.defineProperty(e,"__esModule",{value:!0})},n.t=function(e,t){if(1&t&&(e=n(e)),8&t)return e;if(4&t&&"object"==typeof e&&e&&e.__esModule)return e;var r=Object.create(null);if(n.r(r),Object.defineProperty(r,"default",{enumerable:!0,value:e}),2&t&&"string"!=typeof e)for(var o in e)n.d(r,o,function(t){return e[t]}.bind(null,o));return r},n.n=function(e){var t=e&&e.__esModule?function(){return e.default}:function(){return e};return n.d(t,"a",t),t},n.o=function(e,t){return Object.prototype.hasOwnProperty.call(e,t)},n.p="",n(n.s=7)}([function(e,t,n){"use strict";var r="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(e){return typeof e}:function(e){return e&&"function"==typeof Symbol&&e.constructor===Symbol&&e!==Symbol.prototype?"symbol":typeof e},o=n(2),i=n(10),s=Object.prototype.toString;function a(e){return"[object Array]"===s.call(e)}function u(e){return null!==e&&"object"===(void 0===e?"undefined":r(e))}function c(e){return"[object Function]"===s.call(e)}function f(e,t){if(null!==e&&void 0!==e)if("object"!==(void 0===e?"undefined":r(e))&&(e=[e]),a(e))for(var n=0,o=e.length;n<o;n++)t.call(null,e[n],n,e);else for(var i in e)Object.prototype.hasOwnProperty.call(e,i)&&t.call(null,e[i],i,e)}e.exports={isArray:a,isArrayBuffer:function(e){return"[object ArrayBuffer]"===s.call(e)},isBuffer:i,isFormData:function(e){return"undefined"!=typeof FormData&&e instanceof FormData},isArrayBufferView:function(e){return"undefined"!=typeof ArrayBuffer&&ArrayBuffer.isView?ArrayBuffer.isView(e):e&&e.buffer&&e.buffer instanceof ArrayBuffer},isString:function(e){return"string"==typeof e},isNumber:function(e){return"number"==typeof e},isObject:u,isUndefined:function(e){return void 0===e},isDate:function(e){return"[object Date]"===s.call(e)},isFile:function(e){return"[object File]"===s.call(e)},isBlob:function(e){return"[object Blob]"===s.call(e)},isFunction:c,isStream:function(e){return u(e)&&c(e.pipe)},isURLSearchParams:function(e){return"undefined"!=typeof URLSearchParams&&e instanceof URLSearchParams},isStandardBrowserEnv:function(){return("undefined"==typeof navigator||"ReactNative"!==navigator.product)&&"undefined"!=typeof window&&"undefined"!=typeof document},forEach:f,merge:function e(){var t={};function n(n,o){"object"===r(t[o])&&"object"===(void 0===n?"undefined":r(n))?t[o]=e(t[o],n):t[o]=n}for(var o=0,i=arguments.length;o<i;o++)f(arguments[o],n);return t},extend:function(e,t,n){return f(t,function(t,r){e[r]=n&&"function"==typeof t?o(t,n):t}),e},trim:function(e){return e.replace(/^\s*/,"").replace(/\s*$/,"")}}},function(e,t,n){"use strict";(function(t){var r=n(0),o=n(13),i={"Content-Type":"application/x-www-form-urlencoded"};function s(e,t){!r.isUndefined(e)&&r.isUndefined(e["Content-Type"])&&(e["Content-Type"]=t)}var a={adapter:function(){var e;return"undefined"!=typeof XMLHttpRequest?e=n(3):void 0!==t&&(e=n(3)),e}(),transformRequest:[function(e,t){return o(t,"Content-Type"),r.isFormData(e)||r.isArrayBuffer(e)||r.isBuffer(e)||r.isStream(e)||r.isFile(e)||r.isBlob(e)?e:r.isArrayBufferView(e)?e.buffer:r.isURLSearchParams(e)?(s(t,"application/x-www-form-urlencoded;charset=utf-8"),e.toString()):r.isObject(e)?(s(t,"application/json;charset=utf-8"),JSON.stringify(e)):e}],transformResponse:[function(e){if("string"==typeof e)try{e=JSON.parse(e)}catch(e){}return e}],timeout:0,xsrfCookieName:"XSRF-TOKEN",xsrfHeaderName:"X-XSRF-TOKEN",maxContentLength:-1,validateStatus:function(e){return e>=200&&e<300},headers:{common:{Accept:"application/json, text/plain, */*"}}};r.forEach(["delete","get","head"],function(e){a.headers[e]={}}),r.forEach(["post","put","patch"],function(e){a.headers[e]=r.merge(i)}),e.exports=a}).call(this,n(12))},function(e,t,n){"use strict";e.exports=function(e,t){return function(){for(var n=new Array(arguments.length),r=0;r<n.length;r++)n[r]=arguments[r];return e.apply(t,n)}}},function(e,t,n){"use strict";var r=n(0),o=n(14),i=n(16),s=n(17),a=n(18),u=n(4),c="undefined"!=typeof window&&window.btoa&&window.btoa.bind(window)||n(19);e.exports=function(e){return new Promise(function(t,f){var l=e.data,p=e.headers;r.isFormData(l)&&delete p["Content-Type"];var d=new XMLHttpRequest,h="onreadystatechange",g=!1;if("undefined"==typeof window||!window.XDomainRequest||"withCredentials"in d||a(e.url)||(d=new window.XDomainRequest,h="onload",g=!0,d.onprogress=function(){},d.ontimeout=function(){}),e.auth){var m=e.auth.username||"",y=e.auth.password||"";p.Authorization="Basic "+c(m+":"+y)}if(d.open(e.method.toUpperCase(),i(e.url,e.params,e.paramsSerializer),!0),d.timeout=e.timeout,d[h]=function(){if(d&&(4===d.readyState||g)&&(0!==d.status||d.responseURL&&0===d.responseURL.indexOf("file:"))){var n="getAllResponseHeaders"in d?s(d.getAllResponseHeaders()):null,r={data:e.responseType&&"text"!==e.responseType?d.response:d.responseText,status:1223===d.status?204:d.status,statusText:1223===d.status?"No Content":d.statusText,headers:n,config:e,request:d};o(t,f,r),d=null}},d.onerror=function(){f(u("Network Error",e,null,d)),d=null},d.ontimeout=function(){f(u("timeout of "+e.timeout+"ms exceeded",e,"ECONNABORTED",d)),d=null},r.isStandardBrowserEnv()){var v=n(20),w=(e.withCredentials||a(e.url))&&e.xsrfCookieName?v.read(e.xsrfCookieName):void 0;w&&(p[e.xsrfHeaderName]=w)}if("setRequestHeader"in d&&r.forEach(p,function(e,t){void 0===l&&"content-type"===t.toLowerCase()?delete p[t]:d.setRequestHeader(t,e)}),e.withCredentials&&(d.withCredentials=!0),e.responseType)try{d.responseType=e.responseType}catch(t){if("json"!==e.responseType)throw t}"function"==typeof e.onDownloadProgress&&d.addEventListener("progress",e.onDownloadProgress),"function"==typeof e.onUploadProgress&&d.upload&&d.upload.addEventListener("progress",e.onUploadProgress),e.cancelToken&&e.cancelToken.promise.then(function(e){d&&(d.abort(),f(e),d=null)}),void 0===l&&(l=null),d.send(l)})}},function(e,t,n){"use strict";var r=n(15);e.exports=function(e,t,n,o,i){var s=new Error(e);return r(s,t,n,o,i)}},function(e,t,n){"use strict";e.exports=function(e){return!(!e||!e.__CANCEL__)}},function(e,t,n){"use strict";function r(e){this.message=e}r.prototype.toString=function(){return"Cancel"+(this.message?": "+this.message:"")},r.prototype.__CANCEL__=!0,e.exports=r},function(e,t,n){"use strict";t.__esModule=!0,t.I18N=void 0;var r="function"==typeof Symbol&&"symbol"==typeof Symbol.iterator?function(e){return typeof e}:function(e){return e&&"function"==typeof Symbol&&e.constructor===Symbol&&e!==Symbol.prototype?"symbol":typeof e},o=function(){function e(e,t){for(var n=0;n<t.length;n++){var r=t[n];r.enumerable=r.enumerable||!1,r.configurable=!0,"value"in r&&(r.writable=!0),Object.defineProperty(e,r.key,r)}}return function(t,n,r){return n&&e(t.prototype,n),r&&e(t,r),t}}(),i=n(8),s={extension:".lang",location:"assets/lang/",langs:["en-US"],locale:"en-US",override:{}},a=t.I18N=function(){function e(){var t=arguments.length>0&&void 0!==arguments[0]?arguments[0]:s;!function(e,t){if(!(e instanceof t))throw new TypeError("Cannot call a class as a function")}(this,e),this.langs=Object.create(null),this.loaded=[],this.processConfig(t)}return e.prototype.processConfig=function(e){var t=this,n=Object.assign({},s,e),r=n.location,o=function(e,t){var n={};for(var r in e)t.indexOf(r)>=0||Object.prototype.hasOwnProperty.call(e,r)&&(n[r]=e[r]);return n}(n,["location"]),i=r.replace(/\/?$/,"/");this.config=Object.assign({},{location:i},o);var a=this.config,u=a.override,c=a.preloaded,f=void 0===c?{}:c,l=Object.entries(this.langs).concat(Object.entries(u||f));this.langs=l.reduce(function(e,n){var r=n[0],o=n[1];return e[r]=t.applyLanguage.call(t,r,o),e},{}),this.locale=this.config.locale||this.config.langs[0]},e.prototype.init=function(e){return this.processConfig.call(this,Object.assign({},this.config,e)),this.setCurrent(this.locale)},e.prototype.addLanguage=function(e){var t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:{};t="string"==typeof t?this.processFile.call(this,t):t,this.applyLanguage.call(this,e,t),this.config.langs.push("locale")},e.prototype.getValue=function(e){var t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:this.locale;return this.langs[t]&&this.langs[t][e]||this.getFallbackValue(e)},e.prototype.getFallbackValue=function(e){var t=Object.values(this.langs).find(function(t){return t[e]});return t&&t[e]},e.prototype.makeSafe=function(e){var t={"{":"\\{","}":"\\}","|":"\\|"};return e=e.replace(/\{|\}|\|/g,function(e){return t[e]}),new RegExp(e,"g")},e.prototype.put=function(e,t){return this.current[e]=t},e.prototype.get=function(e,t){var n=this.getValue(e);if(n){var o=n.match(/\{[^}]+?\}/g),i=void 0;if(t&&o)if("object"===(void 0===t?"undefined":r(t)))for(var s=0;s<o.length;s++)i=o[s].substring(1,o[s].length-1),n=n.replace(this.makeSafe(o[s]),t[i]||"");else n=n.replace(/\{[^}]+?\}/g,t);return n}},e.prototype.fromFile=function(e){for(var t,n=e.split("\n"),r={},o=0;o<n.length;o++)(t=n[o].match(/^(.+?) *?= *?([^\n]+)/))&&(r[t[1]]=t[2].replace(/^\s+|\s+$/,""));return r},e.prototype.processFile=function(e){return this.fromFile(e.replace(/\n\n/g,"\n"))},e.prototype.loadLang=function(e){var t=!(arguments.length>1&&void 0!==arguments[1])||arguments[1],n=this;return new Promise(function(r,o){if(-1!==n.loaded.indexOf(e)&&t)return n.applyLanguage.call(n,n.langs[e]),r(n.langs[e]);var s=[n.config.location,e,n.config.extension].join("");return(0,i.get)(s).then(function(t){var o=t.data,i=n.processFile(o);return n.applyLanguage.call(n,e,i),n.loaded.push(e),r(n.langs[e])}).catch(function(){var t=n.applyLanguage.call(n,e);r(t)})})},e.prototype.applyLanguage=function(e){var t=arguments.length>1&&void 0!==arguments[1]?arguments[1]:{},n=this.config.override[e]||{},r=this.langs[e]||{};return this.langs[e]=Object.assign({},r,t,n),this.langs[e]},e.prototype.setCurrent=function(){var e=this,t=arguments.length>0&&void 0!==arguments[0]?arguments[0]:"en-US";return this.loadLang(t).then(function(){return e.locale=t,e.current=e.langs[t],e.current})},o(e,[{key:"getLangs",get:function(){return this.config.langs}}]),e}();t.default=new a},function(e,t,n){"use strict";e.exports=n(9)},function(e,t,n){"use strict";var r=n(0),o=n(2),i=n(11),s=n(1);function a(e){var t=new i(e),n=o(i.prototype.request,t);return r.extend(n,i.prototype,t),r.extend(n,t),n}var u=a(s);u.Axios=i,u.create=function(e){return a(r.merge(s,e))},u.Cancel=n(6),u.CancelToken=n(26),u.isCancel=n(5),u.all=function(e){return Promise.all(e)},u.spread=n(27),e.exports=u,e.exports.default=u},function(e,t,n){"use strict";
/*!
 * Determine if an object is a Buffer
 *
 * @author   Feross Aboukhadijeh <https://feross.org>
 * @license  MIT
 */function r(e){return!!e.constructor&&"function"==typeof e.constructor.isBuffer&&e.constructor.isBuffer(e)}e.exports=function(e){return null!=e&&(r(e)||function(e){return"function"==typeof e.readFloatLE&&"function"==typeof e.slice&&r(e.slice(0,0))}(e)||!!e._isBuffer)}},function(e,t,n){"use strict";var r=n(1),o=n(0),i=n(21),s=n(22);function a(e){this.defaults=e,this.interceptors={request:new i,response:new i}}a.prototype.request=function(e){"string"==typeof e&&(e=o.merge({url:arguments[0]},arguments[1])),(e=o.merge(r,{method:"get"},this.defaults,e)).method=e.method.toLowerCase();var t=[s,void 0],n=Promise.resolve(e);for(this.interceptors.request.forEach(function(e){t.unshift(e.fulfilled,e.rejected)}),this.interceptors.response.forEach(function(e){t.push(e.fulfilled,e.rejected)});t.length;)n=n.then(t.shift(),t.shift());return n},o.forEach(["delete","get","head","options"],function(e){a.prototype[e]=function(t,n){return this.request(o.merge(n||{},{method:e,url:t}))}}),o.forEach(["post","put","patch"],function(e){a.prototype[e]=function(t,n,r){return this.request(o.merge(r||{},{method:e,url:t,data:n}))}}),e.exports=a},function(e,t,n){"use strict";var r,o,i=e.exports={};function s(){throw new Error("setTimeout has not been defined")}function a(){throw new Error("clearTimeout has not been defined")}function u(e){if(r===setTimeout)return setTimeout(e,0);if((r===s||!r)&&setTimeout)return r=setTimeout,setTimeout(e,0);try{return r(e,0)}catch(t){try{return r.call(null,e,0)}catch(t){return r.call(this,e,0)}}}!function(){try{r="function"==typeof setTimeout?setTimeout:s}catch(e){r=s}try{o="function"==typeof clearTimeout?clearTimeout:a}catch(e){o=a}}();var c,f=[],l=!1,p=-1;function d(){l&&c&&(l=!1,c.length?f=c.concat(f):p=-1,f.length&&h())}function h(){if(!l){var e=u(d);l=!0;for(var t=f.length;t;){for(c=f,f=[];++p<t;)c&&c[p].run();p=-1,t=f.length}c=null,l=!1,function(e){if(o===clearTimeout)return clearTimeout(e);if((o===a||!o)&&clearTimeout)return o=clearTimeout,clearTimeout(e);try{o(e)}catch(t){try{return o.call(null,e)}catch(t){return o.call(this,e)}}}(e)}}function g(e,t){this.fun=e,this.array=t}function m(){}i.nextTick=function(e){var t=new Array(arguments.length-1);if(arguments.length>1)for(var n=1;n<arguments.length;n++)t[n-1]=arguments[n];f.push(new g(e,t)),1!==f.length||l||u(h)},g.prototype.run=function(){this.fun.apply(null,this.array)},i.title="browser",i.browser=!0,i.env={},i.argv=[],i.version="",i.versions={},i.on=m,i.addListener=m,i.once=m,i.off=m,i.removeListener=m,i.removeAllListeners=m,i.emit=m,i.prependListener=m,i.prependOnceListener=m,i.listeners=function(e){return[]},i.binding=function(e){throw new Error("process.binding is not supported")},i.cwd=function(){return"/"},i.chdir=function(e){throw new Error("process.chdir is not supported")},i.umask=function(){return 0}},function(e,t,n){"use strict";var r=n(0);e.exports=function(e,t){r.forEach(e,function(n,r){r!==t&&r.toUpperCase()===t.toUpperCase()&&(e[t]=n,delete e[r])})}},function(e,t,n){"use strict";var r=n(4);e.exports=function(e,t,n){var o=n.config.validateStatus;n.status&&o&&!o(n.status)?t(r("Request failed with status code "+n.status,n.config,null,n.request,n)):e(n)}},function(e,t,n){"use strict";e.exports=function(e,t,n,r,o){return e.config=t,n&&(e.code=n),e.request=r,e.response=o,e}},function(e,t,n){"use strict";var r=n(0);function o(e){return encodeURIComponent(e).replace(/%40/gi,"@").replace(/%3A/gi,":").replace(/%24/g,"$").replace(/%2C/gi,",").replace(/%20/g,"+").replace(/%5B/gi,"[").replace(/%5D/gi,"]")}e.exports=function(e,t,n){if(!t)return e;var i;if(n)i=n(t);else if(r.isURLSearchParams(t))i=t.toString();else{var s=[];r.forEach(t,function(e,t){null!==e&&void 0!==e&&(r.isArray(e)?t+="[]":e=[e],r.forEach(e,function(e){r.isDate(e)?e=e.toISOString():r.isObject(e)&&(e=JSON.stringify(e)),s.push(o(t)+"="+o(e))}))}),i=s.join("&")}return i&&(e+=(-1===e.indexOf("?")?"?":"&")+i),e}},function(e,t,n){"use strict";var r=n(0),o=["age","authorization","content-length","content-type","etag","expires","from","host","if-modified-since","if-unmodified-since","last-modified","location","max-forwards","proxy-authorization","referer","retry-after","user-agent"];e.exports=function(e){var t,n,i,s={};return e?(r.forEach(e.split("\n"),function(e){if(i=e.indexOf(":"),t=r.trim(e.substr(0,i)).toLowerCase(),n=r.trim(e.substr(i+1)),t){if(s[t]&&o.indexOf(t)>=0)return;s[t]="set-cookie"===t?(s[t]?s[t]:[]).concat([n]):s[t]?s[t]+", "+n:n}}),s):s}},function(e,t,n){"use strict";var r=n(0);e.exports=r.isStandardBrowserEnv()?function(){var e,t=/(msie|trident)/i.test(navigator.userAgent),n=document.createElement("a");function o(e){var r=e;return t&&(n.setAttribute("href",r),r=n.href),n.setAttribute("href",r),{href:n.href,protocol:n.protocol?n.protocol.replace(/:$/,""):"",host:n.host,search:n.search?n.search.replace(/^\?/,""):"",hash:n.hash?n.hash.replace(/^#/,""):"",hostname:n.hostname,port:n.port,pathname:"/"===n.pathname.charAt(0)?n.pathname:"/"+n.pathname}}return e=o(window.location.href),function(t){var n=r.isString(t)?o(t):t;return n.protocol===e.protocol&&n.host===e.host}}():function(){return!0}},function(e,t,n){"use strict";function r(){this.message="String contains an invalid character"}r.prototype=new Error,r.prototype.code=5,r.prototype.name="InvalidCharacterError",e.exports=function(e){for(var t,n,o=String(e),i="",s=0,a="ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";o.charAt(0|s)||(a="=",s%1);i+=a.charAt(63&t>>8-s%1*8)){if((n=o.charCodeAt(s+=.75))>255)throw new r;t=t<<8|n}return i}},function(e,t,n){"use strict";var r=n(0);e.exports=r.isStandardBrowserEnv()?{write:function(e,t,n,o,i,s){var a=[];a.push(e+"="+encodeURIComponent(t)),r.isNumber(n)&&a.push("expires="+new Date(n).toGMTString()),r.isString(o)&&a.push("path="+o),r.isString(i)&&a.push("domain="+i),!0===s&&a.push("secure"),document.cookie=a.join("; ")},read:function(e){var t=document.cookie.match(new RegExp("(^|;\\s*)("+e+")=([^;]*)"));return t?decodeURIComponent(t[3]):null},remove:function(e){this.write(e,"",Date.now()-864e5)}}:{write:function(){},read:function(){return null},remove:function(){}}},function(e,t,n){"use strict";var r=n(0);function o(){this.handlers=[]}o.prototype.use=function(e,t){return this.handlers.push({fulfilled:e,rejected:t}),this.handlers.length-1},o.prototype.eject=function(e){this.handlers[e]&&(this.handlers[e]=null)},o.prototype.forEach=function(e){r.forEach(this.handlers,function(t){null!==t&&e(t)})},e.exports=o},function(e,t,n){"use strict";var r=n(0),o=n(23),i=n(5),s=n(1),a=n(24),u=n(25);function c(e){e.cancelToken&&e.cancelToken.throwIfRequested()}e.exports=function(e){return c(e),e.baseURL&&!a(e.url)&&(e.url=u(e.baseURL,e.url)),e.headers=e.headers||{},e.data=o(e.data,e.headers,e.transformRequest),e.headers=r.merge(e.headers.common||{},e.headers[e.method]||{},e.headers||{}),r.forEach(["delete","get","head","post","put","patch","common"],function(t){delete e.headers[t]}),(e.adapter||s.adapter)(e).then(function(t){return c(e),t.data=o(t.data,t.headers,e.transformResponse),t},function(t){return i(t)||(c(e),t&&t.response&&(t.response.data=o(t.response.data,t.response.headers,e.transformResponse))),Promise.reject(t)})}},function(e,t,n){"use strict";var r=n(0);e.exports=function(e,t,n){return r.forEach(n,function(n){e=n(e,t)}),e}},function(e,t,n){"use strict";e.exports=function(e){return/^([a-z][a-z\d\+\-\.]*:)?\/\//i.test(e)}},function(e,t,n){"use strict";e.exports=function(e,t){return t?e.replace(/\/+$/,"")+"/"+t.replace(/^\/+/,""):e}},function(e,t,n){"use strict";var r=n(6);function o(e){if("function"!=typeof e)throw new TypeError("executor must be a function.");var t;this.promise=new Promise(function(e){t=e});var n=this;e(function(e){n.reason||(n.reason=new r(e),t(n.reason))})}o.prototype.throwIfRequested=function(){if(this.reason)throw this.reason},o.source=function(){var e;return{token:new o(function(t){e=t}),cancel:e}},e.exports=o},function(e,t,n){"use strict";e.exports=function(e){return function(t){return e.apply(null,t)}}}]);
//# sourceMappingURL=data:application/json;charset=utf-8;base64,eyJ2ZXJzaW9uIjozLCJzb3VyY2VzIjpbXSwibmFtZXMiOltdLCJtYXBwaW5ncyI6IiIsImZpbGUiOiJtaTE4bi5taW4uanMiLCJzb3VyY2VSb290IjoiIn0=

/***/ }),

/***/ 395:
/***/ (function(module) {


module.exports = function storageAvailable(type) {
	try {
		var storage = window[type],
			x = '__storage_test__';
		storage.setItem(x, x);
		storage.removeItem(x);
		return true;
	}
	catch(e) {
		return false;
	}
}


/***/ }),

/***/ 287:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

var api = __webpack_require__(292);
            var content = __webpack_require__(349);

            content = content.__esModule ? content.default : content;

            if (typeof content === 'string') {
              content = [[module.id, content, '']];
            }

var options = {"attributes":{"class":"formBuilder-injected-style"}};

options.insert = "head";
options.singleton = false;

var update = api(content, options);



module.exports = content.locals || {};

/***/ }),

/***/ 292:
/***/ (function(module, __unused_webpack_exports, __webpack_require__) {

"use strict";


var isOldIE = function isOldIE() {
  var memo;
  return function memorize() {
    if (typeof memo === 'undefined') {
      // Test for IE <= 9 as proposed by Browserhacks
      // @see http://browserhacks.com/#hack-e71d8692f65334173fee715c222cb805
      // Tests for existence of standard globals is to allow style-loader
      // to operate correctly into non-standard environments
      // @see https://github.com/webpack-contrib/style-loader/issues/177
      memo = Boolean(window && document && document.all && !window.atob);
    }

    return memo;
  };
}();

var getTarget = function getTarget() {
  var memo = {};
  return function memorize(target) {
    if (typeof memo[target] === 'undefined') {
      var styleTarget = document.querySelector(target); // Special case to return head of iframe instead of iframe itself

      if (window.HTMLIFrameElement && styleTarget instanceof window.HTMLIFrameElement) {
        try {
          // This will throw an exception if access to iframe is blocked
          // due to cross-origin restrictions
          styleTarget = styleTarget.contentDocument.head;
        } catch (e) {
          // istanbul ignore next
          styleTarget = null;
        }
      }

      memo[target] = styleTarget;
    }

    return memo[target];
  };
}();

var stylesInDom = [];

function getIndexByIdentifier(identifier) {
  var result = -1;

  for (var i = 0; i < stylesInDom.length; i++) {
    if (stylesInDom[i].identifier === identifier) {
      result = i;
      break;
    }
  }

  return result;
}

function modulesToDom(list, options) {
  var idCountMap = {};
  var identifiers = [];

  for (var i = 0; i < list.length; i++) {
    var item = list[i];
    var id = options.base ? item[0] + options.base : item[0];
    var count = idCountMap[id] || 0;
    var identifier = "".concat(id, " ").concat(count);
    idCountMap[id] = count + 1;
    var index = getIndexByIdentifier(identifier);
    var obj = {
      css: item[1],
      media: item[2],
      sourceMap: item[3]
    };

    if (index !== -1) {
      stylesInDom[index].references++;
      stylesInDom[index].updater(obj);
    } else {
      stylesInDom.push({
        identifier: identifier,
        updater: addStyle(obj, options),
        references: 1
      });
    }

    identifiers.push(identifier);
  }

  return identifiers;
}

function insertStyleElement(options) {
  var style = document.createElement('style');
  var attributes = options.attributes || {};

  if (typeof attributes.nonce === 'undefined') {
    var nonce =  true ? __webpack_require__.nc : 0;

    if (nonce) {
      attributes.nonce = nonce;
    }
  }

  Object.keys(attributes).forEach(function (key) {
    style.setAttribute(key, attributes[key]);
  });

  if (typeof options.insert === 'function') {
    options.insert(style);
  } else {
    var target = getTarget(options.insert || 'head');

    if (!target) {
      throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
    }

    target.appendChild(style);
  }

  return style;
}

function removeStyleElement(style) {
  // istanbul ignore if
  if (style.parentNode === null) {
    return false;
  }

  style.parentNode.removeChild(style);
}
/* istanbul ignore next  */


var replaceText = function replaceText() {
  var textStore = [];
  return function replace(index, replacement) {
    textStore[index] = replacement;
    return textStore.filter(Boolean).join('\n');
  };
}();

function applyToSingletonTag(style, index, remove, obj) {
  var css = remove ? '' : obj.media ? "@media ".concat(obj.media, " {").concat(obj.css, "}") : obj.css; // For old IE

  /* istanbul ignore if  */

  if (style.styleSheet) {
    style.styleSheet.cssText = replaceText(index, css);
  } else {
    var cssNode = document.createTextNode(css);
    var childNodes = style.childNodes;

    if (childNodes[index]) {
      style.removeChild(childNodes[index]);
    }

    if (childNodes.length) {
      style.insertBefore(cssNode, childNodes[index]);
    } else {
      style.appendChild(cssNode);
    }
  }
}

function applyToTag(style, options, obj) {
  var css = obj.css;
  var media = obj.media;
  var sourceMap = obj.sourceMap;

  if (media) {
    style.setAttribute('media', media);
  } else {
    style.removeAttribute('media');
  }

  if (sourceMap && typeof btoa !== 'undefined') {
    css += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(sourceMap)))), " */");
  } // For old IE

  /* istanbul ignore if  */


  if (style.styleSheet) {
    style.styleSheet.cssText = css;
  } else {
    while (style.firstChild) {
      style.removeChild(style.firstChild);
    }

    style.appendChild(document.createTextNode(css));
  }
}

var singleton = null;
var singletonCounter = 0;

function addStyle(obj, options) {
  var style;
  var update;
  var remove;

  if (options.singleton) {
    var styleIndex = singletonCounter++;
    style = singleton || (singleton = insertStyleElement(options));
    update = applyToSingletonTag.bind(null, style, styleIndex, false);
    remove = applyToSingletonTag.bind(null, style, styleIndex, true);
  } else {
    style = insertStyleElement(options);
    update = applyToTag.bind(null, style, options);

    remove = function remove() {
      removeStyleElement(style);
    };
  }

  update(obj);
  return function updateStyle(newObj) {
    if (newObj) {
      if (newObj.css === obj.css && newObj.media === obj.media && newObj.sourceMap === obj.sourceMap) {
        return;
      }

      update(obj = newObj);
    } else {
      remove();
    }
  };
}

module.exports = function (list, options) {
  options = options || {}; // Force single-tag solution on IE6-9, which has a hard limit on the # of <style>
  // tags it will allow on a page

  if (!options.singleton && typeof options.singleton !== 'boolean') {
    options.singleton = isOldIE();
  }

  list = list || [];
  var lastIdentifiers = modulesToDom(list, options);
  return function update(newList) {
    newList = newList || [];

    if (Object.prototype.toString.call(newList) !== '[object Array]') {
      return;
    }

    for (var i = 0; i < lastIdentifiers.length; i++) {
      var identifier = lastIdentifiers[i];
      var index = getIndexByIdentifier(identifier);
      stylesInDom[index].references--;
    }

    var newLastIdentifiers = modulesToDom(newList, options);

    for (var _i = 0; _i < lastIdentifiers.length; _i++) {
      var _identifier = lastIdentifiers[_i];

      var _index = getIndexByIdentifier(_identifier);

      if (stylesInDom[_index].references === 0) {
        stylesInDom[_index].updater();

        stylesInDom.splice(_index, 1);
      }
    }

    lastIdentifiers = newLastIdentifiers;
  };
};

/***/ }),

/***/ 138:
/***/ (function(module) {

"use strict";
module.exports = "data:font/woff;base64,";

/***/ }),

/***/ 488:
/***/ (function(module) {

"use strict";
module.exports = "data:image/svg+xml; utf8, <svg xmlns=%27http://www.w3.org/2000/svg%27 width=%2716%27 height=%2716%27 fill=%27currentColor%27 class=%27bi bi-list-nested%27 viewBox=%270 0 16 16%27><path fill-rule=%27evenodd%27 d=%27M4.5 11.5A.5.5 0 0 1 5 11h10a.5.5 0 0 1 0 1H5a.5.5 0 0 1-.5-.5zm-2-4A.5.5 0 0 1 3 7h10a.5.5 0 0 1 0 1H3a.5.5 0 0 1-.5-.5zm-2-4A.5.5 0 0 1 1 3h10a.5.5 0 0 1 0 1H1a.5.5 0 0 1-.5-.5z%27/></svg>";

/***/ }),

/***/ 591:
/***/ (function(module) {

"use strict";
module.exports = "data:image/svg+xml; utf8,<svg xmlns=%27http://www.w3.org/2000/svg%27 width=%2716%27 height=%2716%27 fill=%27currentColor%27 class=%27bi bi-plus-circle%27 viewBox=%270 0 16 16%27><path d=%27M8 15A7 7 0 1 1 8 1a7 7 0 0 1 0 14zm0 1A8 8 0 1 0 8 0a8 8 0 0 0 0 16z%27/><path d=%27M8 4a.5.5 0 0 1 .5.5v3h3a.5.5 0 0 1 0 1h-3v3a.5.5 0 0 1-1 0v-3h-3a.5.5 0 0 1 0-1h3v-3A.5.5 0 0 1 8 4z%27/></svg>";

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			id: moduleId,
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = __webpack_modules__;
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	!function() {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = function(module) {
/******/ 			var getter = module && module.__esModule ?
/******/ 				function() { return module['default']; } :
/******/ 				function() { return module; };
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	!function() {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = function(exports, definition) {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	!function() {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = function(exports) {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/jsonp chunk loading */
/******/ 	!function() {
/******/ 		__webpack_require__.b = (typeof document !== 'undefined' && document.baseURI) || self.location.href;
/******/ 		
/******/ 		// object to store loaded and loading chunks
/******/ 		// undefined = chunk not loaded, null = chunk preloaded/prefetched
/******/ 		// [resolve, reject, Promise] = chunk loading, 0 = chunk loaded
/******/ 		var installedChunks = {
/******/ 			998: 0
/******/ 		};
/******/ 		
/******/ 		// no chunk on demand loading
/******/ 		
/******/ 		// no prefetching
/******/ 		
/******/ 		// no preloaded
/******/ 		
/******/ 		// no HMR
/******/ 		
/******/ 		// no HMR manifest
/******/ 		
/******/ 		// no on chunks loaded
/******/ 		
/******/ 		// no jsonp function
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/nonce */
/******/ 	!function() {
/******/ 		__webpack_require__.nc = undefined;
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be in strict mode.
!function() {
"use strict";

// EXTERNAL MODULE: ../src/sass/form-builder.scss
var form_builder = __webpack_require__(287);
// EXTERNAL MODULE: ../node_modules/lodash/throttle.js
var throttle = __webpack_require__(858);
var throttle_default = /*#__PURE__*/__webpack_require__.n(throttle);
;// ../src/js/dom.js
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
const instanceDom = {};
const defaultSubtypes = {
  text: ['text', 'password', 'email', 'color', 'tel'],
  header: ['h1', 'h2', 'h3'],
  button: ['button', 'submit', 'reset'],
  paragraph: ['p', 'address', 'blockquote', 'canvas', 'output'],
  textarea: ['textarea', 'quill']
};
const remove = element => {
  if (element.parentNode) {
    element.parentNode.removeChild(element);
  }
};
const empty = element => {
  while (element.firstChild) {
    element.removeChild(element.firstChild);
  }
  return element;
};
const filter = (elems, term, show = true) => {
  const filteredElems = [];
  let toggle = ['none', 'block'];
  if (show) {
    toggle = toggle.reverse();
  }
  for (let i = elems.length - 1; i >= 0; i--) {
    const txt = elems[i].textContent.toLowerCase();
    if (txt.indexOf(term.toLowerCase()) !== -1) {
      elems[i].style.display = toggle[0];
      filteredElems.push(elems[i]);
    } else {
      elems[i].style.display = toggle[1];
    }
  }
  return filteredElems;
};
const optionFields = ['select', 'checkbox-group', 'checkbox', 'radio-group', 'autocomplete'];
const optionFieldsRegEx = new RegExp(`(${optionFields.join('|')})`);
class Dom {
  constructor(formID) {
    _defineProperty(this, "stage", void 0);
    _defineProperty(this, "controls", void 0);
    _defineProperty(this, "formActions", void 0);
    _defineProperty(this, "editorWrap", void 0);
    this.optionFields = optionFields;
    this.optionFieldsRegEx = optionFieldsRegEx;
    this.subtypes = defaultSubtypes;
    this.empty = empty;
    this.filter = filter;
    instanceDom[formID] = this;
    return instanceDom[formID];
  }
  onRender(node, cb) {
    if (!node.parentElement) {
      window.requestAnimationFrame(() => this.onRender(node, cb));
    } else {
      cb(node);
    }
  }
}
;// ../src/js/data.js
const instanceData = {};
class Data {
  constructor(formID) {
    this.formData = {};
    this.formID = formID;
    instanceData[formID] = this;
  }
}
const availablefields = {};
// EXTERNAL MODULE: ../node_modules/mi18n/dist/mi18n.min.js
var mi18n_min = __webpack_require__(252);
var mi18n_min_default = /*#__PURE__*/__webpack_require__.n(mi18n_min);
;// ../src/js/sanitizer.js
const sanitizerConfig = {
  clobberingProtection: {
    document: true,
    form: true,
    namespaceAttributes: false
  },
  backendOrder: ['dompurify', 'sanitizer', 'fallback'],
  backends: {
    sanitizer: (() => {
      if (typeof window['Sanitizer'] !== 'function') return false;
      try {
        return new window.Sanitizer({
          elements: ['div', 'p', 'br', 'span', 'strong', 'em', 'b', 'i', 'u', 'ul', 'ol', 'li', 'a', 'img', 'table', 'thead', 'tbody', 'tr', 'th', 'td', 'form', 'label', 'input', 'select', 'option', 'textarea', 'button', 'fieldset', 'legend', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6'],
          attributes: ['class', 'id', 'title', 'contenteditable', 'href', 'target', 'rel', 'src', 'alt', 'width', 'height', 'type', 'name', 'value', 'checked', 'disabled', 'placeholder', 'required', 'for', 'selected', 'rows', 'cols', 'multiple', 'method', 'action', 'tabindex', 'readonly', 'minlength', 'maxlength', 'pattern', 'autocomplete', 'autofocus'],
          dataAttributes: true
        });
      } catch (e) {
        return false;
      }
    })(),
    dompurify: window.DOMPurify ? (purify => {
      purify.setConfig({
        SANITIZE_DOM: false,
        ADD_ATTR: ['contenteditable']
      });
      return purify;
    })(window.DOMPurify) : false,
    fallback: content => content
  }
};
const setSanitizerConfig = config => {
  if (typeof config !== 'object') {
    throw 'Invalid value given to setSanitizerConfig, expected config object';
  }
  if (config.hasOwnProperty('clobberingProtection')) {
    ['document', 'form', 'namespaceAttributes'].forEach(type => {
      if (config.clobberingProtection.hasOwnProperty(type) && typeof config.clobberingProtection[type] === 'boolean') {
        sanitizerConfig.clobberingProtection[type] = config.clobberingProtection[type];
      }
    });
  }
  if (config.hasOwnProperty('backends')) {
    if (typeof config.backends === 'object') {
      Object.keys(config.backends).forEach(implementation => sanitizerConfig.backends[implementation] = config.backends[implementation]);
    } else {
      throw 'backends config expected to be an Object';
    }
  }
  if (config.hasOwnProperty('backendOrder')) {
    sanitizerConfig.backendOrder = [];
    if (Array.isArray(config.backendOrder)) {
      config.backendOrder.forEach(backend => {
        if (sanitizerConfig.backends.hasOwnProperty(backend)) {
          sanitizerConfig.backendOrder.push(backend);
        } else {
          throw 'unknown sanitizer backend ' + backend;
        }
      });
    } else {
      throw 'backendOrder config expected to be an Array of backend keys as strings';
    }
  }
};
const isPotentiallyDangerousAttribute = (attrName, attrValue) => {
  if (sanitizerConfig.backendOrder.length === 0) {
    return false;
  }
  const attrNameLc = attrName.toLowerCase();
  attrValue = attrValue ? attrValue + '' : '';
  return attrNameLc.startsWith('on') || ['form', 'formaction'].includes(attrNameLc) || attrValue.trim().toLowerCase().startsWith('javascript:');
};
function fallbackSanitizer(content) {
  const context = document.implementation.createHTMLDocument('');
  const base = context.createElement('base');
  base.href = document.location.href;
  context.head.appendChild(base);
  const exclude_tags = ['applet', 'comment', 'embed', 'iframe', 'link', 'listing', 'meta', 'noscript', 'object', 'plaintext', 'script', 'style', 'xmp'];
  const output = $.parseHTML(content, context, false);
  $(output).find('*').addBack().each((nindex, node) => {
    if (node.nodeName === '#text') {
      return;
    }
    if (node.tagName && exclude_tags.includes(node.tagName.toLowerCase())) {
      if (node.parentElement) {
        node.parentElement.removeChild(node);
      } else if (output.includes(node)) {
        output.splice(output.indexOf(node), 1);
      }
      return;
    }
    if (node.attributes) {
      Array.from(node.attributes).forEach(attribute => {
        if (isPotentiallyDangerousAttribute(attribute.name, attribute.value)) {
          $(node).removeAttr(attribute.name);
        }
      });
    }
  });
  const tmp = context.createElement('div');
  $(tmp).html(output);
  return tmp.innerHTML;
}
sanitizerConfig.backends.fallback = fallbackSanitizer;
const attributeWillClobber = value => {
  const check_doc = document;
  const check_form = document.createElement('form');
  return value in check_doc || value in check_form;
};
const sanitizeNamedAttribute = value => {
  const check_doc = sanitizerConfig.clobberingProtection.document ? document : false;
  const check_form = sanitizerConfig.clobberingProtection.form ? document.createElement('form') : false;
  if (check_doc && value in check_doc || check_form && value in check_form) {
    return sanitizerConfig.clobberingProtection.namespaceAttributes ? 'user-content-' + value : undefined;
  }
  return value;
};
const sanitizeDomClobbering = element => {
  $(element).find('*').each((nindex, node) => {
    const protectedTypes = ['id', 'name'];
    if (['embed', 'form', 'iframe', 'image', 'img', 'object'].includes(node.tagName.toLowerCase())) {
      node.removeAttribute('name');
    }
    protectedTypes.forEach(attrName => {
      if (node.hasAttribute(attrName)) {
        const value = sanitizeNamedAttribute(node.getAttribute(attrName));
        if (value === undefined) {
          node.removeAttribute(attrName);
        } else {
          node.setAttribute(attrName, value);
        }
      }
    });
  });
  return element;
};
const sanitizersCallbacks = {
  fallback: (element, content) => {
    const purifier = sanitizerConfig.backends.fallback;
    const supported = typeof purifier === 'function';
    if (supported) {
      content = purifier(content);
    }
    element.innerHTML = content;
    return supported;
  },
  dompurify: (element, content) => {
    const purifier = sanitizerConfig.backends.dompurify;
    if (purifier === false || !purifier.isSupported) {
      return false;
    }
    element.innerHTML = purifier.sanitize(content);
    return true;
  },
  sanitizer: (element, content) => {
    const sanitizer = sanitizerConfig.backends.sanitizer;
    if (sanitizer) {
      element.setHTML(content, {
        sanitizer: sanitizer
      });
      return true;
    }
    return false;
  }
};
const setElementContent = (element, content, asText = false) => {
  if (asText) {
    element.textContent = content;
  } else {
    const proxyElem = document.createElement(element.tagName);
    const performedBy = sanitizerConfig.backendOrder.find(type => sanitizersCallbacks[type](proxyElem, content));
    if (performedBy !== undefined) {
      sanitizeDomClobbering(proxyElem);
      element.innerHTML = proxyElem.innerHTML;
      return element;
    }
    element.innerHTML = content;
    return element;
  }
};
const sanitizer = {
  setElementContent,
  setSanitizerConfig,
  sanitizeNamedAttribute,
  isPotentiallyDangerousAttribute,
  attributeWillClobber
};
/* harmony default export */ var js_sanitizer = ((/* unused pure expression or super */ null && (sanitizer)));
;// ../src/js/utils.js
const _excluded = ["events"],
  _excluded2 = ["tag", "content"];
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { utils_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function utils_defineProperty(e, r, t) { return (r = utils_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function utils_toPropertyKey(t) { var i = utils_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function utils_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function _objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = _objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function _objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }

window.fbLoaded = {
  js: [],
  css: []
};
window.fbEditors = {
  quill: {},
  tinymce: {}
};
const trimObj = function (obj, removeFalse = false) {
  if (null == obj || typeof obj !== 'object') return obj;
  const attrs = typeof window.structuredClone === 'function' ? window.structuredClone(obj) : Object.assign({}, obj);
  const xmlRemove = [null, undefined, ''];
  if (removeFalse) {
    xmlRemove.push(false);
  }
  for (const attr in attrs) {
    if (xmlRemove.includes(attrs[attr])) {
      delete attrs[attr];
    } else if (Array.isArray(attrs[attr])) {
      if (!attrs[attr].length) {
        delete attrs[attr];
      }
    }
  }
  return attrs;
};
const validAttr = function (attr) {
  const invalid = ['values', 'enableOther', 'other', 'label', 'subtype'];
  return !invalid.includes(attr);
};
const xmlAttrString = attrs => Object.entries(attrs).map(([key, val]) => `${hyphenCase(key)}="${val}"`).join(' ');
const attrString = attrs => Object.entries(attrs).map(([key, val]) => validAttr(key) && Object.values(safeAttr(key, val)).join('')).filter(Boolean).join(' ');
const safeAttr = (name, value) => {
  name = safeAttrName(name);
  let valString;
  if (value) {
    if (Array.isArray(value)) {
      valString = escapeAttr(value.join(' '));
    } else {
      if (typeof value === 'boolean') {
        value = value.toString();
      }
      valString = escapeAttr(value.trim());
    }
  }
  value = value ? `="${valString}"` : '';
  return {
    name,
    value
  };
};
const flattenArray = arr => arr.reduce((acc, val) => acc.concat(Array.isArray(val) ? flattenArray(val) : val), []);
const safeAttrName = name => {
  const safeAttr = {
    className: 'class'
  };
  return safeAttr[name] || hyphenCase(name);
};
const safeClassName = className => className.replace(/\[([^\]]+)\]/g, '-$1');
const hyphenCase = str => {
  str = str.replace(/[^\w\s\-\[\]]/gi, '');
  str = safeClassName(str);
  str = str.replace(/([A-Z])/g, function ($1) {
    return '-' + $1.toLowerCase();
  });
  return str.replace(/\s/g, '-').replace(/^-+/g, '');
};
const camelCase = str => str.replace(/-([a-z])/g, (m, w) => w.toUpperCase());
const bindEvents = (element, events) => {
  if (events) {
    for (const event in events) {
      if (events.hasOwnProperty(event)) {
        element.addEventListener(event, evt => events[event](evt));
      }
    }
  }
};
const nameAttr = function () {
  let lepoch;
  let counter = 0;
  return function (field) {
    const epoch = Date.now();
    if (epoch === lepoch) {
      ++counter;
    } else {
      counter = 0;
      lepoch = epoch;
    }
    const prefix = field.type || hyphenCase(field.label);
    return prefix + '-' + epoch + '-' + counter;
  };
}();
const getContentType = content => {
  if (content === undefined) {
    return content;
  }
  return [['array', content => Array.isArray(content)], ['node', content => content instanceof window.Node || content instanceof window.HTMLElement], ['component', () => content?.dom], [typeof content, () => true]].find(typeCondition => typeCondition[1](content))[0];
};
const markup = function (tag, content = '', attributes = {}) {
  let contentType = getContentType(content);
  const {
      events
    } = attributes,
    attrs = _objectWithoutProperties(attributes, _excluded);
  const field = document.createElement(tag);
  const appendContent = {
    string: content => {
      setElementContent(field, field.innerHTML + content);
    },
    object: config => {
      const {
          tag,
          content
        } = config,
        data = _objectWithoutProperties(config, _excluded2);
      return field.appendChild(markup(tag, content, data));
    },
    node: content => {
      return field.appendChild(content);
    },
    array: content => {
      for (let i = 0; i < content.length; i++) {
        contentType = getContentType(content[i]);
        appendContent[contentType](content[i]);
      }
    },
    function: content => {
      content = content();
      contentType = getContentType(content);
      appendContent[contentType](content);
    },
    undefined: () => {}
  };
  for (const attr in attrs) {
    if (attrs.hasOwnProperty(attr)) {
      const name = safeAttrName(attr);
      let attrVal = Array.isArray(attrs[attr]) ? unique(attrs[attr].join(' ').split(' ')).join(' ') : attrs[attr];
      if (isPotentiallyDangerousAttribute(name, attrVal)) {
        continue;
      }
      if (typeof attrVal === 'boolean') {
        if (attrVal === true) {
          const val = name === 'contenteditable' ? true : name;
          field.setAttribute(name, val);
        }
      } else {
        if (name === 'id' || name === 'name') {
          attrVal = sanitizeNamedAttribute(attrVal);
        }
        if (attrVal !== undefined) {
          field.setAttribute(name, attrVal);
        }
      }
    }
  }
  if (content) {
    appendContent[contentType](content);
  }
  bindEvents(field, events);
  return field;
};
const xmlParseAttrs = elem => {
  const attrs = elem.attributes;
  const data = {};
  forEach(attrs, attr => {
    let attrVal = attrs[attr].value || '';
    if (attrVal.match(/false|true/g)) {
      attrVal = attrVal === 'true';
    } else if (attrVal.match(/undefined/g)) {
      attrVal = undefined;
    }
    if (attrVal) {
      data[camelCase(attrs[attr].name)] = attrVal;
    }
  });
  return data;
};
const xmlParseOptions = options => {
  const data = [];
  for (let i = 0; i < options.length; i++) {
    const optionData = _objectSpread(_objectSpread({}, xmlParseAttrs(options[i])), {}, {
      label: options[i].textContent
    });
    data.push(optionData);
  }
  return data;
};
const xmlParseUserData = userData => {
  const data = [];
  if (userData.length) {
    const values = userData[0].getElementsByTagName('value');
    for (let i = 0; i < values.length; i++) {
      data.push(values[i].textContent);
    }
  }
  return data;
};
const parseXML = xmlString => {
  const parser = new window.DOMParser();
  const xml = parser.parseFromString(xmlString, 'text/xml');
  const formData = [];
  if (xml) {
    const fields = xml.getElementsByTagName('field');
    for (let i = 0; i < fields.length; i++) {
      const fieldData = xmlParseAttrs(fields[i]);
      const options = fields[i].getElementsByTagName('option');
      const userData = fields[i].getElementsByTagName('userData');
      if (options && options.length) {
        fieldData.values = xmlParseOptions(options);
      }
      if (userData && userData.length) {
        fieldData.userData = xmlParseUserData(userData);
      }
      formData.push(fieldData);
    }
  }
  return formData;
};
const parsedHtml = html => {
  const escapeElement = document.createElement('textarea');
  escapeElement.innerHTML = html;
  return escapeElement.textContent;
};
const escapeHtml = html => {
  const escapeElement = document.createElement('textarea');
  escapeElement.textContent = html;
  return escapeElement.innerHTML;
};
const escapeAttr = str => {
  const match = {
    '"': '&quot;',
    '&': '&amp;',
    '<': '&lt;',
    '>': '&gt;'
  };
  const replaceTag = tag => match[tag] || tag;
  return typeof str === 'string' ? str.replace(/["&<>]/g, replaceTag) : str;
};
const escapeAttrs = attrs => {
  for (const attr in attrs) {
    if (attrs.hasOwnProperty(attr)) {
      attrs[attr] = escapeAttr(attrs[attr]);
    }
  }
  return attrs;
};
const forEach = function (array, callback, scope) {
  for (let i = 0; i < array.length; i++) {
    callback.call(scope, i, array[i]);
  }
};
const unique = array => {
  return array.filter((elem, pos, arr) => arr.indexOf(elem) === pos);
};
const removeFromArray = (val, arr) => {
  const index = arr.indexOf(val);
  if (index > -1) {
    arr.splice(index, 1);
  }
};
const getScripts = (scriptScr, path = '') => {
  const $ = jQuery;
  let _arr = [];
  if (!Array.isArray(scriptScr)) {
    scriptScr = [scriptScr];
  }
  if (!isCached(scriptScr)) {
    _arr = jQuery.map(scriptScr, src => {
      const options = {
        dataType: 'script',
        cache: true,
        url: (path || '') + src
      };
      return jQuery.ajax(options).done(() => window.fbLoaded.js.push(src));
    });
  }
  _arr.push(jQuery.Deferred(deferred => $(deferred.resolve)));
  return jQuery.when(..._arr);
};
const isCached = (src, type = 'js') => {
  const cache = window.fbLoaded[type];
  return Array.isArray(src) ? src.every(s => cache.includes(s)) : cache.includes(src);
};
const getStyles = (scriptScr, path = '') => {
  if (!Array.isArray(scriptScr)) {
    scriptScr = [scriptScr];
  }
  scriptScr.forEach(src => {
    let type = 'href';
    let key = src;
    let id = '';
    if (typeof src == 'object') {
      type = src.type || (src.style ? 'inline' : 'href');
      id = src.id;
      key = id || src.href || src.style;
      src = type === 'inline' ? src.style : src.href;
    }
    if (isCached(key, 'css')) {
      return;
    }
    if (type === 'href') {
      const link = document.createElement('link');
      link.type = 'text/css';
      link.rel = 'stylesheet';
      link.href = (path || '') + src;
      document.head.appendChild(link);
    } else {
      $(`<style type="text/css">${src}</style>`).attr('id', id).appendTo($(document.head));
    }
    window.fbLoaded.css.push(key);
  });
};
const capitalize = str => {
  return str.replace(/\b\w/g, function (m) {
    return m.toUpperCase();
  });
};
const merge = (obj1, obj2) => {
  const mergedObj = Object.assign({}, obj1, obj2);
  for (const prop in obj2) {
    if (mergedObj.hasOwnProperty(prop)) {
      if (Array.isArray(obj2[prop])) {
        mergedObj[prop] = Array.isArray(obj1[prop]) ? unique(obj1[prop].concat(obj2[prop])) : obj2[prop];
      } else if (typeof obj2[prop] === 'object') {
        mergedObj[prop] = merge(obj1[prop], obj2[prop]);
      } else {
        mergedObj[prop] = obj2[prop];
      }
    }
  }
  return mergedObj;
};
const addEventListeners = (el, evts, cb) => evts.split(' ').forEach(e => el.addEventListener(e, cb, false));
const closest = (el, cls) => {
  const className = cls.replace('.', '');
  while ((el = el.parentElement) && !el.classList.contains(className));
  return el;
};
const mobileClass = () => {
  let mobileClass = '';
  (a => {
    if (/(android|bb\d+|meego).+mobile|avantgo|bada\/|blackberry|blazer|compal|elaine|fennec|hiptop|iemobile|ip(hone|od)|iris|kindle|lge |maemo|midp|mmp|mobile.+firefox|netfront|opera m(ob|in)i|palm( os)?|phone|p(ixi|re)\/|plucker|pocket|psp|series(4|6)0|symbian|treo|up\.(browser|link)|vodafone|wap|windows ce|xda|xiino/i.test(a)) {
      mobileClass = 'formbuilder-mobile';
    }
  })(navigator.userAgent || navigator.vendor || window.opera);
  return mobileClass;
};
const safename = str => {
  return str.replace(/\s/g, '-').replace(/[^a-zA-Z0-9[\]_-]/g, '');
};
const forceNumber = str => str.replace(/[^0-9]/g, '');
const subtract = (arr, from) => {
  return from.filter(function (a) {
    return !~this.indexOf(a);
  }, arr);
};
const insertStyle = srcs => {
  srcs = Array.isArray(srcs) ? srcs : [srcs];
  const promises = srcs.map(({
    src,
    id
  }) => new Promise(resolve => {
    if (window.fbLoaded.css.includes(src)) {
      return resolve(src);
    }
    const formeoStyle = markup('link', null, {
      href: src,
      rel: 'stylesheet',
      id
    });
    document.head.insertBefore(formeoStyle, document.head.firstChild);
  }));
  return Promise.all(promises);
};
const removeStyle = id => {
  const elem = document.getElementById(id);
  return elem.parentElement.removeChild(elem);
};
const bootstrapColumnRegex = /^col-(xs|sm|md|lg)-([^\s]+)/;
const getAllGridRelatedClasses = className => {
  return typeof className === 'string' ? className.split(' ').filter(x => bootstrapColumnRegex.test(x) || x.startsWith('row-')) : [];
};
function titleCase(str) {
  const lowers = ['a', 'an', 'and', 'as', 'at', 'but', 'by', 'for', 'for', 'from', 'in', 'into', 'near', 'nor', 'of', 'on', 'onto', 'or', 'the', 'to', 'with'].map(lower => `\\s${lower}\\s`);
  const regex = new RegExp(`(?!${lowers.join('|')})\\w\\S*`, 'g');
  return `${str}`.replace(regex, txt => txt.charAt(0).toUpperCase() + txt.slice(1).replace(/[A-Z]/g, word => ` ${word}`));
}
function firstNumberOrUndefined(...options) {
  return options.find(x => typeof x === 'number');
}
const utils = {
  addEventListeners,
  attrString,
  camelCase,
  capitalize,
  closest,
  getContentType,
  escapeAttr,
  escapeAttrs,
  escapeHtml,
  forceNumber,
  forEach,
  getScripts,
  getStyles,
  hyphenCase,
  isCached,
  markup,
  merge,
  mobileClass,
  nameAttr,
  parsedHtml,
  parseXML,
  removeFromArray,
  safeAttr,
  safeAttrName,
  safename,
  subtract,
  trimObj,
  unique,
  validAttr,
  titleCase,
  firstNumberOrUndefined
};
utils.splitObject = (obj, keys) => {
  const reconstructObj = initialObj => (result, key) => {
    result[key] = initialObj[key];
    return result;
  };
  const kept = Object.keys(obj).filter(key => keys.includes(key)).reduce(reconstructObj(obj), {});
  const rest = Object.keys(obj).filter(key => !keys.includes(key)).reduce(reconstructObj(obj), {});
  return [kept, rest];
};
$.fn.swapWith = function (that) {
  const $this = this;
  const $that = $(that);
  const $temp = $('<div>');
  $this.before($temp);
  $that.before($this);
  $temp.before($that).remove();
  return $this;
};
const generateSelectorClassNames = classNamesObj => Object.entries(classNamesObj).reduce((acc, [key, val]) => _objectSpread(_objectSpread({}, acc), {}, {
  [`${key}Selector`]: `.${val}`
}), {});
/* harmony default export */ var js_utils = (utils);
;// ../src/js/control.js
const control_excluded = ["label", "type"];
function control_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = control_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function control_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }


class control {
  constructor(config, preview) {
    this.rawConfig = jQuery.extend({}, config);
    config = jQuery.extend({}, config);
    this.preview = preview;
    delete config.isPreview;
    if (this.preview) {
      delete config.required;
    }
    const properties = ['label', 'description', 'subtype', 'required', 'disabled'];
    for (const prop of properties) {
      this[prop] = config[prop];
      delete config[prop];
    }
    if (!config.id) {
      if (config.name) {
        config.id = config.name;
      } else {
        config.id = 'control-' + Math.floor(Math.random() * 10000000 + 1);
      }
    }
    this.id = config.id;
    this.type = config.type;
    if (this.description) {
      config.title = this.description;
    }
    if (!control.controlConfig) {
      control.controlConfig = {};
    }
    const classId = this.subtype ? this.type + '.' + this.subtype : this.type;
    this.classConfig = jQuery.extend({}, control.controlConfig[this.type] || {}, control.controlConfig[classId] || {});
    if (this.subtype) {
      config.type = this.subtype;
    }
    if (this.required) {
      config['required'] = 'required';
      config['aria-required'] = 'true';
    }
    if (this.disabled) {
      config['disabled'] = 'disabled';
    }
    this.config = config;
    this.configure();
  }
  static get definition() {
    return {};
  }
  static register(types, controlClass, parentType) {
    const prefix = parentType ? parentType + '.' : '';
    if (!control.classRegister) {
      control.classRegister = {};
    }
    if (!Array.isArray(types)) {
      types = [types];
    }
    for (const type of types) {
      if (type.indexOf('.') !== -1) {
        control.error(`Ignoring type ${type}. Cannot use the character '.' in a type name.`);
        continue;
      }
      control.classRegister[prefix + type] = controlClass;
    }
  }
  static getRegistered(type = false) {
    const types = Object.keys(control.classRegister);
    if (!types.length) {
      return types;
    }
    return types.filter(key => {
      if (type) {
        return key.indexOf(type + '.') > -1;
      }
      return key.indexOf('.') === -1;
    });
  }
  static getRegisteredSubtypes() {
    const types = {};
    for (const key in control.classRegister) {
      if (control.classRegister.hasOwnProperty(key)) {
        const [type, subtype] = key.split('.');
        if (!subtype) {
          continue;
        }
        if (!types[type]) {
          types[type] = [];
        }
        types[type].push(subtype);
      }
    }
    return types;
  }
  static getClass(type, subtype) {
    const lookup = subtype ? type + '.' + subtype : type;
    const controlClass = control.classRegister[lookup] || control.classRegister[type];
    if (!controlClass) {
      return control.error('Invalid control type. (Type: ' + type + ', Subtype: ' + subtype + '). Please ensure you have registered it, and imported it correctly.');
    }
    return controlClass;
  }
  static loadCustom(controls) {
    let controlClasses = [];
    if (controls) {
      controlClasses = controlClasses.concat(controls);
    }
    if (window.fbControls) {
      controlClasses = controlClasses.concat(window.fbControls);
    }
    if (!this.fbControlsLoaded) {
      for (const loadControl of controlClasses) {
        loadControl(control, control.classRegister);
      }
      this.fbControlsLoaded = true;
    }
  }
  static mi18n(lookup, args) {
    const def = this.definition;
    let i18n = def.i18n || {};
    const locale = (mi18n_min_default()).locale;
    i18n = i18n[locale] || i18n.default || i18n;
    const lookupCamel = this.camelCase(lookup);
    const value = typeof i18n == 'object' ? i18n[lookupCamel] || i18n[lookup] : i18n;
    if (value) {
      return value;
    }
    let mapped = def.mi18n;
    if (typeof mapped === 'object') {
      mapped = mapped[lookupCamel] || mapped[lookup];
    }
    if (!mapped) {
      mapped = lookupCamel;
    }
    return mi18n_min_default().get(mapped, args);
  }
  static active(type) {
    return !Array.isArray(this.definition.inactive) || this.definition.inactive.indexOf(type) === -1;
  }
  static label(type) {
    return this.mi18n(type);
  }
  static icon(type) {
    const def = this.definition;
    if (def && typeof def.icon === 'object') {
      return def.icon[type];
    }
    return def.icon;
  }
  configure() {}
  build() {
    const _this$config = this.config,
      {
        label,
        type
      } = _this$config,
      data = control_objectWithoutProperties(_this$config, control_excluded);
    return this.markup(type, parsedHtml(label), data);
  }
  on(eventType) {
    const events = {
      prerender: element => element,
      render: evt => {
        const onRender = () => {
          if (this.onRender) {
            this.onRender(evt);
          }
        };
        if (this.css) {
          getStyles(this.css);
        }
        if (this.js && !isCached(this.js)) {
          getScripts(this.js).done(onRender);
        } else {
          onRender(evt);
        }
      }
    };
    return eventType ? events[eventType] : events;
  }
  static error(message) {
    throw new Error(message);
  }
  markup(tag, content = '', attributes = {}) {
    this.element = markup(tag, content, attributes);
    return this.element;
  }
  parsedHtml(html) {
    return parsedHtml(html);
  }
  static camelCase(str) {
    return camelCase(str);
  }
}
control.jsonAttrs = new Map();
control.parseJsonAttrs = field => {
  const attrs = control.jsonAttrs.get(field?.type);
  if (attrs) {
    attrs.forEach(attr => {
      if (typeof field[attr] === 'string') {
        try {
          field[attr] = JSON.parse(field[attr]);
        } catch {}
      }
    });
  }
  return field;
};
control.stringifyJsonAttrs = field => {
  const attrs = control.jsonAttrs.get(field?.type);
  if (attrs) {
    attrs.forEach(attr => {
      if (field[attr] != null && typeof field[attr] !== 'string') {
        field[attr] = JSON.stringify(field[attr]);
      }
    });
  }
  return field;
};
;// ../src/js/layout.js



const processClassName = (data, field) => {
  let className = data.id ? `formbuilder-${data.type} form-group field-${data.id}` : '';
  if (data.className) {
    const classes = getAllGridRelatedClasses(data.className);
    if (classes && classes.length > 0) {
      className += ` ${classes.join(' ')}`;
      if (!Array.isArray(field)) {
        field = [field];
      }
      field.forEach(item => {
        if (item.classList) {
          item.classList.remove(...classes);
        }
        item.querySelectorAll('[class*=row-],[class*=col-]').forEach(element => {
          if (element.classList) {
            element.classList.remove(...classes);
          }
        });
      });
    }
  }
  return className;
};
class layout {
  constructor(templates, preview = false, disableHTMLLabels = false, controlConfig = {}) {
    this.preview = preview !== null && preview !== void 0 ? preview : false;
    this.disableHTMLLabels = disableHTMLLabels !== null && disableHTMLLabels !== void 0 ? disableHTMLLabels : false;
    this.controlConfig = controlConfig !== null && controlConfig !== void 0 ? controlConfig : {};
    this.templates = {
      label: null,
      help: null,
      default: (field, label, help, data) => {
        if (help) {
          label.appendChild(help);
        }
        return this.markup('div', [label, field], {
          className: processClassName(data, field)
        });
      },
      noLabel: (field, label, help, data) => {
        return this.markup('div', field, {
          className: processClassName(data, field)
        });
      },
      hidden: field => {
        return field;
      }
    };
    if (templates) {
      this.templates = jQuery.extend(this.templates, templates);
    }
    this.configure();
  }
  configure() {}
  build(renderControl, data, forceTemplate) {
    if (this.preview) {
      if (data.name) {
        data.name = data.name + '-preview';
      } else {
        data.name = js_utils.nameAttr(data) + '-preview';
      }
    }
    data.id = data.name;
    this.data = jQuery.extend({}, data);
    control.controlConfig = this.controlConfig;
    const controlInstance = new renderControl(data, this.preview);
    let field = controlInstance.build();
    if (typeof field !== 'object' || !field.field) {
      field = {
        field: field
      };
    }
    if (typeof field.field === 'string') {
      const tmpField = this.markup('div', field.field, {});
      if (tmpField.childElementCount === 1) {
        field.field = tmpField.children.item(0);
      } else {
        field.field = Array.from(tmpField.children);
      }
    }
    const label = this.label();
    const help = this.help();
    let elementTemplate;
    if (forceTemplate && this.isTemplate(forceTemplate)) {
      elementTemplate = forceTemplate;
    } else {
      elementTemplate = this.isTemplate(field.layout) ? field.layout : 'default';
    }
    const element = this.processTemplate(elementTemplate, field.field, label, help);
    controlInstance.on('prerender')(element);
    element.addEventListener('fieldRendered', controlInstance.on('render'));
    return element;
  }
  label() {
    const label = this.data.label || '';
    const labelText = this.disableHTMLLabels ? document.createTextNode(label) : js_utils.parsedHtml(label);
    const labelContents = [labelText];
    if (this.data.required) {
      labelContents.push(this.markup('span', '*', {
        className: 'formbuilder-required'
      }));
    }
    if (this.isTemplate('label')) {
      return this.processTemplate('label', labelContents);
    }
    return this.markup('label', labelContents, {
      for: this.data.id,
      className: `formbuilder-${this.data.type}-label`
    });
  }
  help() {
    if (!this.data.description) {
      return null;
    }
    if (this.isTemplate('help')) {
      return this.processTemplate('help', this.data.description);
    }
    return this.markup('span', '?', {
      className: 'tooltip-element',
      tooltip: this.data.description
    });
  }
  isTemplate(template) {
    return typeof this.templates[template] === 'function';
  }
  processTemplate(template, ...args) {
    let processed = this.templates[template](...args, this.data);
    if (processed.jquery) {
      processed = processed[0];
    }
    return processed;
  }
  markup(tag, content = '', attributes = {}) {
    return js_utils.markup(tag, content, attributes);
  }
}
;// ../src/js/config.js

const noop = () => null;
mi18n_min_default().addLanguage('en-US', {"NATIVE_NAME":"English (US)","ENGLISH_NAME":"English","addOption":"Add Option +","allFieldsRemoved":"All fields were removed.","allowMultipleFiles":"Allow users to upload multiple files","allowSelect":"Allow selection","autocomplete":"Autocomplete","button":"Button","cannotBeEmpty":"This field cannot be empty","checkboxGroup":"Checkbox Group","checkbox":"Checkbox","checkboxes":"Checkboxes","className":"Class","clearAllMessage":"Are you sure you want to clear all fields?","clear":"Clear","close":"Close","content":"Content","copy":"Copy To Clipboard","copyButton":"&#43;","copyButtonTooltip":"Copy","dateField":"Date Field","description":"Help Text","descriptionField":"Description","devMode":"Developer Mode","editNames":"Edit Names","editorTitle":"Form Elements","editXML":"Edit XML","enableOther":"Enable &quot;Other&quot;","enableOtherMsg":"Let users enter an unlisted option","fieldDeleteWarning":"false","fieldVars":"Field Variables","fieldNonEditable":"This field cannot be edited.","fieldRemoveWarning":"Are you sure you want to remove this field?","fileUpload":"File Upload","formUpdated":"Form Updated","getStarted":"Drag a field from the right to this area","header":"Header","hide":"Edit","hidden":"Hidden Input","inline":"Inline","inlineDesc":"Display {type} inline","label":"Label","labelEmpty":"Field Label cannot be empty","limitRole":"Limit access to one or more of the following roles:","mandatory":"Mandatory","maxlength":"Max Length","minOptionMessage":"This field requires a minimum of 2 options","minSelectionRequired":"Minimum {min} selections required","multipleFiles":"Multiple Files","name":"Name","no":"No","noFieldsToClear":"There are no fields to clear","number":"Number","off":"Off","on":"On","option":"Option","optionCount":"Option {count}","options":"Options","optional":"optional","optionLabelPlaceholder":"Label","optionValuePlaceholder":"Value","optionEmpty":"Option value required","other":"Other","paragraph":"Paragraph","placeholder":"Placeholder","placeholders.value":"Value","placeholders.label":"Label","placeholders.email":"Enter your email","placeholders.className":"space separated classes","placeholders.password":"Enter your password","preview":"Preview","radioGroup":"Radio Group","radio":"Radio","removeMessage":"Remove Element","removeOption":"Remove Option","remove":"&#215;","required":"Required","reset":"Reset","requireValidOption":"Only accept a pre-defined Option","richText":"Rich Text Editor","roles":"Access","rows":"Rows","save":"Save","selectOptions":"Options","select":"Select","selectColor":"Select Color","selectionsMessage":"Allow Multiple Selections","size":"Size","sizes":"Sizes","size.xs":"Extra Small","size.sm":"Small","size.m":"Default","size.lg":"Large","step":"Step","style":"Style","styles":"Styles","styles.btn":"Button Styles","styles.btn.default":"Default","styles.btn.danger":"Danger","styles.btn.info":"Info","styles.btn.primary":"Primary","styles.btn.success":"Success","styles.btn.warning":"Warning","submit":"Submit","subtype":"Type","text":"Text Field","textArea":"Text Area","toggle":"Toggle","warning":"Warning!","value":"Value","viewJSON":"[{&hellip;}]","viewXML":"&lt;/&gt;","yes":"Yes"});
const defaultOptions = {
  actionButtons: [],
  allowStageSort: true,
  append: false,
  controlOrder: ['autocomplete', 'button', 'checkbox-group', 'checkbox', 'date', 'file', 'header', 'hidden', 'number', 'paragraph', 'radio-group', 'select', 'text', 'textarea'],
  controlPosition: 'right',
  dataType: 'json',
  defaultFields: [],
  disabledActionButtons: [],
  disabledAttrs: [],
  disabledFieldButtons: {},
  disabledSubtypes: {},
  disableFields: [],
  disableHTMLLabels: false,
  disableInjectedStyle: false,
  editOnAdd: false,
  fields: [],
  fieldRemoveWarn: false,
  fieldEditContainer: null,
  inputSets: [],
  notify: {
    error: error => {
      console.log(error);
    },
    success: success => {
      console.log(success);
    },
    warning: warning => {
      console.warn(warning);
    }
  },
  onAddField: (fieldId, fieldData) => fieldData,
  onAddFieldAfter: (fieldId, fieldData) => fieldData,
  onAddOption: obj => obj,
  onClearAll: noop,
  onCloseFieldEdit: noop,
  onOpenFieldEdit: noop,
  onRemoveField: noop,
  onSave: noop,
  persistDefaultFields: false,
  prepend: false,
  replaceFields: [],
  roles: {
    1: 'Administrator'
  },
  sanitizerOptions: {
    clobberingProtection: {
      document: false,
      form: false
    },
    backendOrder: []
  },
  scrollToFieldOnAdd: true,
  showActionButtons: true,
  sortableControls: false,
  stickyControls: {
    enable: true,
    offset: {
      top: 5,
      bottom: 'auto',
      right: 'auto'
    }
  },
  subtypes: {},
  templates: {},
  typeUserAttrs: {},
  typeUserDisabledAttrs: {},
  typeUserEvents: {},
  defaultGridColumnClass: 'col-md-12',
  cancelGridModeDistance: 100,
  enableColumnInsertMenu: false,
  enableEnhancedBootstrapGrid: false
};
const styles = {
  btn: ['default', 'danger', 'info', 'primary', 'success', 'warning']
};
const defaultI18n = {
  location: 'assets/lang/'
};
const instanceConfig = {};
const gridClassNames = {
  rowWrapperClass: 'rowWrapper',
  colWrapperClass: 'colWrapper',
  tmpRowPlaceholderClass: 'tempRowWrapper',
  invisibleRowPlaceholderClass: 'invisibleRowPlaceholder'
};
const defaultTimeout = 333;
const defaultFieldSelector = 'li.form-field';
// EXTERNAL MODULE: ../node_modules/storage-available/storage-available.js
var storage_available = __webpack_require__(395);
var storage_available_default = /*#__PURE__*/__webpack_require__.n(storage_available);
;// ../src/js/helpers.js
const helpers_excluded = ["values"],
  helpers_excluded2 = ["label", "events"],
  _excluded3 = ["actionButtons", "replaceFields"];
function helpers_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function helpers_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? helpers_ownKeys(Object(t), !0).forEach(function (r) { helpers_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : helpers_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function helpers_defineProperty(e, r, t) { return (r = helpers_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function helpers_toPropertyKey(t) { var i = helpers_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function helpers_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
function helpers_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = helpers_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function helpers_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }







class Helpers {
  constructor(formId, layout, formBuilder) {
    this.data = instanceData[formId];
    this.d = instanceDom[formId];
    this.config = instanceConfig[formId];
    this.doCancel = false;
    this.layout = layout;
    this.handleKeyDown = this.handleKeyDown.bind(this);
    this.formBuilder = formBuilder;
    this.toastTimer = null;
  }
  startMoving(event, ui) {
    ui.item.show().addClass('moving');
    this.doCancel = true;
    this.from = ui.item.parent();
  }
  stopMoving(event, ui) {
    const _this = this;
    ui.item.removeClass('moving');
    if (_this.doCancel) {
      if (ui.sender) {
        $(ui.sender).sortable('cancel');
      }
      this.from.closest('.frmb-control').sortable('cancel');
    }
    _this.save();
    _this.doCancel = false;
  }
  beforeStop(event, ui) {
    const _this = this;
    const opts = this.config.opts;
    const form = _this.d.stage;
    const lastIndex = form.childNodes.length - 1;
    const cancelArray = [];
    _this.stopIndex = ui.placeholder.closest('ul.stage-wrap > *').index() - 1;
    if (!opts.sortableControls && ui.item.parent().hasClass('frmb-control')) {
      cancelArray.push(true);
    }
    cancelArray.push(ui.item.is(':not(li.input-control,li.input-set-control)') && !ui.item.parent().hasClass('frmb-control'));
    if (opts.prepend) {
      cancelArray.push(_this.stopIndex === 0);
    }
    if (opts.append) {
      cancelArray.push(_this.stopIndex + 1 === lastIndex);
    }
    _this.doCancel = cancelArray.some(elem => elem === true);
  }
  getTypes($field) {
    const types = {
      type: $field.attr('type')
    };
    const subtype = $('.fld-subtype', $field).val();
    if (subtype !== types.type) {
      types.subtype = subtype;
    }
    return types;
  }
  fieldOptionData(field) {
    const options = [];
    const $options = $('.sortable-options li', field);
    $options.each(i => {
      const option = $options[i];
      const stringAttrs = option.querySelectorAll('input:not([type=checkbox]):not([type=radio]), select');
      const boolAttrs = option.querySelectorAll('input[type=checkbox], input[type=radio]');
      const attrs = {};
      forEach(stringAttrs, i => {
        const stringAttr = stringAttrs[i];
        const attrName = stringAttr.dataset.attr;
        attrs[attrName] = stringAttr.value;
      });
      forEach(boolAttrs, i => {
        const boolAttr = boolAttrs[i];
        const attrName = boolAttr.getAttribute('data-attr');
        attrs[attrName] = boolAttr.checked;
      });
      options.push(attrs);
    });
    return options;
  }
  xmlSave(form) {
    const formData = this.prepData(form);
    const xmlSerializer = new XMLSerializer();
    const fields = ['<fields>'];
    formData.forEach(field => {
      const {
          values
        } = field,
        fieldData = helpers_objectWithoutProperties(field, helpers_excluded);
      let fieldHTML = [`<field ${xmlAttrString(fieldData)}>`];
      if (optionFields.includes(field.type)) {
        const options = values.map(option => markup('option', option.label, option).outerHTML);
        fieldHTML = fieldHTML.concat(options);
      }
      fieldHTML.push('</field>');
      fields.push(fieldHTML);
    });
    fields.push('</fields>');
    const formTemplate = markup('form-template', flattenArray(fields).join(''));
    return xmlSerializer.serializeToString(formTemplate);
  }
  prepData(form) {
    const formData = [];
    const _this = this;
    const config = this.config;
    if (form.childNodes.length !== 0) {
      const fields = [];
      forEach(form.childNodes, function (_index, fieldWrapper) {
        const $fieldWrapper = $(fieldWrapper);
        $fieldWrapper.find('li.form-field').each(function (i, field) {
          fields.push(field);
        });
      });
      forEach(form.childNodes, function (_index, testElement) {
        const $testElement = $(testElement);
        if ($testElement.is('li') && $testElement.hasClass('form-field')) {
          fields.push(testElement);
        }
      });
      if (fields.length) {
        fields.forEach(field => {
          const $field = $(field);
          if (!$field.hasClass('disabled-field')) {
            let fieldData = _this.getTypes($field);
            const $roleInputs = $('.roles-field:checked', field);
            const roleVals = $roleInputs.map(index => $roleInputs[index].value).get();
            fieldData = Object.assign({}, fieldData, _this.getAttrVals(field));
            if (fieldData.subtype) {
              if (fieldData.subtype === 'quill') {
                const id = `${fieldData.name}-preview`;
                if (window.fbEditors.quill[id]) {
                  const instance = window.fbEditors.quill[id].instance;
                  const data = instance.getContents();
                  fieldData.value = window.JSON.stringify(data.ops);
                }
              } else if (fieldData.subtype === 'tinymce' && window.tinymce) {
                const id = `${fieldData.name}-preview`;
                const editor = window.tinymce.get(id);
                if (editor) {
                  fieldData.value = editor.getContent();
                }
              }
            }
            if (roleVals.length) {
              fieldData.role = roleVals.join(',');
            }
            fieldData.className = fieldData.className || fieldData.class;
            if (fieldData.className && $field.attr('addeddefaultcolumnclass') == 'true' && $field.closest(this.formBuilder.rowWrapperClassSelector).children().length == 1 && fieldData.className.includes(config.opts.defaultGridColumnClass)) {
              const classes = getAllGridRelatedClasses(fieldData.className);
              if (classes && classes.length > 0) {
                classes.forEach(element => {
                  fieldData.className = fieldData.className.replace(element, '').trim();
                });
              }
            }
            if (fieldData.className) {
              const regex = new RegExp('(?:^|\\s)btn-(' + styles.btn.join('|') + ')(?:\\s|$)', 'g');
              const match = regex.exec(fieldData.className);
              if (match) {
                fieldData.style = match[1];
              }
            }
            fieldData = trimObj(fieldData);
            control.parseJsonAttrs(fieldData);
            $field.find('.form-group.field-options').each((_, attribute) => {
              const attributeName = attribute.getAttribute('name');
              fieldData[attributeName] = _this.fieldOptionData(attribute);
            });
            formData.push(fieldData);
          }
        });
      }
    }
    return formData;
  }
  getData(formData) {
    const data = this.data;
    if (!formData) {
      formData = this.config.opts.formData;
    }
    if (!formData) {
      return false;
    }
    const setData = {
      xml: formData => Array.isArray(formData) ? formData : parseXML(formData),
      json: formData => {
        if (typeof formData === 'string') {
          return window.JSON.parse(formData);
        }
        return formData;
      }
    };
    data.formData = setData[this.config.opts.dataType](formData) || [];
    return data.formData;
  }
  save(minify = false) {
    const _this = this;
    const data = this.data;
    const stage = this.d.stage;
    const doSave = {
      xml: () => _this.xmlSave(stage),
      json: minify => window.JSON.stringify(_this.prepData(stage), null, minify && '  ')
    };
    data.formData = doSave[this.config.opts.dataType](minify);
    stage.dispatchEvent(new Event('formSaved', {
      bubbles: true,
      cancelable: false
    }));
    return data.formData;
  }
  incrementId(id) {
    const split = id.lastIndexOf('-');
    const newFieldNumber = parseInt(id.substring(split + 1)) + 1;
    const baseString = id.substring(0, split);
    return `${baseString}-${newFieldNumber}`;
  }
  getAttrVals(field) {
    const config = this.config;
    const fieldData = Object.create(null);
    const attrs = field.querySelectorAll('[class*="fld-"]');
    forEach(attrs, index => {
      const attr = attrs[index];
      const name = camelCase(attr.getAttribute('name'));
      const attrVal = [[attr.attributes.contenteditable, () => config.opts.dataType === 'xml' ? escapeHtml(attr.innerHTML) : attr.innerHTML], [attr.type === 'checkbox', () => attr.checked], [attr.type === 'number' && attr.value !== '', () => Number(attr.value)], [attr.attributes.multiple, () => $(attr).val()], [true, () => attr.value]].find(([condition]) => !!condition)[1]();
      const regex = /^([^[]+)\[([^[\]]+)\]$/;
      const matches = regex.exec(name);
      if (matches) {
        const [, objName, propName] = matches;
        fieldData[objName] ??= {};
        fieldData[objName][propName] = attrVal;
      } else {
        fieldData[name] = attrVal;
      }
    });
    return fieldData;
  }
  updatePreview($field) {
    const _this = this;
    const fieldClass = $field.attr('class');
    const field = $field[0];
    if (!field || fieldClass.includes('input-control')) {
      return;
    }
    const fieldType = $field.attr('type');
    const $prevHolder = $('.prev-holder', field);
    let previewData = Object.assign({}, _this.getAttrVals(field), {
      type: fieldType
    });
    $field.find('.form-group.field-options').each((_, attribute) => {
      const attributeName = attribute.getAttribute('name');
      previewData[attributeName] = _this.fieldOptionData(attribute);
    });
    previewData = trimObj(previewData, true);
    previewData.className = _this.classNames(field, previewData);
    $field.data('fieldData', previewData);
    const custom = _this.formBuilder.controls.custom.lookup(previewData.type);
    const template = _this.formBuilder.controls.custom.getClass(previewData.type);
    const controlClass = custom ? custom.class : template || control.getClass(previewData.type, previewData.subtype);
    const preview = this.layout.build(controlClass, previewData);
    empty($prevHolder[0]);
    $prevHolder[0].appendChild(preview);
    preview.dispatchEvent(new Event('fieldRendered', {
      bubbles: true,
      cancelable: false
    }));
  }
  disabledTT(stage) {
    const move = (e, elem) => {
      const fieldOffset = elem.field.getBoundingClientRect();
      const x = e.clientX - fieldOffset.left - 21;
      const y = e.clientY - fieldOffset.top - elem.tt.offsetHeight - 12;
      elem.tt.style.transform = `translate(${x}px, ${y}px)`;
    };
    const disabledFields = stage.querySelectorAll('.disabled-field');
    forEach(disabledFields, index => {
      const field = disabledFields[index];
      const title = mi18n_min_default().get('fieldNonEditable');
      if (title) {
        const tt = markup('p', title, {
          className: 'frmb-tt'
        });
        field.appendChild(tt);
        field.addEventListener('mousemove', e => move(e, {
          tt,
          field
        }));
      }
    });
  }
  classNames(field, previewData) {
    const className = field.querySelector('.fld-className');
    const styleField = field.querySelector('.btn-style');
    const style = styleField && styleField.value;
    if (!className) {
      return;
    }
    const {
      type
    } = previewData;
    const classes = className.multiple ? $(className).val() : className.value.trim().split(' ');
    const types = {
      button: 'btn',
      submit: 'btn'
    };
    const primaryType = types[type];
    if (primaryType && style) {
      for (let i = 0; i < classes.length; i++) {
        const re = new RegExp(`^${primaryType}-(?:` + styles.btn.join('|') + ')$');
        const match = classes[i].match(re);
        if (match) {
          classes.splice(i, 1, primaryType + '-' + style);
        }
      }
      classes.push(primaryType + '-' + style);
      classes.push(primaryType);
    }
    const trimmedClassName = unique(classes).join(' ').trim();
    className.value = trimmedClassName;
    return trimmedClassName;
  }
  closeConfirm(overlay, dialog) {
    if (!overlay) {
      overlay = document.getElementsByClassName('form-builder-overlay')[0];
    }
    overlay && remove(overlay);
    if (!dialog) {
      dialog = document.getElementsByClassName('form-builder-dialog')[0];
    }
    dialog && remove(dialog);
    document.removeEventListener('keydown', this.handleKeyDown, false);
    document.dispatchEvent(new Event('modalClosed', {
      bubbles: true,
      cancelable: false
    }));
  }
  handleKeyDown(e) {
    const keyCode = e.keyCode || e.which;
    if (keyCode === 27) {
      e.preventDefault();
      this.closeConfirm.call(this);
    }
  }
  showOverlay() {
    const overlay = markup('div', null, {
      className: 'form-builder-overlay'
    });
    document.body.appendChild(overlay);
    overlay.classList.add('visible');
    overlay.addEventListener('click', ({
      target
    }) => this.closeConfirm(target), false);
    document.addEventListener('keydown', this.handleKeyDown, false);
    return overlay;
  }
  confirm(message, yesAction, coords = false, className = '') {
    const _this = this;
    const i18n = (mi18n_min_default()).current;
    const overlay = _this.showOverlay();
    const yes = markup('button', i18n.yes, {
      className: 'yes btn btn-success btn-sm'
    });
    const no = markup('button', i18n.no, {
      className: 'no btn btn-danger btn-sm'
    });
    no.onclick = function () {
      _this.closeConfirm(overlay);
    };
    yes.onclick = function () {
      yesAction();
      _this.closeConfirm(overlay);
    };
    const btnWrap = markup('div', [no, yes], {
      className: 'button-wrap'
    });
    className = 'form-builder-dialog ' + className;
    const miniModal = markup('div', [message, btnWrap], {
      className
    });
    if (!coords) {
      const dE = document.documentElement;
      coords = {
        pageX: Math.max(dE.clientWidth, window.innerWidth || 0) / 2,
        pageY: Math.max(dE.clientHeight, window.innerHeight || 0) / 2
      };
      miniModal.style.position = 'fixed';
    } else {
      miniModal.classList.add('positioned');
    }
    miniModal.style.left = coords.pageX + 'px';
    miniModal.style.top = coords.pageY + 'px';
    document.body.appendChild(miniModal);
    yes.focus();
    return miniModal;
  }
  dialog(content, coords = false, className = '') {
    const _this = this;
    const clientWidth = document.documentElement.clientWidth;
    const clientHeight = document.documentElement.clientHeight;
    _this.showOverlay();
    className = 'form-builder-dialog ' + className;
    const miniModal = markup('div', content, {
      className: className
    });
    if (!coords) {
      coords = {
        pageX: Math.max(clientWidth, window.innerWidth || 0) / 2,
        pageY: Math.max(clientHeight, window.innerHeight || 0) / 2
      };
      miniModal.style.position = 'fixed';
    } else {
      miniModal.classList.add('positioned');
    }
    miniModal.style.left = coords.pageX + 'px';
    miniModal.style.top = coords.pageY + 'px';
    document.body.appendChild(miniModal);
    document.dispatchEvent(new Event('modalOpened', {
      bubbles: true,
      cancelable: false
    }));
    if (className.indexOf('data-dialog') !== -1) {
      document.dispatchEvent(new Event('viewData', {
        bubbles: true,
        cancelable: false
      }));
    }
    return miniModal;
  }
  confirmRemoveAll(e) {
    const _this = this;
    const config = this.config;
    const formID = e.target.id.match(/frmb-\d{13}/)[0];
    const stage = document.getElementById(formID);
    const i18n = (mi18n_min_default()).current;
    const fields = $('li.form-field', stage);
    const buttonPosition = e.target.getBoundingClientRect();
    const bodyRect = document.body.getBoundingClientRect();
    const coords = {
      pageX: buttonPosition.left + buttonPosition.width / 2,
      pageY: buttonPosition.top - bodyRect.top - 12
    };
    if (fields.length) {
      _this.confirm(i18n.clearAllMessage, () => {
        _this.removeAllFields.call(_this, stage);
        if (config.opts.persistDefaultFields && config.opts.defaultFields) {
          this.addDefaultFields();
        } else {
          config.opts.notify.success(i18n.allFieldsRemoved);
        }
        config.opts.onClearAll();
      }, coords);
    } else {
      _this.dialog(i18n.noFieldsToClear, coords);
    }
  }
  addDefaultFields() {
    this.config.opts.defaultFields.forEach(field => this.formBuilder.prepFieldVars(field));
    this.d.stage.classList.remove('empty');
  }
  removeAllFields(stage) {
    const i18n = (mi18n_min_default()).current;
    const opts = this.config.opts;
    const fields = stage.querySelectorAll(this.formBuilder.fieldSelector);
    const markEmptyArray = [];
    if (!fields.length) {
      return;
    }
    if (opts.prepend) {
      markEmptyArray.push(true);
    }
    if (opts.append) {
      markEmptyArray.push(true);
    }
    if (!markEmptyArray.some(Boolean)) {
      stage.classList.add('empty');
      stage.dataset.content = i18n.getStarted;
    }
    this.emptyStage(stage);
  }
  emptyStage(stage) {
    empty(stage).classList.remove('removing');
    stage.dispatchEvent(new Event('stageEmptied', {
      bubbles: true,
      cancelable: false
    }));
    this.save();
  }
  stageIsEmpty() {
    return $(this.d.stage).find('li').length === 0;
  }
  setFieldOrder($cbUL) {
    if (!this.config.opts.sortableControls) {
      return false;
    }
    const JSON = window.JSON;
    const fieldOrder = [];
    $cbUL.children().each((index, element) => {
      const type = $(element).data('type');
      if (type) {
        fieldOrder.push(type);
      }
    });
    if (storage_available_default()('sessionStorage')) {
      window.sessionStorage.setItem('fieldOrder', JSON.stringify(fieldOrder));
    }
    return fieldOrder;
  }
  closeAllEdit() {
    const _this = this;
    $(_this.d.stage).find('li.form-field').each((i, elem) => {
      this.closeField(elem.id, false);
    });
  }
  toggleEdit(fieldId, animate = true) {
    const field = document.getElementById(fieldId);
    if (!field) {
      return;
    }
    if ($(field).hasClass('editing')) {
      return this.closeField(fieldId, animate);
    } else {
      return this.openField(fieldId, animate);
    }
  }
  closeField(fieldId, animate = true) {
    const _this = this;
    const field = document.getElementById(fieldId);
    if (!field) {
      return field;
    }
    const $editPanel = $('.frm-holder', field);
    const $preview = $('.prev-holder', field);
    let currentlyEditing = false;
    if ($(field).hasClass('editing')) {
      currentlyEditing = true;
    }
    if (!currentlyEditing) {
      return field;
    }
    field.classList.toggle('editing');
    $('.toggle-form', field).toggleClass('open');
    if (animate) {
      $preview.slideToggle(250);
      $editPanel.slideToggle(250);
    } else {
      $preview.toggle();
      $editPanel.toggle();
    }
    this.updatePreview($(field));
    const liContainer = $(`#${fieldId}`);
    const rowContainer = $(`#${fieldId}-cont`);
    rowContainer.append(liContainer);
    this.removeContainerProtection(rowContainer.attr('id'));
    this.config.opts.onCloseFieldEdit($editPanel[0]);
    this.d.stage.dispatchEvent(new Event('fieldEditClosed', {
      bubbles: true,
      cancelable: false
    }));
    const prevHolder = liContainer.find('.prev-holder');
    const resultsTimeout = setTimeout(() => {
      clearTimeout(resultsTimeout);
      const cleanResults = _this.tmpCleanPrevHolder(prevHolder);
      cleanResults.forEach(result => {
        if (result['columnInfo'].columnSize) {
          const currentClassRow = _this.getBootstrapColumnClass(rowContainer.attr('class'));
          if (currentClassRow !== result['columnInfo'].columnSize) {
            rowContainer.removeClass(currentClassRow).addClass(result['columnInfo'].columnSize);
            _this.tmpCleanPrevHolder(prevHolder);
          }
        }
      });
    }, 300);
    return field;
  }
  openField(fieldId, animate = true) {
    const field = document.getElementById(fieldId);
    if (!field) {
      return field;
    }
    const $editPanel = $('.frm-holder', field);
    const $preview = $('.prev-holder', field);
    let currentlyEditing = false;
    if ($(field).hasClass('editing')) {
      currentlyEditing = true;
    }
    if (currentlyEditing) {
      return field;
    }
    field.classList.toggle('editing');
    $('.toggle-form', field).toggleClass('open');
    if (animate) {
      $preview.slideToggle(250);
      $editPanel.slideToggle(250);
    } else {
      $preview.toggle();
      $editPanel.toggle();
    }
    this.updatePreview($(field));
    const liContainer = $(`#${fieldId}`);
    const colWrapper = $(`#${fieldId}-cont`);
    const rowWrapper = colWrapper.closest(this.formBuilder.rowWrapperClassSelector);
    this.formBuilder.preserveTempContainers.push(colWrapper.attr('id'));
    liContainer.insertAfter(rowWrapper);
    this.formBuilder.currentEditPanel = $editPanel[0];
    this.config.opts.onOpenFieldEdit($editPanel[0]);
    this.d.stage.dispatchEvent(new Event('fieldEditOpened', {
      bubbles: true,
      cancelable: false
    }));
    $(document).trigger('fieldOpened', [{
      rowWrapperID: rowWrapper.attr('id')
    }]);
    return field;
  }
  getStyle(elem, property = false) {
    let style;
    if (window.getComputedStyle) {
      style = window.getComputedStyle(elem, null);
    } else if (elem.currentStyle) {
      style = elem.currentStyle;
    }
    return property ? style[property] : style;
  }
  showData() {
    const formData = escapeHtml(this.getFormData(this.config.opts.dataType, true));
    const code = markup('code', formData, {
      className: `formData-${this.config.opts.dataType}`
    });
    this.dialog(markup('pre', code), false, 'data-dialog');
  }
  removeField(fieldID, animationSpeed = 250) {
    let fieldRemoved = false;
    const _this = this;
    const form = this.d.stage;
    const fields = form.getElementsByClassName('form-field');
    if (!fields.length) {
      this.config.opts.notify.warning('No fields to remove');
      return false;
    }
    if (!fieldID) {
      const availableIds = [].slice.call(fields).map(field => {
        return field.id;
      });
      this.config.opts.notify.warning('fieldID required to remove specific fields.');
      this.config.opts.notify.warning('Removing last field since no ID was supplied.');
      this.config.opts.notify.warning('Available IDs: ' + availableIds.join(', '));
      fieldID = availableIds[availableIds.length - 1];
    }
    const field = document.getElementById(fieldID);
    if (!field) {
      this.config.opts.notify.warning('Field not found');
      return false;
    }
    const $field = $(field);
    const fieldRowWrapper = $field.closest(this.formBuilder.rowWrapperClassSelector);
    $field.slideUp(animationSpeed, function () {
      $field.removeClass('deleting');
      $field.remove();
      fieldRemoved = true;
      _this.save();
      if (!form.childNodes.length) {
        form.classList.add('empty');
        form.dataset.content = (mi18n_min_default()).current.getStarted;
      }
    });
    const userEvents = Object.assign({}, this.config.opts.typeUserEvents['*'], this.config.opts.typeUserEvents[field.type]);
    if (userEvents && userEvents.onremove) {
      userEvents.onremove(field);
    }
    this.d.stage.dispatchEvent(new Event('fieldRemoved', {
      bubbles: true,
      cancelable: false
    }));
    if (fieldRowWrapper.length) {
      this.removeContainerProtection(`${fieldID}-cont`);
      const timeout = setTimeout(() => {
        clearTimeout(timeout);
        $(document).trigger('checkRowCleanup', [{
          rowWrapperID: fieldRowWrapper.attr('id')
        }]);
      }, defaultTimeout);
    }
    const fieldData = $field.data('fieldData') || {};
    this.config.opts.onRemoveField(fieldID, fieldData, field);
    return fieldRemoved;
  }
  processActionButtons(buttonData) {
    const {
        label,
        events
      } = buttonData,
      attrs = helpers_objectWithoutProperties(buttonData, helpers_excluded2);
    let labelText = label;
    const data = this.data;
    if (!labelText) {
      if (attrs.id) {
        labelText = (mi18n_min_default()).current[attrs.id] || capitalize(attrs.id);
      } else {
        labelText = '';
      }
    } else {
      labelText = (mi18n_min_default()).current[labelText] || labelText;
    }
    if (!attrs.id) {
      attrs.id = `${data.formID}-action-${Math.round(Math.random() * 1000)}`;
    } else {
      attrs.id = `${data.formID}-${attrs.id}-action`;
    }
    const button = markup('button', labelText, attrs);
    if (events) {
      for (const event in events) {
        if (events.hasOwnProperty(event)) {
          button.addEventListener(event, evt => events[event](evt));
        }
      }
    }
    return button;
  }
  processSubtypes(subtypeOpts) {
    const disabledSubtypes = this.config.opts.disabledSubtypes;
    for (const fieldType in subtypeOpts) {
      if (subtypeOpts.hasOwnProperty(fieldType)) {
        control.register(subtypeOpts[fieldType], control.getClass(fieldType), fieldType);
      }
    }
    const registeredSubtypes = control.getRegisteredSubtypes();
    const subtypeDef = Object.entries(registeredSubtypes).reduce((acc, [key, val]) => {
      acc[key] = disabledSubtypes[key] && subtract(disabledSubtypes[key], val) || val;
      return acc;
    }, {});
    const subtypes = {};
    for (const fieldType in subtypeDef) {
      if (subtypeDef.hasOwnProperty(fieldType)) {
        const formatted = [];
        for (const subtype of subtypeDef[fieldType]) {
          const controlClass = control.getClass(fieldType, subtype);
          const label = controlClass.mi18n(`subtype.${subtype}`) || controlClass.mi18n(subtype) || subtype;
          formatted.push({
            label,
            value: subtype
          });
        }
        subtypes[fieldType] = formatted;
      }
    }
    return subtypes;
  }
  editorUI(formID, controlPosition) {
    const d = this.d;
    const data = this.data;
    const id = formID || data.formID;
    const controlPositionClass = (controlPosition || '') === 'left' ? 'controls-left' : 'controls-right';
    d.editorWrap = markup('div', null, {
      id: `${data.formID}-form-wrap`,
      className: `form-wrap form-builder formbuilder-embedded-bootstrap ${mobileClass()} ${controlPositionClass}`
    });
    d.stage = markup('ul', null, {
      id,
      className: 'frmb stage-wrap'
    });
    d.controls = markup('ul', null, {
      id: `${id}-control-box`,
      className: 'frmb-control'
    });
    const buttons = this.formActionButtons();
    d.formActions = markup('div', buttons, {
      className: 'form-actions btn-group'
    });
  }
  formActionButtons() {
    const opts = this.config.opts;
    return opts.actionButtons.map(btnData => {
      if (btnData.id && opts.disabledActionButtons.indexOf(btnData.id) === -1) {
        return this.processActionButtons(btnData);
      }
    }).filter(Boolean);
  }
  processOptions(options) {
    const _this = this;
    const {
        actionButtons,
        replaceFields
      } = options,
      opts = helpers_objectWithoutProperties(options, _excluded3);
    let fieldEditContainer = opts.fieldEditContainer;
    if (typeof opts.fieldEditContainer === 'string') {
      fieldEditContainer = document.querySelector(opts.fieldEditContainer);
    }
    const mergedActionButtons = [{
      type: 'button',
      id: 'clear',
      className: 'clear-all btn btn-danger',
      events: {
        click: _this.confirmRemoveAll.bind(_this)
      }
    }, {
      type: 'button',
      label: 'viewJSON',
      id: 'data',
      className: 'btn btn-default get-data',
      events: {
        click: _this.showData.bind(_this)
      }
    }, {
      type: 'button',
      id: 'save',
      className: 'btn btn-primary save-template',
      events: {
        click: evt => {
          _this.save();
          _this.config.opts.onSave(evt, _this.data.formData);
        }
      }
    }].concat(actionButtons);
    opts.fields = opts.fields.concat(replaceFields);
    opts.disableFields = opts.disableFields.concat(replaceFields.map(({
      type
    }) => type && type));
    if (opts.dataType === 'xml') {
      opts.disableHTMLLabels = true;
    }
    _this.config.opts = helpers_objectSpread({
      actionButtons: mergedActionButtons,
      fieldEditContainer
    }, opts);
    return _this.config.opts;
  }
  input(attrs = {}) {
    return markup('input', null, attrs);
  }
  getFormData(type = 'js', formatted = false) {
    const h = this;
    const data = {
      js: () => h.prepData(h.d.stage),
      xml: () => h.xmlSave(h.d.stage),
      json: formatted => window.JSON.stringify(h.prepData(h.d.stage), null, formatted && '  ')
    };
    return data[type](formatted);
  }
  tmpCleanPrevHolder($prevHolder) {
    const _this = this;
    const cleanedFields = [];
    const formGroup = $prevHolder.find('.form-group');
    tmpCleanColumnInfo(formGroup);
    formGroup.find('*').each(function (i, field) {
      tmpCleanColumnInfo($(field));
    });
    function tmpCleanColumnInfo($field) {
      const classAttr = $field.attr('class');
      if (typeof classAttr !== 'undefined' && classAttr !== false) {
        const parseResult = _this.tryParseColumnInfo($field[0]);
        $field.attr('class', $field.attr('class').replace('__fb-tmp-col-', 'col-'));
        $field.attr('class', $field.attr('class').replace('__fb-tmp-row-', 'row-'));
        $field.attr('class', $field.attr('class').replace('col-', '__fb-tmp-col-'));
        $field.attr('class', $field.attr('class').replace('row-', '__fb-tmp-row-'));
        const result = {};
        result['field'] = $field;
        result['columnInfo'] = parseResult;
        cleanedFields.push(result);
      }
    }
    return cleanedFields;
  }
  tryParseColumnInfo(data) {
    const result = {};
    if (data.className) {
      const classes = getAllGridRelatedClasses(data.className);
      if (classes && classes.length > 0) {
        classes.forEach(element => {
          if (element.startsWith('row-')) {
            result['rowUniqueId'] = element.replace('row-', '').trim();
          } else {
            result['columnSize'] = element;
          }
        });
      }
    }
    return result;
  }
  removeContainerProtection(containerID) {
    const index = this.formBuilder.preserveTempContainers.indexOf(containerID);
    if (index !== -1) {
      this.formBuilder.preserveTempContainers.splice(index, 1);
    }
  }
  toggleHighlight(field, ms = 600) {
    field.addClass('moveHighlight');
    setTimeout(function () {
      field.removeClass('moveHighlight');
    }, ms);
  }
  showToast(msg, timeout = 3000) {
    if (this.toastTimer != null) {
      window.clearTimeout(this.toastTimer);
      this.toastTimer = null;
    }
    this.toastTimer = setTimeout(function () {
      $('.snackbar').removeClass('show');
    }, timeout);
    $('.snackbar').addClass('show').html(msg);
  }
  getDistanceBetweenPoints(x1, y1, x2, y2) {
    const y = x2 - x1;
    const x = y2 - y1;
    return Math.floor(Math.sqrt(x * x + y * y));
  }
  getRowClass(className) {
    if (className) {
      const splitClasses = className.split(' ').filter(x => x.startsWith('row-'));
      if (splitClasses && splitClasses.length > 0) {
        return splitClasses[0];
      }
    }
    return '';
  }
  getRowValue(className) {
    if (className) {
      const rowClass = this.getRowClass(className);
      if (rowClass) {
        return rowClass.split('-')[1];
      }
    }
    return '0';
  }
  changeRowClass(className, newValue) {
    const rowClass = this.getRowClass(className);
    return className.replace(rowClass, `row-${newValue}`);
  }
  getBootstrapColumnValue(className) {
    if (className) {
      const bootstrapClass = this.getBootstrapColumnClass(className);
      if (bootstrapClass) {
        return parseInt(bootstrapClass.split('-')[2]);
      }
    }
    return 0;
  }
  getBootstrapColumnPrefix(className) {
    if (className) {
      const bootstrapClass = this.getBootstrapColumnClass(className);
      if (bootstrapClass) {
        return `${bootstrapClass.split('-')[0]}-${bootstrapClass.split('-')[1]}`;
      }
    }
    return '';
  }
  getBootstrapColumnClass(className) {
    if (className) {
      const splitClasses = className.split(' ').filter(className => bootstrapColumnRegex.test(className));
      if (splitClasses && splitClasses.length > 0) {
        return splitClasses[0];
      }
    }
    return '';
  }
  changeBootstrapClass(className, newValue) {
    const boostrapClass = this.getBootstrapColumnClass(className);
    return className.replace(boostrapClass, `${this.getBootstrapColumnPrefix(className)}-${newValue}`);
  }
  syncBootstrapColumnWrapperAndClassProperty(fieldID, newValue) {
    const colWrapper = $(`#${fieldID}-cont`);
    colWrapper.attr('class', this.changeBootstrapClass(colWrapper.attr('class'), newValue));
    const inputClassElement = $(`#className-${fieldID}`);
    if (inputClassElement.val()) {
      inputClassElement.val(this.changeBootstrapClass(inputClassElement.val(), newValue));
    }
  }
  syncFieldWithNewRow(field, wrapperRow) {
    if (field) {
      const inputClassElement = $(field).find('.fld-className');
      const currentClassName = inputClassElement.val()?.trim();
      if (currentClassName) {
        var _wrapperRow$className;
        let currentClasses = currentClassName.split(' ');
        const oldRow = this.getRowClass(currentClassName);
        const newRow = this.getRowClass((_wrapperRow$className = wrapperRow?.className) !== null && _wrapperRow$className !== void 0 ? _wrapperRow$className : '');
        if (oldRow !== newRow) {
          if (oldRow) {
            currentClasses = currentClasses.filter(function (obj) {
              return obj !== oldRow;
            });
          }
          if (newRow) {
            currentClasses.push(newRow);
          }
          inputClassElement.val(currentClasses.join(' '));
        }
      }
    }
  }
}
;// ../src/js/control/autocomplete.js
const autocomplete_excluded = ["values", "type"];
function autocomplete_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = autocomplete_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function autocomplete_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }


class controlAutocomplete extends control {
  static get definition() {
    return {
      mi18n: {
        requireValidOption: 'requireValidOption'
      }
    };
  }
  build() {
    const _this$config = this.config,
      {
        values,
        type
      } = _this$config,
      data = autocomplete_objectWithoutProperties(_this$config, autocomplete_excluded);
    const keyboardNav = e => {
      const list = e.target.nextSibling.nextSibling;
      const hiddenField = e.target.nextSibling;
      const activeOption = this.getActiveOption(list);
      const keyCodeMapVals = [[38, () => {
        const previous = this.getPreviousOption(activeOption);
        if (previous) {
          this.selectOption(list, previous);
        }
      }], [40, () => {
        const next = this.getNextOption(activeOption);
        if (next) {
          this.selectOption(list, next);
        }
      }], [13, () => {
        if (activeOption) {
          e.target.value = activeOption.innerHTML;
          hiddenField.value = activeOption.getAttribute('value');
          if (list.style.display === 'none') {
            this.showList(list, activeOption);
          } else {
            this.hideList(list);
          }
        } else {
          if (this.config.requireValidOption) {
            if (!this.isOptionValid(list, e.target.value)) {
              e.target.value = '';
              e.target.nextSibling.value = '';
            }
          }
        }
        e.preventDefault();
      }], [27, () => {
        this.hideList(list);
      }]];
      const keyCodeMap = new Map(keyCodeMapVals);
      let direction = keyCodeMap.get(e.keyCode);
      if (!direction) {
        direction = () => false;
      }
      return direction();
    };
    const fauxEvents = {
      focus: evt => {
        const list = evt.target.nextSibling.nextSibling;
        const filteredOptions = filter(list.querySelectorAll('li'), evt.target.value);
        evt.target.addEventListener('keydown', keyboardNav);
        if (evt.target.value.length > 0) {
          const selectedOption = filteredOptions.length > 0 ? filteredOptions[filteredOptions.length - 1] : null;
          this.showList(list, selectedOption);
        }
      },
      blur: evt => {
        evt.target.removeEventListener('keydown', keyboardNav);
        const blurTimeout = setTimeout(() => {
          evt.target.nextSibling.nextSibling.style.display = 'none';
          clearTimeout(blurTimeout);
        }, 200);
        if (this.config.requireValidOption) {
          const list = evt.target.nextSibling.nextSibling;
          if (!this.isOptionValid(list, evt.target.value)) {
            evt.target.value = '';
            evt.target.nextSibling.value = '';
          }
        }
      },
      input: evt => {
        const list = evt.target.nextSibling.nextSibling;
        const hiddenField = evt.target.nextSibling;
        hiddenField.value = evt.target.value;
        const filteredOptions = filter(list.querySelectorAll('li'), evt.target.value);
        if (filteredOptions.length == 0) {
          this.hideList(list);
        } else {
          let activeOption = this.getActiveOption(list);
          if (!activeOption) {
            activeOption = filteredOptions[filteredOptions.length - 1];
          }
          this.showList(list, activeOption);
        }
      }
    };
    const fauxAttrs = Object.assign({}, data, {
      id: `${data.id}-input`,
      autocomplete: 'off',
      events: fauxEvents
    });
    const hiddenAttrs = Object.assign({}, data, {
      type: 'hidden'
    });
    delete fauxAttrs.name;
    const field = [this.markup('input', null, fauxAttrs), this.markup('input', null, hiddenAttrs)];
    const options = values.map(optionData => {
      const label = optionData.label;
      const config = {
        events: {
          click: evt => {
            const list = evt.target.parentElement;
            const field = list.previousSibling.previousSibling;
            field.value = optionData.label;
            field.nextSibling.value = optionData.value;
            this.hideList(list);
          }
        },
        value: optionData.value
      };
      return this.markup('li', label, config);
    });
    field.push(this.markup('ul', options, {
      id: `${data.id}-list`,
      className: `formbuilder-${type}-list`
    }));
    return field;
  }
  hideList(list) {
    this.selectOption(list, null);
    list.style.display = 'none';
  }
  showList(list, selectedOption) {
    this.selectOption(list, selectedOption);
    list.style.display = 'block';
    list.style.width = list.parentElement.offsetWidth + 'px';
  }
  getActiveOption(list) {
    const activeOption = list.getElementsByClassName('active-option')[0];
    if (activeOption && activeOption.style.display !== 'none') {
      return activeOption;
    }
    return null;
  }
  getPreviousOption(current) {
    let previous = current;
    do {
      previous = previous ? previous.previousSibling : null;
    } while (previous != null && previous.style.display === 'none');
    return previous;
  }
  getNextOption(current) {
    let next = current;
    do {
      next = next ? next.nextSibling : null;
    } while (next != null && next.style.display === 'none');
    return next;
  }
  selectOption(list, selectedOption) {
    const options = list.querySelectorAll('li');
    for (let i = 0; i < options.length; i++) {
      options[i].classList.remove('active-option');
    }
    if (selectedOption) {
      selectedOption.classList.add('active-option');
    }
  }
  isOptionValid(list, value) {
    const options = list.querySelectorAll('li');
    let validValue = false;
    for (let i = 0; i < options.length; i++) {
      if (options[i].innerHTML === value) {
        validValue = true;
        break;
      }
    }
    return validValue;
  }
  onRender(evt) {
    if (this.config.userData) {
      const $el = $('#' + this.config.name);
      const $options = $el.next();
      const preSelectedOption = this.config.userData[0];
      let selectedOption = null;
      $options.find('li').each(function () {
        if ($(this).attr('value') === preSelectedOption) {
          selectedOption = $(this).get(0);
        }
      });
      if (selectedOption === null) {
        if (this.config.requireValidOption) {
          return;
        } else {
          $el.prev().val(this.config.userData[0]);
          return;
        }
      }
      $el.prev().val(selectedOption.innerHTML);
      $el.val(selectedOption.getAttribute('value'));
      const list = $el.next().get(0);
      if (list.style.display === 'none') {
        this.showList(list, selectedOption);
      } else {
        this.hideList(list);
      }
    }
    return evt;
  }
}
control.register('autocomplete', controlAutocomplete);
;// ../src/js/control/button.js

class controlButton extends control {
  build() {
    return {
      field: this.markup('button', this.label, this.config),
      layout: 'noLabel'
    };
  }
}
control.register('button', controlButton);
control.register(['button', 'submit', 'reset'], controlButton, 'button');
;// ../src/js/control/custom.js

class controlCustom extends control {
  constructor(config, preview, template) {
    super(config, preview);
    this.template = template;
  }
  build() {
    let custom = this.template;
    if (!custom) {
      return control.error(`Invalid custom control type '${this.type}'. Please ensure you have registered it correctly as a template option.`);
    }
    const fieldData = Object.assign(this.config);
    const properties = ['label', 'description', 'subtype', 'id', 'preview', 'required', 'title', 'aria-required', 'type'];
    for (const prop of properties) {
      fieldData[prop] = this.config[prop] || this[prop];
    }
    custom = custom.bind(this);
    custom = custom(fieldData);
    if (custom.js) {
      this.js = custom.js;
    }
    if (custom.css) {
      this.css = custom.css;
    }
    this.onRender = custom.onRender;
    return {
      field: custom.field,
      layout: custom.layout
    };
  }
}
;// ../src/js/control/hidden.js

class controlHidden extends control {
  build() {
    this.field = this.markup('input', null, this.config);
    return {
      field: this.field,
      layout: 'hidden'
    };
  }
  onRender() {
    if (this.config.userData) {
      $(this.field).val(this.config.userData[0]);
    }
  }
}
control.register('hidden', controlHidden);
;// ../src/js/control/paragraph.js
const paragraph_excluded = ["type"];
function paragraph_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = paragraph_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function paragraph_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }


class controlParagraph extends control {
  build() {
    const _this$config = this.config,
      {
        type
      } = _this$config,
      attrs = paragraph_objectWithoutProperties(_this$config, paragraph_excluded);
    let tag = type;
    const typeMap = {
      paragraph: 'p',
      header: this.subtype
    };
    if (typeMap[type]) {
      tag = typeMap[type];
    }
    return {
      field: this.markup(tag, js_utils.parsedHtml(this.label), attrs),
      layout: 'noLabel'
    };
  }
}
control.register(['paragraph', 'header'], controlParagraph);
control.register(['p', 'address', 'blockquote', 'canvas', 'output'], controlParagraph, 'paragraph');
control.register(['h1', 'h2', 'h3', 'h4', 'h5', 'h6'], controlParagraph, 'header');
;// ../src/js/control/select.js
const select_excluded = ["values", "value", "placeholder", "type", "inline", "other", "toggle"],
  select_excluded2 = ["label"];
function select_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = select_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function select_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }


class controlSelect extends control {
  static get definition() {
    return {
      inactive: ['checkbox'],
      mi18n: {
        minSelectionRequired: 'minSelectionRequired'
      }
    };
  }
  build() {
    const options = [];
    const _this$config = this.config,
      {
        values,
        value,
        placeholder,
        type,
        inline,
        other,
        toggle
      } = _this$config,
      data = select_objectWithoutProperties(_this$config, select_excluded);
    const optionType = type.replace('-group', '');
    const isSelect = type === 'select';
    if (data.multiple || type === 'checkbox-group') {
      data.name = data.name + '[]';
    }
    if ((type === 'checkbox-group' || type === 'radio-group') && data.required) {
      const self = this;
      const defaultOnRender = this.onRender.bind(this);
      this.onRender = function () {
        defaultOnRender();
        self.groupRequired();
      };
    }
    delete data.title;
    if (values) {
      if (placeholder && isSelect) {
        options.push(this.markup('option', placeholder, {
          disabled: true,
          selected: true,
          value: ''
        }));
      }
      for (let i = 0; i < values.length; i++) {
        let option = values[i];
        if (typeof option === 'string') {
          option = {
            label: option,
            value: option
          };
        }
        const {
            label = ''
          } = option,
          optionAttrs = select_objectWithoutProperties(option, select_excluded2);
        optionAttrs.id = `${data.id}-${i}`;
        if (!optionAttrs.selected || placeholder) {
          delete optionAttrs.selected;
        }
        if (typeof value !== 'undefined' && optionAttrs.value === value) {
          optionAttrs.selected = true;
        }
        if (isSelect) {
          const o = this.markup('option', document.createTextNode(label), optionAttrs);
          options.push(o);
        } else {
          const labelContents = [label];
          let wrapperClass = `formbuilder-${optionType}`;
          if (inline) {
            wrapperClass += '-inline';
          }
          optionAttrs.type = optionType;
          if (optionAttrs.selected) {
            optionAttrs.checked = 'checked';
            delete optionAttrs.selected;
          }
          const input = this.markup('input', null, Object.assign({}, data, optionAttrs));
          const labelAttrs = {
            for: optionAttrs.id
          };
          let output = [input, this.markup('label', labelContents, labelAttrs)];
          if (toggle) {
            delete labelAttrs.for;
            labelAttrs.className = 'kc-toggle';
            labelContents.unshift(input, this.markup('span'));
            output = this.markup('label', labelContents, labelAttrs);
          }
          const wrapper = this.markup('div', output, {
            className: wrapperClass
          });
          options.push(wrapper);
        }
      }
      if (!isSelect && other) {
        var _data$className;
        const otherOptionAttrs = {
          id: `${data.id}-other`,
          className: `${(_data$className = data.className) !== null && _data$className !== void 0 ? _data$className : ''} other-option`,
          value: ''
        };
        let wrapperClass = `formbuilder-${optionType}`;
        if (inline) {
          wrapperClass += '-inline';
        }
        const optionAttrs = Object.assign({}, data, otherOptionAttrs);
        optionAttrs.type = optionType;
        const otherValAttrs = {
          type: 'text',
          events: {
            input: evt => {
              const otherInput = evt.target;
              const other = otherInput.parentElement.previousElementSibling;
              other.value = otherInput.value;
            }
          },
          id: `${otherOptionAttrs.id}-value`,
          className: 'other-val'
        };
        const primaryInput = this.markup('input', null, optionAttrs);
        const otherInputs = [document.createTextNode(control.mi18n('other')), this.markup('input', null, otherValAttrs)];
        const inputLabel = this.markup('label', otherInputs, {
          for: optionAttrs.id
        });
        const wrapper = this.markup('div', [primaryInput, inputLabel], {
          className: wrapperClass
        });
        options.push(wrapper);
      }
    }
    if (type === 'select') {
      this.dom = this.markup(optionType, options, trimObj(data, true));
    } else {
      let className = type;
      if (inline) {
        className += ` ${className}--inline`;
      }
      this.dom = this.markup('div', options, {
        className
      });
    }
    return this.dom;
  }
  groupRequired() {
    const allInputs = this.element.getElementsByTagName('input');
    const checkboxes = this.element.querySelectorAll('input:not([type=text])');
    const otherCheckbox = this.element.querySelector('.other-option');
    const otherValue = this.element.querySelector('.other-val');
    const setValidity = (checkbox, isValid) => {
      const minReq = control.mi18n('minSelectionRequired', 1);
      if (isValid) {
        checkbox.setCustomValidity('');
      } else {
        checkbox.setCustomValidity(minReq);
      }
    };
    const toggleRequired = (checkboxes, otherCheckbox, otherValue, isValid) => {
      Array.prototype.forEach.call(checkboxes, cb => {
        if (isValid) {
          cb.removeAttribute('required');
        } else {
          cb.setAttribute('required', 'required');
        }
        setValidity(cb, isValid);
      });
      if (otherCheckbox) {
        if (otherCheckbox.checked) {
          otherValue.setAttribute('required', 'required');
        } else {
          otherValue.removeAttribute('required');
        }
      }
    };
    const toggleValid = () => {
      const isValid = [].some.call(checkboxes, cb => cb.checked);
      toggleRequired(checkboxes, otherCheckbox, otherValue, isValid);
    };
    for (let i = allInputs.length - 1; i >= 0; i--) {
      allInputs[i].addEventListener('change', toggleValid);
    }
    toggleValid();
  }
  onRender() {
    if (this.config.userData) {
      const selectedOptions = this.config.userData.slice();
      if (this.config.type === 'select') {
        $(this.dom).val(selectedOptions).prop('selected', true);
      } else if (this.config.type.endsWith('-group')) {
        if (this.config.type === 'checkbox-group') {
          this.dom.querySelectorAll('input[type=checkbox]').forEach(input => {
            input.removeAttribute('checked');
          });
        }
        this.dom.querySelectorAll('input').forEach(input => {
          if (input.classList.contains('other-val')) {
            return;
          }
          for (let i = 0; i < selectedOptions.length; i++) {
            if (input.value === selectedOptions[i]) {
              input.setAttribute('checked', 'checked');
              selectedOptions.splice(i, 1);
              break;
            }
          }
          if (input.id.endsWith('-other') && selectedOptions.length > 0) {
            const otherVal = this.dom.querySelector(`#${input.id}-value`);
            input.setAttribute('checked', 'checked');
            otherVal.value = input.value = selectedOptions[0];
            otherVal.style.display = 'inline-block';
          }
        });
      }
    }
  }
}
control.register(['select', 'checkbox-group', 'radio-group', 'checkbox'], controlSelect);
;// ../src/js/control/text.js

class controlText extends control {
  static get definition() {
    return {
      mi18n: {
        date: 'dateField',
        file: 'fileUpload'
      }
    };
  }
  build() {
    let {
      name
    } = this.config;
    name = this.config.multiple ? `${name}[]` : name;
    const inputConfig = Object.assign({}, this.config, {
      name
    });
    this.dom = this.markup('input', null, inputConfig);
    return this.dom;
  }
  onRender() {
    if (this.config.userData) {
      $(this.dom).val(this.config.userData[0]);
    }
  }
}
control.register(['text', 'file', 'date', 'number'], controlText);
control.register(['text', 'password', 'email', 'color', 'tel'], controlText, 'text');
control.register(['date', 'time', 'datetime-local'], controlText, 'date');
control.register(['number', 'range'], controlText, 'number');
;// ../src/js/control/textarea.js
const textarea_excluded = ["value"];
function textarea_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = textarea_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function textarea_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }

class controlTextarea extends control {
  static get definition() {
    return {
      mi18n: {
        textarea: 'textArea'
      }
    };
  }
  build() {
    const _this$config = this.config,
      {
        value = ''
      } = _this$config,
      attrs = textarea_objectWithoutProperties(_this$config, textarea_excluded);
    delete attrs['type'];
    this.field = this.markup('textarea', this.parsedHtml(value), attrs);
    return this.field;
  }
  onRender() {
    if (this.config.userData) {
      $(this.field).val(this.config.userData[0]);
    }
  }
  on(eventType) {
    if (eventType == 'prerender' && this.preview) {
      return element => {
        if (this.field) {
          element = this.field;
        }
        $(element).on('mousedown', e => {
          e.stopPropagation();
        });
      };
    }
    return super.on(eventType);
  }
}
control.register('textarea', controlTextarea);
control.register('textarea', controlTextarea, 'textarea');
;// ../src/js/control/textarea.tinymce.js
const textarea_tinymce_excluded = ["value"];
function textarea_tinymce_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = textarea_tinymce_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function textarea_tinymce_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }

class controlTinymce extends controlTextarea {
  configure() {
    this.js = [];
    if (!window.tinymce) {
      this.js.push('https://cdnjs.cloudflare.com/ajax/libs/tinymce/4.9.11/tinymce.min.js');
    }
    if (this.classConfig.js) {
      let js = this.classConfig.js;
      if (!Array.isArray(js)) {
        js = new Array(js);
      }
      this.js = this.js.concat(js);
      delete this.classConfig.js;
    }
    if (this.classConfig.css) {
      this.css = this.classConfig.css;
    }
    this.editorOptions = {
      height: 250,
      paste_data_images: true,
      plugins: ['advlist', 'autolink', 'lists', 'link', 'image', 'charmap', 'print', 'preview', 'anchor', 'searchreplace', 'visualblocks', 'code', 'fullscreen', 'insertdatetime', 'media', 'table', 'contextmenu', 'paste', 'code'],
      toolbar: 'undo redo | styleselect | bold italic | alignleft aligncenter alignright alignjustify | bullist numlist outdent indent | link image | table'
    };
  }
  build() {
    const _this$config = this.config,
      {
        value = ''
      } = _this$config,
      attrs = textarea_tinymce_objectWithoutProperties(_this$config, textarea_tinymce_excluded);
    delete attrs['type'];
    this.field = this.markup('textarea', this.parsedHtml(value), attrs);
    if (attrs.disabled) {
      this.editorOptions.readonly = true;
    }
    return this.field;
  }
  onRender() {
    const oldInst = window.tinymce.get(this.id);
    if (oldInst) {
      window.tinymce.remove(oldInst);
    }
    const options = jQuery.extend(this.editorOptions, this.classConfig);
    options.target = this.field;
    const removedPlugins = [];
    if (Number(window.tinymce.majorVersion) >= 5) {
      removedPlugins.push('contextmenu');
    }
    if (Number(window.tinymce.majorVersion) >= 6) {
      removedPlugins.push('paste', 'print');
    }
    options.plugins = options.plugins.filter(plugin => {
      return removedPlugins.indexOf(plugin) === -1;
    });
    const userData = this.config.userData ? this.parsedHtml(this.config.userData[0]) : undefined;
    const copiedData = window.lastFormBuilderCopiedTinyMCE ? this.parsedHtml(window.lastFormBuilderCopiedTinyMCE) : undefined;
    window.lastFormBuilderCopiedTinyMCE = null;
    const afterInit = function (inst) {
      if (copiedData) {
        inst[0].setContent(copiedData);
      } else if (userData) {
        inst[0].setContent(userData);
      }
    };
    setTimeout(() => {
      window.tinymce.init(options).then(afterInit);
    }, 0);
  }
}
controlTextarea.register('tinymce', controlTinymce, 'textarea');
;// ../src/js/control/textarea.quill.js
const textarea_quill_excluded = ["value"];
function textarea_quill_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = textarea_quill_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function textarea_quill_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function textarea_quill_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function textarea_quill_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? textarea_quill_ownKeys(Object(t), !0).forEach(function (r) { textarea_quill_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : textarea_quill_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function textarea_quill_defineProperty(e, r, t) { return (r = textarea_quill_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function textarea_quill_toPropertyKey(t) { var i = textarea_quill_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function textarea_quill_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }


class controlQuill extends controlTextarea {
  configure() {
    const defaultClassConfig = {
      js: 'https://cdn.quilljs.com/1.2.4/quill.js',
      css: 'https://cdn.quilljs.com/1.2.4/quill.snow.css'
    };
    const defaultEditorConfig = {
      modules: {
        toolbar: [[{
          header: [1, 2, false]
        }], ['bold', 'italic', 'underline'], ['code-block']]
      },
      placeholder: this.config.placeholder || '',
      theme: 'snow'
    };
    const [customClassConfig, customEditorConfig] = js_utils.splitObject(this.classConfig, ['css', 'js']);
    Object.assign(this, textarea_quill_objectSpread(textarea_quill_objectSpread({}, defaultClassConfig), customClassConfig));
    this.editorConfig = textarea_quill_objectSpread(textarea_quill_objectSpread({}, defaultEditorConfig), customEditorConfig);
  }
  build() {
    const _this$config = this.config,
      {
        value = ''
      } = _this$config,
      attrs = textarea_quill_objectWithoutProperties(_this$config, textarea_quill_excluded);
    delete attrs['type'];
    this.field = this.markup('div', null, attrs);
    if (this.field.classList.contains('form-control')) {
      this.field.classList.remove('form-control');
    }
    return this.field;
  }
  onRender(evt) {
    const value = this.config.value || '';
    const Delta = window.Quill.import('delta');
    window.fbEditors.quill[this.id] = {};
    const editor = window.fbEditors.quill[this.id];
    editor.instance = new window.Quill(this.field, this.editorConfig);
    editor.data = new Delta();
    if (value) {
      editor.instance.setContents(window.JSON.parse(this.parsedHtml(value)));
    }
    editor.instance.on('text-change', function (delta) {
      editor.data = editor.data.compose(delta);
    });
    return evt;
  }
}
controlTextarea.register('quill', controlQuill, 'textarea');
;// ../src/js/control/index.js










/* harmony default export */ var js_control = ({
  controlAutocomplete: controlAutocomplete,
  controlButton: controlButton,
  controlCustom: controlCustom,
  controlHidden: controlHidden,
  controlParagraph: controlParagraph,
  controlSelect: controlSelect,
  controlText: controlText,
  controlTextarea: controlTextarea,
  controlTinymce: controlTinymce,
  controlQuill: controlQuill
});
;// ../src/js/customControls.js



class customControls {
  constructor(templates = {}, fields = []) {
    this.customRegister = {};
    this.templateControlRegister = {};
    this.def = {
      icon: {},
      i18n: {}
    };
    this.register(templates, fields);
  }
  register(templates = {}, fields = []) {
    fields.forEach(field => {
      if (field.template) {
        const fieldType = field.type || field.attrs?.type;
        templates[fieldType] = field.template;
      }
    });
    const locale = (mi18n_min_default()).locale;
    if (!this.def.i18n[locale]) {
      this.def.i18n[locale] = {};
    }
    const _this = this;
    Object.keys(templates).forEach(templateName => {
      const templateControl = function (config, preview) {
        this.customControl = new controlCustom(config, preview, templates[templateName]);
        this.build = function () {
          return this.customControl.build();
        };
        this.on = function (eventType) {
          return this.customControl.on(eventType);
        };
      };
      templateControl.definition = {};
      templateControl.label = type => _this.label(type);
      templateControl.icon = type => _this.icon(type);
      this.templateControlRegister[templateName] = templateControl;
    });
    for (const field of fields) {
      let type = field.type;
      field.attrs = field.attrs || {};
      if (!type) {
        if (!field.attrs.type) {
          control.error('Ignoring invalid custom field definition. Please specify a type property.');
          continue;
        }
        type = field.attrs.type;
      }
      let lookup = field.subtype || type;
      if (!templates[type]) {
        try {
          const controlClass = control.getClass(type, field.subtype);
          lookup = field.datatype ? field.datatype : `${type}-${Math.floor(Math.random() * 9000 + 1000)}`;
          this.customRegister[lookup] = jQuery.extend(field, {
            type: type,
            class: controlClass
          });
        } catch (e) {
          control.error('Error while registering custom field: ' + type + (field.subtype ? ':' + field.subtype : '') + '. Unable to find any existing defined control or template for rendering.');
        }
      } else {
        const controlClass = this.templateControlRegister[type];
        controlClass.definition = field;
        this.customRegister[lookup] = jQuery.extend(field, {
          type: type,
          class: controlClass
        });
      }
      this.def.i18n[locale][lookup] = Array.isArray(field.label) ? mi18n_min_default().get(...field.label) || field.label[0] : field.label;
      this.def.icon[lookup] = field.icon;
    }
  }
  label(type) {
    const def = this.def;
    let i18n = def.i18n || {};
    const locale = (mi18n_min_default()).locale;
    i18n = i18n[locale] || i18n.default || i18n;
    const lookupCamel = control.camelCase(type);
    const value = typeof i18n == 'object' ? i18n[lookupCamel] || i18n[type] : i18n;
    if (value) {
      return value;
    } else {
      let mapped = def.mi18n;
      if (typeof mapped === 'object') {
        mapped = mapped[lookupCamel] || mapped[type];
      }
      if (!mapped) {
        mapped = lookupCamel;
      }
      return mi18n_min_default().get(mapped);
    }
  }
  get definition() {
    return {};
  }
  icon(type) {
    const def = this.def;
    if (def && typeof def.icon === 'object') {
      return def.icon[type];
    }
    return def.icon;
  }
  getRegistered(type = false) {
    if (type) {
      var _this$templateControl;
      return (_this$templateControl = this.templateControlRegister[type]) !== null && _this$templateControl !== void 0 ? _this$templateControl : undefined;
    }
    return Object.keys(this.customRegister);
  }
  getClass(type) {
    var _this$templateControl2;
    return (_this$templateControl2 = this.templateControlRegister[type]) !== null && _this$templateControl2 !== void 0 ? _this$templateControl2 : undefined;
  }
  lookup(lookup) {
    return this.customRegister[lookup];
  }
}
;// ../src/fonts/config.json
var fonts_config_namespaceObject = /*#__PURE__*/JSON.parse('{"fn":"formbuilder-icon-"}');
;// ../src/js/controls.js







const css_prefix_text = fonts_config_namespaceObject.fn;
class Controls {
  constructor(opts, d) {
    this.opts = opts;
    this.dom = d.controls;
    this.getRegistered = control.getRegistered;
    this.init();
  }
  init() {
    this.setupControls();
    this.appendControls();
  }
  setupControls() {
    const opts = this.opts;
    control.loadCustom(opts.controls);
    this.custom = new customControls(opts.templates, opts.fields);
    const registeredControls = control.getRegistered();
    const customFields = this.custom.getRegistered();
    if (customFields) {
      jQuery.merge(registeredControls, customFields);
    }
    this.registeredSubtypes = control.getRegisteredSubtypes();
    if (opts.sortableControls) {
      this.dom.classList.add('sort-enabled');
    }
    this.controlList = [];
    this.allControls = {};
    for (let i = 0; i < registeredControls.length; i++) {
      const type = registeredControls[i];
      let custom = this.custom.lookup(type);
      let controlClass;
      let label;
      if (custom) {
        controlClass = custom.class;
        label = this.custom.label(type);
      } else {
        custom = {};
        controlClass = control.getClass(type);
        if (!controlClass || !controlClass.active(type)) {
          continue;
        }
        label = controlClass.label(type);
      }
      const icon = custom.icon || controlClass.icon(type);
      const iconClassName = !icon ? custom.iconClassName || `${css_prefix_text + type.replace(/-[\d]{4}$/, '')}` : '';
      if (icon) {
        label = `<span class="control-icon">${icon}</span>${label}`;
      }
      const newFieldControl = markup('li', markup('span', label), {
        className: `${iconClassName} input-control input-control-${i}`
      });
      newFieldControl.dataset.type = type;
      this.controlList.push(type);
      this.allControls[type] = newFieldControl;
    }
    if (opts.inputSets.length) {
      opts.inputSets.forEach((set, i) => {
        let {
          name,
          label
        } = set;
        name = name || hyphenCase(label);
        if (set.icon) {
          label = `<span class="control-icon">${set.icon}</span>${label}`;
        }
        const inputSet = markup('li', markup('span', label), {
          className: `input-set-control input-set-${i}`
        });
        inputSet.dataset.type = name;
        this.controlList.push(name);
        this.allControls[name] = inputSet;
      });
    }
  }
  orderFields(controls) {
    const opts = this.opts;
    const controlOrder = opts.controlOrder.concat(controls);
    let fieldOrder;
    if (storage_available_default()('sessionStorage')) {
      if (opts.sortableControls) {
        fieldOrder = window.sessionStorage.getItem('fieldOrder');
      } else {
        window.sessionStorage.removeItem('fieldOrder');
      }
    }
    if (!fieldOrder) {
      fieldOrder = unique(controlOrder);
    } else {
      fieldOrder = window.JSON.parse(fieldOrder);
      fieldOrder = unique(fieldOrder.concat(controls));
      fieldOrder = Object.keys(fieldOrder).map(i => fieldOrder[i]);
    }
    fieldOrder.forEach(field => {
      const randomKey = new RegExp('-[\\d]{4}$');
      if (field.match(randomKey)) {
        const baseFieldIndex = fieldOrder.indexOf(field.replace(randomKey, ''));
        if (baseFieldIndex !== -1) {
          fieldOrder.splice(fieldOrder.indexOf(field), 1);
          fieldOrder.splice(baseFieldIndex + 1, fieldOrder.indexOf(field), field);
        }
      }
    });
    if (opts.disableFields.length) {
      fieldOrder = fieldOrder.filter(type => !opts.disableFields.includes(type));
    }
    return fieldOrder.filter(Boolean);
  }
  appendControls() {
    const fragment = document.createDocumentFragment();
    empty(this.dom);
    this.orderFields(this.controlList).forEach(controlKey => {
      const control = this.allControls[controlKey];
      if (control) {
        fragment.appendChild(control);
      }
    });
    this.dom.appendChild(fragment);
  }
  getClass(type, subtype) {
    return this.custom.getClass(type) || control.getClass(type, subtype);
  }
}
;// ../src/js/form-builder.js
const form_builder_excluded = ["class", "className"],
  form_builder_excluded2 = ["multiple", "options", "label", "value", "class", "className"],
  form_builder_excluded3 = ["className"],
  _excluded4 = ["events"],
  _excluded5 = ["tag", "content"],
  _excluded6 = ["i18n"];
function form_builder_objectWithoutProperties(e, t) { if (null == e) return {}; var o, r, i = form_builder_objectWithoutPropertiesLoose(e, t); if (Object.getOwnPropertySymbols) { var n = Object.getOwnPropertySymbols(e); for (r = 0; r < n.length; r++) o = n[r], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e, o) && (i[o] = e[o]); } return i; }
function form_builder_objectWithoutPropertiesLoose(r, e) { if (null == r) return {}; var t = {}; for (var n in r) if ({}.hasOwnProperty.call(r, n)) { if (-1 !== e.indexOf(n)) continue; t[n] = r[n]; } return t; }
function form_builder_ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function form_builder_objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? form_builder_ownKeys(Object(t), !0).forEach(function (r) { form_builder_defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : form_builder_ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function form_builder_defineProperty(e, r, t) { return (r = form_builder_toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function form_builder_toPropertyKey(t) { var i = form_builder_toPrimitive(t, "string"); return "symbol" == typeof i ? i : i + ""; }
function form_builder_toPrimitive(t, r) { if ("object" != typeof t || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != typeof i) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }














const form_builder_css_prefix_text = fonts_config_namespaceObject.fn;
const {
  rowWrapperClass,
  colWrapperClass,
  tmpRowPlaceholderClass,
  invisibleRowPlaceholderClass
} = gridClassNames;
const {
  rowWrapperClassSelector,
  colWrapperClassSelector,
  tmpRowPlaceholderClassSelector,
  invisibleRowPlaceholderClassSelector
} = generateSelectorClassNames(gridClassNames);
function FormBuilder(opts, element, $) {
  const formBuilder = this;
  const i18n = (mi18n_min_default()).current;
  const formID = `frmb-${Date.now()}`;
  const data = new Data(formID);
  const d = new Dom(formID);
  const config = instanceConfig[formID] = {};
  let formRows = [];
  formBuilder.preserveTempContainers = [];
  formBuilder.rowWrapperClassSelector = rowWrapperClassSelector;
  formBuilder.colWrapperClassSelector = colWrapperClassSelector;
  formBuilder.colWrapperClass = colWrapperClass;
  formBuilder.fieldSelector = opts.enableEnhancedBootstrapGrid ? rowWrapperClassSelector : defaultFieldSelector;
  setSanitizerConfig(opts.sanitizerOptions);
  if ($(element).closest('form').length) {
    opts.notify.warning('WARNING: FormBuilder does not support being contained with a <form> Element');
  }
  if (!opts.layout) {
    opts.layout = layout;
  }
  const layoutEngine = new opts.layout(opts.layoutTemplates, true, opts.disableHTMLLabels, opts.controlConfig);
  const h = new Helpers(formID, layoutEngine, formBuilder);
  const m = markup;
  opts = h.processOptions(opts);
  h.editorUI(formID, opts.controlPosition);
  data.formID = formID;
  data.lastID = `${data.formID}-fld-0`;
  const controls = new Controls(opts, d);
  formBuilder.controls = controls;
  const subtypes = config.subtypes = h.processSubtypes(opts.subtypes);
  const $stage = $(d.stage);
  d.stage.fbInstance = formBuilder;
  const $cbUL = $(d.controls);
  let insertingNewControl = false;
  let insertTargetIsRow = false;
  let insertTargetIsColumn = false;
  let $targetInsertWrapper;
  let cloneControls;
  function enhancedBootstrapEnabled() {
    return !!opts.enableEnhancedBootstrapGrid;
  }
  $stage.sortable({
    cursor: 'move',
    opacity: 0.9,
    revert: 150,
    beforeStop: (evt, ui) => h.beforeStop.call(h, evt, ui),
    start: (evt, ui) => h.startMoving.call(h, evt, ui),
    stop: (evt, ui) => h.stopMoving.call(h, evt, ui),
    change: function (event, ui) {
      if (opts.prepend && ui.placeholder.index() < 1) {
        $('li.form-prepend').after(ui.placeholder);
      } else if (opts.append && ui.placeholder.index() >= $stage.children('li').length - 1) {
        $('li.form-append').before(ui.placeholder);
      }
    },
    cancel: ['input', 'select', 'textarea', '.disabled-field', '.form-elements', '.btn', 'button', '.is-locked'].join(', '),
    placeholder: 'frmb-placeholder hoverDropStyleInverse'
  });
  if (!opts.allowStageSort) {
    $stage.sortable('disable');
  }
  $cbUL.sortable({
    helper: 'clone',
    opacity: 0.9,
    connectWith: `#${formID}, ${rowWrapperClassSelector}`,
    cancel: '.formbuilder-separator',
    cursor: 'move',
    scroll: false,
    placeholder: 'hoverDropStyleInverse ui-state-highlight',
    tolerance: 'pointer',
    start: (evt, ui) => h.startMoving.call(h, evt, ui),
    stop: (evt, ui) => {
      h.stopMoving.call(h, evt, ui);
    },
    revert: 150,
    beforeStop: (evt, ui) => h.beforeStop.call(h, evt, ui),
    distance: 3,
    change: function (event, ui) {
      if (opts.prepend && ui.placeholder.index() < 1) {
        $('li.form-prepend').after(ui.placeholder);
      } else if (opts.append && ui.placeholder.index() >= $stage.children('li').length - 1) {
        $('li.form-append').before(ui.placeholder);
      }
    },
    update: function (event, ui) {
      if (h.doCancel) {
        return false;
      }
      if ($(ui.item).closest('.stage-wrap').length && $(ui.item).closest(rowWrapperClassSelector).length === 0) {
        h.doCancel = true;
        processControl(ui.item);
      } else {
        if (enhancedBootstrapEnabled()) {
          hideInvisibleRowPlaceholders();
        }
        h.setFieldOrder($cbUL);
        h.doCancel = !opts.sortableControls;
      }
    }
  });
  $cbUL.on('mouseenter', function () {
    if (!h.stageIsEmpty()) {
      $stage.children(tmpRowPlaceholderClassSelector + ':not(:last-child)').addClass(invisibleRowPlaceholderClass);
    }
  });
  const processControl = control => {
    if (control[0].classList.contains('input-set-control')) {
      const inputSets = [];
      const inputSet = opts.inputSets.find(set => hyphenCase(set.name || set.label) === control[0].dataset.type);
      if (inputSet && inputSet.showHeader) {
        const header = {
          type: 'header',
          subtype: 'h2',
          id: inputSet.name,
          label: inputSet.label
        };
        inputSets.push(header);
      }
      inputSets.push(...inputSet.fields);
      inputSets.forEach(field => {
        prepFieldVars(field, true);
        if (h.stopIndex || h.stopIndex === 0) {
          h.stopIndex++;
        }
      });
    } else {
      prepFieldVars(control, true);
    }
  };
  const $editorWrap = $(d.editorWrap);
  $('<div class="snackbar">').appendTo($editorWrap);
  let cbClasses = 'cb-wrap';
  let cbStyle = '';
  if (opts.stickyControls.enable) {
    cbClasses += ' sticky-controls';
    const offsetDefaults = {
      top: 0,
      bottom: 'auto',
      right: 'auto',
      left: 'auto'
    };
    const offset = Object.assign({}, offsetDefaults, config.opts.stickyControls.offset);
    if (offset.top !== 0) {
      cbStyle = `top: ${offset.top}px`;
    }
  }
  const cbWrap = m('div', d.controls, {
    id: `${data.formID}-cb-wrap`,
    className: cbClasses,
    style: cbStyle
  });
  if (opts.showActionButtons) {
    cbWrap.appendChild(d.formActions);
  }
  const gridModeHelp = m('div', '', {
    id: `${data.formID}-gridModeHelp`,
    className: 'grid-mode-help'
  });
  cbWrap.appendChild(gridModeHelp);
  $editorWrap.append(d.stage, cbWrap);
  if (element.type !== 'textarea') {
    $(element).append($editorWrap);
  } else {
    $(element).replaceWith($editorWrap);
  }
  $(d.controls).on('click', 'li.input-control, li.input-set-control', ({
    target
  }) => {
    if (h.stageIsEmpty()) {
      $stage.find(tmpRowPlaceholderClassSelector).eq(0).remove();
    }
    const $control = $(target).closest('li');
    h.stopIndex = opts.append ? $stage.children().length - 1 : undefined;
    processControl($control);
    h.save.call(h);
  });
  const nonEditableFields = () => {
    const cancelArray = [];
    const disabledField = type => m('li', opts[type], {
      className: `disabled-field form-${type}`
    });
    if (opts.prepend && !$('.disabled-field.form-prepend', d.stage).length) {
      cancelArray.push(true);
      $stage.prepend(disabledField('prepend'));
    }
    if (opts.append && !$('.disabled-field.form-append', d.stage).length) {
      cancelArray.push(true);
      $stage.append(disabledField('append'));
    }
    h.disabledTT(d.stage);
    return cancelArray.some(elem => elem === true);
  };
  const prepFieldVars = (fieldArg, isNew = false) => {
    let field = {};
    if (fieldArg instanceof jQuery) {
      field.type = fieldArg[0].dataset.type;
      if (field.type) {
        const custom = controls.custom.lookup(field.type);
        if (custom) {
          const customType = field.type;
          field = Object.assign({}, custom);
          field.label = controls.custom.label(customType);
        } else {
          const controlClass = controls.getClass(field.type);
          field.label = controlClass.label(field.type);
        }
      } else {
        const attrs = fieldArg[0].attributes;
        if (!isNew) {
          field.values = fieldArg.children().map((index, elem) => {
            return {
              label: $(elem).text(),
              value: $(elem).attr('value'),
              selected: Boolean($(elem).attr('selected'))
            };
          });
        }
        for (let i = attrs.length - 1; i >= 0; i--) {
          field[attrs[i].name] = attrs[i].value;
        }
      }
    } else {
      field = Object.assign({}, fieldArg);
    }
    if (!field.name) {
      field.name = nameAttr(field);
    }
    if (isNew && ['text', 'number', 'file', 'date', 'select', 'textarea', 'autocomplete'].includes(field.type)) {
      field.className = field.className || 'form-control';
    }
    const match = /(?:^|\s)btn-(.*?)(?:\s|$)/g.exec(field.className);
    if (match) {
      field.style = match[1];
    }
    if (isNew) {
      const eventTimeout = setTimeout(() => {
        $stage[0].dispatchEvent(new Event('fieldAdded', {
          bubbles: true,
          cancelable: false
        }));
        clearTimeout(eventTimeout);
      }, 10);
    }
    control.stringifyJsonAttrs(field);
    opts.onAddField(data.lastID, field);
    appendNewField(field, isNew);
    opts.onAddFieldAfter(data.lastID, field);
    d.stage.classList.remove('empty');
  };
  formBuilder.prepFieldVars = prepFieldVars;
  formBuilder.generateAdvFields = values => generateAdvFields(control.stringifyJsonAttrs(form_builder_objectSpread({}, values)));
  formBuilder.getAttrVals = node => h.getAttrVals(node);
  formBuilder.updatePreview = $node => h.updatePreview($node);
  const loadFields = function (formData) {
    formData = h.getData(formData);
    if (formData && formData.length) {
      formData.forEach(field => CaptureRowData(field));
      formData.forEach(fieldData => prepFieldVars(trimObj(fieldData)));
      d.stage.classList.remove('empty');
    } else if (opts.defaultFields && opts.defaultFields.length) {
      config.opts.defaultFields.forEach(field => CaptureRowData(field));
      h.addDefaultFields();
    } else if (!opts.prepend && !opts.append) {
      d.stage.classList.add('empty');
      d.stage.dataset.content = mi18n_min_default().get('getStarted');
    }
    if (nonEditableFields()) {
      d.stage.classList.remove('empty');
    }
    h.save();
  };
  function CaptureRowData(field) {
    const gridRowValue = h.getRowValue(field.className);
    if (gridRowValue && !formRows.includes(gridRowValue)) {
      formRows.push(gridRowValue);
    }
  }
  const fieldOptions = function (fieldName, fieldData) {
    var _fieldData$noSelect;
    const {
      type
    } = fieldData;
    let fieldValues;
    const optionActions = [m('a', mi18n_min_default().get('addOption'), {
      className: 'add add-opt'
    })];
    const fieldLabel = fieldName === 'values' ? mi18n_min_default().get('selectOptions') : i18n[fieldName];
    const fieldOptions = [m('label', fieldLabel, {
      className: 'false-label'
    })];
    const optionsNoSelect = (_fieldData$noSelect = fieldData['noSelect']) !== null && _fieldData$noSelect !== void 0 ? _fieldData$noSelect : false;
    const isMultiple = fieldData.multiple || type === 'checkbox-group';
    const optionDataTemplate = count => {
      const label = mi18n_min_default().get('optionCount', count);
      return {
        selected: false,
        label,
        value: hyphenCase(label)
      };
    };
    const values = fieldName === 'options' ? fieldData['values'] : fieldData[fieldName] || fieldData['values'];
    if (!values || !values.length) {
      let defaultOptCount = [1, 2, 3];
      if (['checkbox-group', 'checkbox'].includes(type)) {
        defaultOptCount = [1];
      }
      fieldValues = defaultOptCount.map(optionDataTemplate);
      const firstOption = fieldValues[0];
      if (firstOption.hasOwnProperty('selected') && type !== 'radio-group') {
        firstOption.selected = true;
      }
    } else {
      fieldValues = values.map(option => Object.assign({}, {
        selected: false
      }, option));
    }
    const optionActionsWrap = m('div', optionActions, {
      className: 'option-actions'
    });
    const optionGroupName = nameAttr({
      type: 'grp-options'
    }) + '-options';
    const options = m('ol', fieldValues.map((option, index) => {
      const optionData = config.opts.onAddOption(option, {
        type,
        index,
        isMultiple,
        fieldName
      });
      if (optionsNoSelect) {
        optionData.selected = false;
      }
      return selectFieldOptions(optionGroupName, optionData, isMultiple);
    }), {
      className: 'sortable-options' + (optionsNoSelect ? ' options-no-select' : '')
    });
    const optionsWrap = m('div', [options, optionActionsWrap], {
      className: 'sortable-options-wrap'
    });
    fieldOptions.push(optionsWrap);
    return m('div', fieldOptions, {
      name: fieldName,
      className: 'form-group field-options'
    });
  };
  const defaultFieldAttrs = type => {
    const defaultAttrs = ['required', 'label', 'description', 'placeholder', 'className', 'name', 'access', 'value'];
    const noValFields = ['header', 'paragraph', 'file', 'autocomplete'].concat(d.optionFields);
    const valueField = !noValFields.includes(type);
    const typeAttrsMap = {
      autocomplete: defaultAttrs.concat(['options', 'requireValidOption']),
      button: ['label', 'subtype', 'style', 'className', 'name', 'value', 'access'],
      checkbox: ['required', 'label', 'description', 'toggle', 'inline', 'className', 'name', 'access', 'other', 'options'],
      text: defaultAttrs.concat(['subtype', 'maxlength']),
      date: defaultAttrs.concat(['subtype', 'min', 'max', 'step']),
      file: defaultAttrs.concat(['multiple']),
      header: ['label', 'subtype', 'className', 'access'],
      hidden: ['name', 'value', 'access'],
      paragraph: ['label', 'subtype', 'className', 'access'],
      number: defaultAttrs.concat(['subtype', 'min', 'max', 'step']),
      select: defaultAttrs.concat(['multiple', 'options']),
      textarea: defaultAttrs.concat(['subtype', 'maxlength', 'rows'])
    };
    if (type in controls.registeredSubtypes && !(type in typeAttrsMap)) {
      typeAttrsMap[type] = defaultAttrs.concat(['subtype']);
    }
    typeAttrsMap['checkbox-group'] = typeAttrsMap.checkbox;
    typeAttrsMap['radio-group'] = typeAttrsMap.checkbox;
    const typeAttrs = typeAttrsMap[type];
    if (type === 'radio-group') {
      removeFromArray('toggle', typeAttrs);
    }
    if (['header', 'paragraph', 'button'].includes(type)) {
      removeFromArray('description', typeAttrs);
    }
    if (!valueField) {
      removeFromArray('value', typeAttrs);
    }
    return typeAttrs || defaultAttrs;
  };
  const generateAdvFields = values => {
    var _opts$typeUserAttrs$, _opts$typeUserAttrs$t, _typeClass$definition;
    const {
      type,
      subtype
    } = values;
    const roles = values.role !== undefined ? values.role.split(',') : [];
    const advFields = [];
    const typeClass = controls.getClass(type);
    const fieldAttrs = defaultFieldAttrs(type);
    const advFieldMap = {
      required: () => requiredField(values),
      toggle: () => boolAttribute('toggle', values, {
        first: mi18n_min_default().get('toggle')
      }),
      inline: () => {
        const labels = {
          first: mi18n_min_default().get('inline'),
          second: mi18n_min_default().get('inlineDesc', type.replace('-group', ''))
        };
        return boolAttribute('inline', values, labels);
      },
      label: () => textAttribute('label', values),
      description: () => textAttribute('description', values),
      subtype: isHidden => {
        const events = {
          change: evt => {
            const {
              value
            } = evt.target;
            const field = closest(evt.target, '.form-field');
            const curAdvFields = evt.target.closest('.form-elements-inner');
            const newAdvFields = generateAdvFields(form_builder_objectSpread(form_builder_objectSpread(form_builder_objectSpread({}, values), h.getAttrVals(field)), {}, {
              subtype: value
            }));
            curAdvFields.replaceWith(newAdvFields);
          }
        };
        return selectAttribute('subtype', form_builder_objectSpread({
          events
        }, values), subtypes[type], isHidden);
      },
      style: () => btnStyles(values.style),
      placeholder: () => textAttribute('placeholder', values),
      rows: () => numberAttribute('rows', values),
      className: isHidden => textAttribute('className', values, isHidden),
      name: isHidden => textAttribute('name', values, isHidden),
      value: () => textAttribute('value', values),
      maxlength: () => numberAttribute('maxlength', values),
      access: () => {
        const rolesDisplay = values.role ? 'style="display:block"' : '';
        const availableRoles = [];
        for (key in opts.roles) {
          if (opts.roles.hasOwnProperty(key)) {
            const roleId = `fld-${data.lastID}-roles-${key}`;
            const cbAttrs = {
              type: 'checkbox',
              name: 'roles[]',
              value: key,
              id: roleId,
              className: 'roles-field'
            };
            if (roles.includes(key)) {
              cbAttrs.checked = 'checked';
            }
            const checkbox = h.input(cbAttrs);
            const label = m('label', [checkbox, ` ${opts.roles[key]}`], {
              for: roleId
            });
            availableRoles.push(label);
          }
        }
        const availableRolesDiv = m('div', availableRoles, {
          className: 'available-roles',
          style: rolesDisplay
        });
        const accessLabels = {
          first: mi18n_min_default().get('roles'),
          second: mi18n_min_default().get('limitRole'),
          content: availableRolesDiv
        };
        return boolAttribute('access', values, accessLabels);
      },
      other: () => boolAttribute('other', values, {
        first: mi18n_min_default().get('enableOther'),
        second: mi18n_min_default().get('enableOtherMsg')
      }),
      options: () => fieldOptions('values', values),
      requireValidOption: () => boolAttribute('requireValidOption', values, {
        first: ' ',
        second: mi18n_min_default().get('requireValidOption')
      }),
      multiple: () => {
        const typeLabels = {
          default: {
            first: 'Multiple',
            second: 'set multiple attribute'
          },
          file: {
            first: mi18n_min_default().get('multipleFiles'),
            second: mi18n_min_default().get('allowMultipleFiles')
          },
          select: {
            first: ' ',
            second: mi18n_min_default().get('selectionsMessage')
          }
        };
        return boolAttribute('multiple', values, typeLabels[type] || typeLabels.default);
      }
    };
    let key;
    const numAttrs = ['min', 'max', 'step'];
    numAttrs.forEach(numAttr => {
      advFieldMap[numAttr] = type === 'number' ? () => numberAttribute(numAttr, values) : () => textAttribute(numAttr, values);
    });
    const noDisable = ['name', 'className', 'subtype'];
    const tuaLookup = [type, subtype].filter(Boolean).join('-');
    const typeUserAttrs = form_builder_objectSpread(form_builder_objectSpread(form_builder_objectSpread({}, (_opts$typeUserAttrs$ = opts.typeUserAttrs['*']) !== null && _opts$typeUserAttrs$ !== void 0 ? _opts$typeUserAttrs$ : {}), (_opts$typeUserAttrs$t = opts.typeUserAttrs[tuaLookup]) !== null && _opts$typeUserAttrs$t !== void 0 ? _opts$typeUserAttrs$t : {}), (_typeClass$definition = typeClass?.definition?.userAttrs) !== null && _typeClass$definition !== void 0 ? _typeClass$definition : {});
    Object.keys(fieldAttrs).forEach(index => {
      const attr = fieldAttrs[index];
      const useDefaultAttr = [true];
      const isDisabled = opts.disabledAttrs.includes(attr);
      if (opts.typeUserDisabledAttrs[type]) {
        const typeDisabledAttrs = opts.typeUserDisabledAttrs[type];
        useDefaultAttr.push(!typeDisabledAttrs.includes(attr));
      }
      if (typeClass.definition.hasOwnProperty('disabledAttrs')) {
        const userDisabledAttrs = typeClass.definition.disabledAttrs;
        useDefaultAttr.push(!userDisabledAttrs.includes(attr));
      }
      if (typeClass.definition.hasOwnProperty('defaultAttrs')) {
        const userAttrs = Object.keys(typeClass.definition.defaultAttrs);
        useDefaultAttr.push(!userAttrs.includes(attr));
      }
      if (typeUserAttrs) {
        const userAttrs = Object.keys(typeUserAttrs);
        useDefaultAttr.push(!userAttrs.includes(attr));
      }
      if (isDisabled && !noDisable.includes(attr)) {
        useDefaultAttr.push(false);
      }
      if (useDefaultAttr.every(Boolean)) {
        advFields.push(advFieldMap[attr](isDisabled));
      }
    });
    if (typeClass.definition.hasOwnProperty('defaultAttrs')) {
      const customAttr = processTypeUserAttrs(typeClass.definition.defaultAttrs, values);
      advFields.push(customAttr);
    }
    if (typeUserAttrs) {
      const customAttr = processTypeUserAttrs(typeUserAttrs, values);
      advFields.push(customAttr);
    }
    return m('div', advFields, {
      className: 'form-elements-inner'
    });
  };
  function userAttrType(attrData) {
    var _attrData$value;
    const value = (_attrData$value = attrData.value) !== null && _attrData$value !== void 0 ? _attrData$value : attrData;
    return [['array', ({
      options
    }) => !!options], ['boolean', ({
      type
    }) => type === 'checkbox'], ['options', ({
      type
    }) => type === 'options'], [typeof value, () => true]].find(typeCondition => typeCondition[1](attrData))[0];
  }
  function hasSubType(values, subType) {
    return values.subtype && values.subtype === subType;
  }
  function processTypeUserAttrs(typeUserAttr, values) {
    const advField = [];
    const attrTypeMap = {
      array: selectUserAttrs,
      string: inputUserAttrs,
      number: numberAttribute,
      boolean: (attrName, attrData) => {
        let isChecked = false;
        if (attrName.type === 'checkbox') {
          isChecked = Boolean(attrData.hasOwnProperty('value') ? attrData.value : false);
        } else if (values.hasOwnProperty(attrName)) {
          isChecked = values[attrName];
        } else if (attrData.hasOwnProperty('value') || attrData.hasOwnProperty('checked')) {
          isChecked = attrData.value || attrData.checked || false;
        }
        return boolAttribute(attrName, form_builder_objectSpread(form_builder_objectSpread({}, attrData), {}, {
          [attrName]: isChecked
        }), {
          first: i18n[attrName]
        });
      },
      options: fieldOptions,
      object: (attrName, {
        [attrName]: value,
        label,
        className
      }) => {
        const id = `${attrName}-${data.lastID}`;
        const userAttrs = Object.entries(value).reduce((acc, [name, val]) => {
          const subAttrName = `${attrName}[${name}]`;
          acc[subAttrName] = {
            label: i18n[name] || titleCase(name),
            value: val,
            name: subAttrName
          };
          return acc;
        }, {});
        const labelDom = m('label', label || i18n[attrName]);
        const inputWrap = m('div', processTypeUserAttrs(userAttrs, values), {
          id,
          className: 'input-group-wrap'
        });
        return m('div', [labelDom, inputWrap], {
          className: ['form-group', `${safeClassName(attrName)}-wrap`, className]
        });
      }
    };
    for (const attribute in typeUserAttr) {
      if (typeUserAttr.hasOwnProperty(attribute)) {
        const attrValType = userAttrType(typeUserAttr[attribute]);
        if (attrValType !== 'undefined') {
          const orig = mi18n_min_default().get(attribute);
          const _typeUserAttr$attribu = typeUserAttr[attribute],
            {
              [attribute]: value
            } = _typeUserAttr$attribu,
            tUA = form_builder_objectWithoutProperties(_typeUserAttr$attribu, [attribute].map(form_builder_toPropertyKey));
          let origValue = tUA.value;
          if (attrValType === 'boolean') {
            tUA[attribute] ??= tUA.value;
          } else if (attrValType === 'number') {
            tUA[attribute] ??= firstNumberOrUndefined(values[attribute], origValue);
          } else {
            origValue ??= '';
            tUA[attribute] ??= values[attribute] || origValue;
          }
          tUA.value = value;
          if (tUA.label) {
            i18n[attribute] = Array.isArray(tUA.label) ? mi18n_min_default().get(...tUA.label) || tUA.label[0] : tUA.label;
          }
          if (attrTypeMap[attrValType]) {
            advField.push(attrTypeMap[attrValType](attribute, tUA));
          }
          i18n[attribute] = orig;
        } else if (attrValType === 'undefined' && hasSubType(values, attribute)) {
          advField.push(processTypeUserAttrs(typeUserAttr[attribute], values));
        } else {
          const def = {};
          def[attribute] = typeUserAttr[attribute];
          opts.notify.warning('Warning: unable to process typeUserAttr definition : ' + JSON.stringify(def));
        }
      }
    }
    return advField;
  }
  function inputUserAttrs(name, inputAttrs) {
    const {
        class: classname,
        className
      } = inputAttrs,
      attrs = form_builder_objectWithoutProperties(inputAttrs, form_builder_excluded);
    let textAttrs = {
      id: name + '-' + data.lastID,
      title: attrs.description || attrs.label || name.toUpperCase(),
      name: name,
      type: attrs.type || 'text',
      className: [safeClassName(`fld-${name}`), (classname || className || '').trim()],
      value: attrs.hasOwnProperty(name) ? attrs[name] : attrs.value || ''
    };
    const label = m('label', i18n[name] || '', {
      for: textAttrs.id
    });
    const optionInputs = ['checkbox', 'checkbox-group', 'radio-group'];
    if (!optionInputs.includes(textAttrs.type)) {
      textAttrs.className.push('form-control');
    }
    textAttrs = Object.assign({}, attrs, textAttrs);
    const textInput = (() => {
      if (textAttrs.type === 'textarea') {
        const textValue = textAttrs.value;
        delete textAttrs.value;
        return m('textarea', textValue, textAttrs);
      } else {
        return m('input', null, textAttrs);
      }
    })();
    const inputWrap = m('div', textInput, {
      className: 'input-wrap'
    });
    return m('div', [label, inputWrap], {
      className: `form-group ${safeClassName(name)}-wrap`
    });
  }
  function selectUserAttrs(name, fieldData) {
    const {
        multiple,
        options,
        label: labelText,
        value,
        class: classname,
        className
      } = fieldData,
      restData = form_builder_objectWithoutProperties(fieldData, form_builder_excluded2);
    const selectValues = fieldData.hasOwnProperty(name) ? fieldData[name] : value || [];
    const optis = Object.keys(options).map(val => {
      const attrs = {
        value: val
      };
      const optionTextVal = options[val];
      const optionText = Array.isArray(optionTextVal) ? mi18n_min_default().get(...optionTextVal) || optionTextVal[0] : optionTextVal;
      if (Array.isArray(selectValues) ? selectValues.includes(val) : val === selectValues) {
        attrs.selected = true;
      }
      return m('option', optionText, attrs);
    });
    const selectAttrs = form_builder_objectSpread({
      id: `${name}-${data.lastID}`,
      title: restData.description || labelText || name.toUpperCase(),
      name,
      className: `fld-${name} form-control ${classname || className || ''}`.trim()
    }, restData);
    if (multiple) {
      selectAttrs.multiple = true;
    }
    const label = m('label', i18n[name] || '', {
      for: selectAttrs.id
    });
    const select = m('select', optis, selectAttrs);
    const inputWrap = m('div', select, {
      className: 'input-wrap'
    });
    return m('div', [label, inputWrap], {
      className: `form-group ${safeClassName(name)}-wrap`
    });
  }
  const boolAttribute = (name, values, labels = {}) => {
    const label = txt => m('label', txt, {
      for: `${name}-${data.lastID}`
    });
    const cbAttrs = {
      type: 'checkbox',
      className: `fld-${name}`,
      name,
      id: `${name}-${data.lastID}`
    };
    if (values[name]) {
      cbAttrs.checked = true;
    }
    const left = [];
    let right = [m('input', null, cbAttrs)];
    if (labels.first) {
      left.push(label(labels.first));
    }
    if (labels.second) {
      right.push(' ', label(labels.second));
    }
    if (labels.content) {
      right.push(labels.content);
    }
    right = m('div', right, {
      className: 'input-wrap'
    });
    return m('div', left.concat(right), {
      className: `form-group ${safeClassName(name)}-wrap`
    });
  };
  const btnStyles = style => {
    const styleLabel = m('label', i18n.style);
    const styleInput = h.input({
      value: style || 'default',
      type: 'hidden',
      className: 'btn-style'
    });
    const styleButtons = styles.btn.map(btnStyle => {
      const classList = ['btn-xs', 'btn', `btn-${btnStyle}`];
      if (style === btnStyle) {
        classList.push('selected');
      }
      return m('button', mi18n_min_default().get(`styles.btn.${btnStyle}`), {
        value: btnStyle,
        type: 'button',
        className: classList.join(' ')
      });
    });
    const styleButtonGroup = m('div', styleButtons, {
      className: 'btn-group',
      role: 'group'
    });
    if (style === 'undefined') {
      style = 'default';
    }
    return m('div', [styleLabel, styleInput, styleButtonGroup], {
      className: 'form-group style-wrap'
    });
  };
  const numberAttribute = (attribute, values) => {
    const {
        className = ''
      } = values,
      attrs = form_builder_objectWithoutProperties(values, form_builder_excluded3);
    const attrVal = Number.isNaN(attrs[attribute]) ? undefined : attrs[attribute];
    const attrLabel = mi18n_min_default().get(attribute) || attribute;
    const placeholder = mi18n_min_default().get(`placeholder.${attribute}`);
    const inputConfig = {
      type: 'number',
      value: attrVal,
      name: attribute,
      placeholder,
      className: [safeClassName(`fld-${attribute}`), 'form-control', `${className}`.trim()],
      id: `${attribute}-${data.lastID}`
    };
    const numberAttribute = h.input(trimObj(inputConfig));
    const inputWrap = m('div', numberAttribute, {
      className: 'input-wrap'
    });
    const inputLabel = m('label', attrLabel, {
      for: inputConfig.id
    });
    return m('div', [inputLabel, inputWrap], {
      className: `form-group ${safeClassName(attribute)}-wrap`
    });
  };
  const selectAttribute = (attribute, _ref, optionData, isHidden = false) => {
    let {
        events
      } = _ref,
      values = form_builder_objectWithoutProperties(_ref, _excluded4);
    const selectOptions = optionData.map((option, i) => {
      let optionAttrs = Object.assign({
        label: `${i18n.option} ${i}`,
        value: undefined
      }, option);
      if (option.value === values[attribute]) {
        optionAttrs.selected = true;
      }
      optionAttrs = trimObj(optionAttrs);
      return m('option', optionAttrs.label, optionAttrs);
    });
    const selectAttrs = {
      id: attribute + '-' + data.lastID,
      name: attribute,
      className: `fld-${attribute} form-control`,
      events
    };
    const labelText = mi18n_min_default().get(attribute) || capitalize(attribute) || '';
    const label = m('label', labelText, {
      for: selectAttrs.id
    });
    const select = m('select', selectOptions, selectAttrs);
    const inputWrap = m('div', select, {
      className: 'input-wrap'
    });
    const attrWrap = m('div', [label, inputWrap], {
      className: `form-group ${selectAttrs.name}-wrap`,
      style: isHidden && 'display: none'
    });
    return attrWrap;
  };
  const textAttribute = (attribute, values, isHidden = false) => {
    const textArea = ['paragraph'];
    let attrVal = values[attribute] || '';
    let attrLabel = mi18n_min_default().get(attribute) || attribute;
    if (attribute === 'label') {
      if (textArea.includes(values.type)) {
        attrLabel = mi18n_min_default().get('content');
      } else {
        attrVal = parsedHtml(attrVal);
      }
    }
    const placeholder = mi18n_min_default().get(`placeholders.${attribute}`) || '';
    let attributefield;
    const noMakeAttr = [];
    if (!noMakeAttr.some(elem => elem === true)) {
      const inputConfig = {
        name: attribute,
        placeholder,
        className: `fld-${attribute} form-control`,
        id: `${attribute}-${data.lastID}`
      };
      const attributeLabel = m('label', attrLabel);
      if (attribute === 'label' && !opts.disableHTMLLabels) {
        inputConfig.contenteditable = true;
        attributefield = m('div', attrVal, inputConfig);
      } else if (values.type === 'textarea' && attribute === 'value') {
        attributefield = m('textarea', attrVal, inputConfig);
      } else {
        inputConfig.value = attrVal;
        inputConfig.type = 'text';
        attributefield = m('input', null, inputConfig);
      }
      const inputWrap = m('div', attributefield, {
        className: 'input-wrap'
      });
      const fieldAttrs = {
        className: `form-group ${attribute}-wrap`
      };
      if (attribute === 'value' && values.subtype === 'quill' || isHidden) {
        fieldAttrs.style = 'display: none';
      }
      attributefield = m('div', [attributeLabel, inputWrap], fieldAttrs);
    }
    return attributefield;
  };
  const requiredField = fieldData => {
    const {
      type
    } = fieldData;
    const noRequire = ['header', 'paragraph', 'button'];
    const noMake = [];
    let requireField = '';
    if (noRequire.includes(type)) {
      noMake.push(true);
    }
    if (!noMake.some(elem => elem === true)) {
      requireField = boolAttribute('required', fieldData, {
        first: mi18n_min_default().get('required')
      });
    }
    return requireField;
  };
  const appendNewField = function (values, isNew = true) {
    const columnData = prepareFieldRow(values);
    data.lastID = h.incrementId(data.lastID);
    const {
      type = 'text',
      subtype
    } = values;
    let label = values.label || (isNew ? i18n[type] || mi18n_min_default().get('label') : '');
    if (type === 'hidden' || label === '') {
      var _mi18n$get;
      label = `${(_mi18n$get = mi18n_min_default().get(type)) !== null && _mi18n$get !== void 0 ? _mi18n$get : type}: ${values.name}`;
    }
    const disabledFieldButtons = opts.disabledFieldButtons[type] || values.disabledFieldButtons;
    let fieldButtons = [m('a', null, {
      type: 'remove',
      id: 'del_' + data.lastID,
      className: `del-button btn ${form_builder_css_prefix_text}cancel delete-confirm`,
      title: mi18n_min_default().get('removeMessage')
    }), m('a', null, {
      type: 'edit',
      id: data.lastID + '-edit',
      className: `toggle-form btn ${form_builder_css_prefix_text}pencil`,
      title: mi18n_min_default().get('hide')
    }), m('a', null, {
      type: 'copy',
      id: data.lastID + '-copy',
      className: `copy-button btn ${form_builder_css_prefix_text}copy`,
      title: mi18n_min_default().get('copyButtonTooltip')
    })];
    if (enhancedBootstrapEnabled()) {
      fieldButtons.push(m('a', null, {
        type: 'grid',
        id: data.lastID + '-grid',
        className: `grid-button btn ${form_builder_css_prefix_text}grid`,
        title: 'Grid Mode'
      }));
    } else {
      fieldButtons.push(m('a', null, {
        type: 'sort',
        id: data.lastID + '-sort-higher',
        className: `sort-button sort-button-higher btn ${form_builder_css_prefix_text}sort-higher`,
        title: 'Move Higher'
      }), m('a', null, {
        type: 'sort',
        id: data.lastID + '-sort-lower',
        className: `sort-button sort-button-lower btn ${form_builder_css_prefix_text}sort-lower`,
        title: 'Move Lower'
      }));
    }
    if (disabledFieldButtons && Array.isArray(disabledFieldButtons)) {
      fieldButtons = fieldButtons.filter(btnData => !disabledFieldButtons.includes(btnData.type));
    }
    const liContents = [m('div', fieldButtons, {
      className: 'field-actions'
    })];
    const labelValue = opts.disableHTMLLabels ? document.createTextNode(label) : parsedHtml(label);
    liContents.push(m('label', labelValue, {
      className: 'field-label'
    }));
    liContents.push(m('span', ' *', {
      className: 'required-asterisk',
      style: values.required ? 'display:inline' : ''
    }));
    const descAttrs = {
      className: 'tooltip-element',
      tooltip: values.description,
      style: values.description ? 'display:inline-block' : 'display:none'
    };
    liContents.push(m('span', '?', descAttrs));
    const prevHolder = m('div', '', {
      className: 'prev-holder',
      dataFieldId: data.lastID
    });
    liContents.push(prevHolder);
    const formElements = m('div', [generateAdvFields(values), m('a', mi18n_min_default().get('close'), {
      className: 'close-field'
    })], {
      className: 'form-elements'
    });
    const editPanel = m('div', formElements, {
      id: `${data.lastID}-holder`,
      className: 'frm-holder',
      dataFieldId: data.lastID
    });
    formBuilder.currentEditPanel = editPanel;
    liContents.push(editPanel);
    const liAttrs = {
      class: `${type}-field form-field`,
      type: type,
      id: data.lastID
    };
    if (type !== subtype) {
      liAttrs.subtype = subtype;
    }
    const field = m('li', liContents, liAttrs);
    const $li = $(field);
    AttachColWrapperHandler($li);
    $li.data('fieldData', {
      attrs: values
    });
    if (typeof h.stopIndex !== 'undefined') {
      $(d.stage).children().eq(h.stopIndex).before($li);
    } else {
      $stage.append($li);
    }
    $('.sortable-options', $li).sortable({
      update: () => UpdatePreviewAndSave($li)
    });
    h.updatePreview($li);
    let rowWrapperNode;
    if (enhancedBootstrapEnabled()) {
      const targetRow = `div.row-${columnData.rowUniqueId}`;
      let newRowCreated = false;
      if ($stage.children(targetRow).length) {
        rowWrapperNode = $stage.children(targetRow);
      } else {
        rowWrapperNode = m('div', null, {
          id: `${field.id}-row`,
          className: `row row-${columnData.rowUniqueId} ${rowWrapperClass}`
        });
        newRowCreated = true;
      }
      if (insertingNewControl && insertTargetIsRow) {
        $targetInsertWrapper.attr('id', rowWrapperNode.id);
        $targetInsertWrapper.attr('class', rowWrapperNode.className);
        $targetInsertWrapper.attr('style', '');
        $targetInsertWrapper.attr('data-row-id', columnData.rowUniqueId);
        rowWrapperNode = $targetInsertWrapper;
      }
      const colWrapperNode = m('div', null, {
        id: `${field.id}-cont`,
        className: `${columnData.columnSize} ${colWrapperClass}`
      });
      if (insertingNewControl && insertTargetIsColumn) {
        if ($targetInsertWrapper.attr('prepend') === 'true') {
          $(colWrapperNode).prependTo(rowWrapperNode);
        } else {
          $(colWrapperNode).insertAfter(`#${$targetInsertWrapper.attr('appendAfter')}`);
        }
      }
      if (!insertTargetIsColumn) {
        $(colWrapperNode).appendTo(rowWrapperNode);
      }
      if (!insertingNewControl && newRowCreated) {
        $li.after(rowWrapperNode);
      }
      $li.appendTo(colWrapperNode);
      if (newRowCreated) {
        setupSortableRowWrapper(rowWrapperNode);
        hideInvisibleRowPlaceholders();
        SetupInvisibleRowPlaceholders(rowWrapperNode);
        if (opts.enableColumnInsertMenu) {
          $(rowWrapperNode).off('mouseenter');
          $(rowWrapperNode).on('mouseenter', function (e) {
            setupColumnInserts($(e.currentTarget));
          });
          $(rowWrapperNode).off('mouseleave');
          $(rowWrapperNode).on('mouseleave', function (e) {
            hideColumnInsertButtons($(e.currentTarget));
          });
        }
      }
      setupColumnInserts(rowWrapperNode, true);
      if (columnData.addedDefaultColumnClass) {
        $li.attr('addedDefaultColumnClass', true);
      }
      h.tmpCleanPrevHolder($(prevHolder));
    }
    if (opts.typeUserEvents[type] && opts.typeUserEvents[type].onadd) {
      opts.typeUserEvents[type].onadd(field);
    } else if (opts.typeUserEvents['*'] && opts.typeUserEvents['*'].onadd) {
      opts.typeUserEvents['*'].onadd(field);
    }
    if (isNew) {
      if (opts.editOnAdd) {
        h.closeAllEdit();
        h.toggleEdit(data.lastID, false);
      }
      if (field.scrollIntoView && opts.scrollToFieldOnAdd) {
        field.scrollIntoView({
          behavior: 'smooth'
        });
      }
    }
    if (enhancedBootstrapEnabled()) {
      if (insertingNewControl && insertTargetIsColumn) {
        autoSizeRowColumns(rowWrapperNode, true);
      }
    }
    insertingNewControl = false;
    insertTargetIsRow = false;
    insertTargetIsColumn = false;
  };
  function AttachColWrapperHandler(colWrapper) {
    if (!enhancedBootstrapEnabled()) {
      return;
    }
    colWrapper.mouseenter(function (e) {
      if (!gridMode) {
        gridModeTargetField = $(this);
        gridModeStartX = e.pageX;
        gridModeStartY = e.pageY;
      }
    });
  }
  function hideInvisibleRowPlaceholders() {
    $stage.find(tmpRowPlaceholderClassSelector + ':not(:last-child)').css('height', '1px').addClass(invisibleRowPlaceholderClass);
  }
  function SetupInvisibleRowPlaceholders(rowWrapperNode) {
    const wrapperClone = $(rowWrapperNode).clone();
    wrapperClone.addClass(invisibleRowPlaceholderClass).addClass(tmpRowPlaceholderClass).html('');
    wrapperClone.css('height', '1px');
    wrapperClone.attr('class', wrapperClone.attr('class').replace('row-', ''));
    wrapperClone.removeAttr('id');
    if ($(rowWrapperNode).index() === 0) {
      const wrapperClone2 = $(wrapperClone).clone();
      $stage.prepend(wrapperClone2);
      setupSortableRowWrapper(wrapperClone2);
    }
    wrapperClone.insertAfter($(rowWrapperNode));
    setupSortableRowWrapper(wrapperClone);
    $stage.find(rowWrapperClassSelector + ':last-of-type').removeClass(invisibleRowPlaceholderClass);
  }
  function ResetAllInvisibleRowPlaceholders() {
    $stage.children(tmpRowPlaceholderClassSelector).remove();
    $stage.children(rowWrapperClassSelector).each((i, elem) => {
      SetupInvisibleRowPlaceholders($(elem));
    });
    $stage.find(rowWrapperClassSelector + ':last-of-type').removeClass(invisibleRowPlaceholderClass);
  }
  function setupSortableRowWrapper(rowWrapperNode) {
    if (!enhancedBootstrapEnabled()) {
      return;
    }
    $(rowWrapperNode).sortable({
      connectWith: [rowWrapperClassSelector],
      cursor: 'move',
      opacity: 0.9,
      revert: 150,
      distance: 3,
      tolerance: 'pointer',
      helper: function (e, el) {
        const clone = el.clone();
        clone.find('.field-actions').remove();
        clone.css({
          width: '20%',
          height: '100px',
          minHeight: '60px',
          overflow: 'hidden'
        });
        return clone;
      },
      over: function (event) {
        const overTarget = $(event.target);
        const overTargetIsPlaceholder = overTarget.hasClass(tmpRowPlaceholderClass);
        if (!overTargetIsPlaceholder) {
          removeColumnInsertButtons(overTarget);
        }
        overTarget.addClass('hoverDropStyleInverse');
        if (!overTargetIsPlaceholder) {
          hideInvisibleRowPlaceholders();
          overTarget.prev(tmpRowPlaceholderClassSelector).removeClass(invisibleRowPlaceholderClass).css('height', '40px');
          overTarget.next(tmpRowPlaceholderClassSelector).removeClass(invisibleRowPlaceholderClass).css('height', '40px');
        }
      },
      out: function (event) {
        $stage.children(tmpRowPlaceholderClassSelector).removeClass('hoverDropStyleInverse');
        $(event.target).removeClass('hoverDropStyleInverse');
      },
      placeholder: 'hoverDropStyleInverse',
      receive: function (event, ui) {
        const senderIsControlsBox = $(ui.sender).attr('id') === $cbUL.attr('id');
        const droppingToNewRow = $(ui.item).parent().hasClass(tmpRowPlaceholderClass);
        const droppingToExistingRow = !droppingToNewRow && $(ui.item).parent().hasClass(rowWrapperClass);
        if (droppingToNewRow) {
          if (senderIsControlsBox) {
            insertTargetIsRow = true;
            insertingNewControl = true;
            $targetInsertWrapper = $(ui.item).parent();
          } else {
            const colWrapper = $(ui.item);
            const columnData = prepareFieldRow({});
            const rowWrapperNode = m('div', null, {
              id: `${colWrapper.find('li').attr('id')}-row`,
              className: `row row-${columnData.rowUniqueId} ${rowWrapperClass}`
            });
            $(ui.item).parent().replaceWith(rowWrapperNode);
            AttachColWrapperHandler($(ui.item));
            colWrapper.appendTo(rowWrapperNode);
            setupSortableRowWrapper(rowWrapperNode);
            h.syncFieldWithNewRow(colWrapper[0], colWrapper.closest(rowWrapperClassSelector)[0]);
          }
        }
        if (droppingToExistingRow && senderIsControlsBox) {
          if ($(ui.item).prev().hasClass('btnAddControl')) {
            $targetInsertWrapper = $(ui.item).prev();
          } else if ($(ui.item).next().hasClass('btnAddControl')) {
            $targetInsertWrapper = $(ui.item).next();
          } else {
            $targetInsertWrapper = $(ui.item).attr('prepend', 'true');
          }
          const parentRowValue = h.getRowClass($(ui.item).parent().attr('class'));
          $targetInsertWrapper.addClass(parentRowValue);
          insertTargetIsColumn = true;
          insertingNewControl = true;
          h.stopIndex = undefined;
        }
        if (insertingNewControl) {
          h.doCancel = true;
          processControl(ui.item);
          h.save.call(h);
        }
        checkRowCleanup();
        ResetAllInvisibleRowPlaceholders();
        const listFieldItem = $(ui.item).find('li');
        if (listFieldItem.length) {
          CheckTinyMCETransition(listFieldItem);
          UpdatePreviewAndSave(listFieldItem);
          h.tmpCleanPrevHolder($(ui.item).find('.prev-holder'));
        }
      },
      start: (event, ui) => {
        $stage.addClass('__preventColButtons');
        removeColumnInsertButtons(ui.item.closest(rowWrapperClassSelector));
      },
      stop: (event, ui) => {
        $stage.removeClass('__preventColButtons');
        $stage.children(tmpRowPlaceholderClassSelector).removeClass('hoverDropStyleInverse');
        checkRowCleanup();
        autoSizeRowColumns(ui.item.closest(rowWrapperClassSelector), true);
      },
      update: (event, ui) => {
        h.syncFieldWithNewRow(ui.item, $(ui.item).closest(rowWrapperClassSelector)[0]);
      }
    });
    const rowId = h.getRowValue(rowWrapperNode.className);
    if (rowId !== '0') {
      $(rowWrapperNode).attr('data-row-id', rowId);
    }
  }
  function CheckTinyMCETransition(fieldListItem) {
    const isTinyMCE = fieldListItem.find('textarea[type="tinymce"]');
    if (isTinyMCE.length) {
      window.lastFormBuilderCopiedTinyMCE = window.tinymce.get(isTinyMCE.attr('id')).save();
    }
  }
  function UpdatePreviewAndSave(fieldListItem) {
    h.updatePreview(fieldListItem);
    h.save.call(h);
  }
  function setupColumnInserts(rowWrapper, hide = false) {
    if (!opts.enableColumnInsertMenu || $stage.hasClass('__preventColButtons')) {
      return;
    }
    $(rowWrapper).children('button.btnAddControl').remove();
    const rowColumns = $(rowWrapper).children(colWrapperClassSelector);
    rowColumns.each((i, elem) => {
      const colWrapper = $(elem);
      colWrapper.addClass('colWithInsertButtons');
      if (rowColumns.index(colWrapper) === 0) {
        $(`<button type="button" class="formbuilder-icon-plus btnAddControl ${h.getRowClass(colWrapper.parent().attr('class'))}" prepend="true" style='visibility: ${hide ? 'hidden' : 'visible'}'></button>`).insertBefore(colWrapper);
      }
      $(`<button type="button" class="formbuilder-icon-plus btnAddControl ${h.getRowClass(colWrapper.parent().attr('class'))}" appendAfter="${colWrapper.attr('id')}" style='visibility: ${hide ? 'hidden' : 'visible'}'></button>`).insertAfter(colWrapper);
    });
  }
  function removeColumnInsertButtons(rowWrapper) {
    rowWrapper.find('button.btnAddControl').remove();
    rowWrapper.find(colWrapperClassSelector).removeClass('colWithInsertButtons');
  }
  function hideColumnInsertButtons(rowWrapper) {
    rowWrapper.find('button.btnAddControl').css('visibility', 'hidden');
  }
  function prepareFieldRow(data) {
    if (!enhancedBootstrapEnabled()) {
      return {};
    }
    const result = h.tryParseColumnInfo(data);
    if (!result.rowUniqueId) {
      if (insertingNewControl && insertTargetIsColumn) {
        result.rowUniqueId = h.getRowValue($targetInsertWrapper.attr('class'));
      } else {
        let nextRow;
        if (formRows.length === 0) {
          nextRow = 1;
        } else {
          const numericalRows = formRows.filter(value => !isNaN(value) && !isNaN(parseInt(value))).map(str => parseInt(str));
          nextRow = Math.max(...numericalRows, 0) + 1;
        }
        result.rowUniqueId = nextRow.toString();
      }
      result.columnSize = opts.defaultGridColumnClass;
      if (!data.className) {
        data.className = '';
      }
      data.className += ` row-${result.rowUniqueId} ${result.columnSize}`;
      result.addedDefaultColumnClass = true;
    }
    if (!formRows.includes(result.rowUniqueId)) {
      formRows.push(result.rowUniqueId);
    }
    return result;
  }
  const selectFieldOptions = function (optionGroupName, optionData, multipleSelect) {
    const optionTemplate = {
      selected: false,
      label: '',
      value: ''
    };
    const optionInputType = {
      selected: multipleSelect ? 'checkbox' : 'radio'
    };
    const optionInputTypeMap = {
      boolean: (value, prop) => {
        const attrs = {
          value,
          type: optionInputType[prop] || 'checkbox'
        };
        if (value) {
          attrs.checked = !!value;
        }
        attrs.name = optionGroupName;
        return ['input', null, attrs];
      },
      number: value => ['input', null, {
        value,
        type: 'number'
      }],
      string: (value, prop) => ['input', null, {
        value,
        type: 'text',
        placeholder: mi18n_min_default().get(`placeholder.${prop}`) || ''
      }],
      array: values => ['select', values.map(({
        label,
        value
      }) => m('option', label, {
        value
      }))],
      object: _ref2 => {
        let {
            tag,
            content
          } = _ref2,
          attrs = form_builder_objectWithoutProperties(_ref2, _excluded5);
        return [tag, content, attrs];
      }
    };
    optionData = form_builder_objectSpread(form_builder_objectSpread({}, optionTemplate), optionData);
    const optionInputs = Object.entries(optionData).map(([prop, val]) => {
      const optionInputDataType = getContentType(val);
      const [tag, content, attrs] = optionInputTypeMap[optionInputDataType](val, prop);
      const optionClassName = `option-${prop} option-attr`;
      attrs['data-attr'] = prop;
      attrs.className = attrs.className ? `${attrs.className} ${optionClassName}` : optionClassName;
      return m(tag, content, attrs);
    });
    const removeAttrs = {
      className: `remove btn ${form_builder_css_prefix_text}cancel`,
      title: mi18n_min_default().get('removeMessage')
    };
    optionInputs.push(m('a', null, removeAttrs));
    return m('li', optionInputs);
  };
  const cloneItem = function cloneItem(currentItem) {
    data.lastID = h.incrementId(data.lastID);
    CheckTinyMCETransition(currentItem);
    const currentId = currentItem.attr('id');
    const type = currentItem.attr('type');
    const cloneName = nameAttr({
      type: type
    });
    const $clone = currentItem.clone();
    $('.fld-name', $clone).val(cloneName);
    $clone.find('[id]').each((i, elem) => {
      elem.id = elem.id.replace(currentId, data.lastID);
    });
    $clone.find('[for]').each((index, elem) => {
      const curId = elem.getAttribute('for');
      const newForId = curId.replace(currentId, data.lastID);
      elem.setAttribute('for', newForId);
    });
    const selects = currentItem.find('select');
    selects.each(function (i) {
      const select = this;
      $clone.find('select').eq(i).val($(select).val());
    });
    $clone.attr('id', data.lastID);
    $clone.attr('name', cloneName);
    $clone.addClass('cloned');
    const sortableOptions = $('.sortable-options', $clone);
    sortableOptions.find('.option-selected').attr('name', nameAttr({
      type: 'grp-options'
    }) + '-options');
    sortableOptions.sortable();
    if (opts.typeUserEvents[type] && opts.typeUserEvents[type].onclone) {
      opts.typeUserEvents[type].onclone($clone[0]);
    } else if (opts.typeUserEvents['*'] && opts.typeUserEvents['*'].onclone) {
      opts.typeUserEvents['*'].onclone($clone[0]);
    }
    return $clone;
  };
  const saveAndUpdate = evt => {
    if (evt) {
      const isDisabled = [({
        type,
        target
      }) => type === 'keyup' && target.name === 'className'].some(typeCondition => typeCondition(evt));
      if (isDisabled) {
        return false;
      }
      UpdatePreviewAndSave($(evt.target).closest('.form-field'));
    }
  };
  const previewSelectors = ['.form-elements input', '.form-elements select', '.form-elements textarea', '.form-elements [contenteditable]'].join(', ');
  $stage.on('input', previewSelectors, throttle_default()(saveAndUpdate, defaultTimeout, {
    leading: false
  }));
  $stage.on('click touchstart', '.remove', e => {
    const $field = $(e.target).parents('.form-field:eq(0)');
    const field = $field[0];
    const type = field.getAttribute('type');
    const $option = $(e.target.parentElement);
    e.preventDefault();
    const options = field.querySelector('.sortable-options');
    const optionsCount = options.childNodes.length;
    if (optionsCount <= 2 && !type.includes('checkbox')) {
      opts.notify.error('Error: ' + mi18n_min_default().get('minOptionMessage'));
    } else {
      $option.slideUp('250', () => {
        $option.remove();
        UpdatePreviewAndSave($field);
      });
    }
  });
  $stage.on('touchstart', 'input', e => {
    const $input = $(e.target);
    if (e.handled !== true) {
      if ($input.attr('type') === 'checkbox') {
        $input.trigger('click');
      } else {
        $input.focus();
        const fieldVal = $input.val();
        $input.val(fieldVal);
      }
    } else {
      return false;
    }
  });
  $stage.on('click touchstart', '.toggle-form, .close-field', function (e) {
    e.stopPropagation();
    e.preventDefault();
    if (e.handled !== true) {
      const targetID = $(e.target).parents('.form-field:eq(0)').attr('id');
      h.toggleEdit(targetID);
      e.handled = true;
    } else {
      return false;
    }
  });
  $stage.on('dblclick', 'li.form-field', e => {
    if (['select', 'input', 'label', 'textarea', 'a'].includes(e.target.tagName.toLowerCase()) || e.target.isContentEditable === true) {
      return;
    }
    e.stopPropagation();
    e.preventDefault();
    if (e.handled !== true) {
      const targetID = $(e.target).closest('li.form-field').attr('id');
      h.toggleEdit(targetID);
      e.handled = true;
    }
  });
  $stage.on('change', '[name="subtype"]', e => {
    const $field = $(e.target).closest('li.form-field');
    const $valWrap = $('.value-wrap', $field);
    $valWrap.toggle(e.target.value !== 'quill');
  });
  $stage.on('change', '[name="name"]', e => {
    const name = e.target.value;
    if (attributeWillClobber(name)) {
      opts.notify.error('Potential for Dom Clobbering with field name ' + name);
    }
  });
  const stageOnChangeSelectors = ['.prev-holder input', '.prev-holder select', '.prev-holder textarea'];
  $stage.on('change', stageOnChangeSelectors.join(', '), e => {
    let prevOptions;
    if (e.target.classList.contains('other-option')) {
      return;
    }
    const field = closest(e.target, '.form-field');
    const optionTypes = ['select', 'checkbox-group', 'radio-group'];
    if (optionTypes.includes(field.type)) {
      const options = field.getElementsByClassName('option-value');
      if (field.type === 'select') {
        forEach(options, i => {
          const selectedOption = options[i].parentElement.childNodes[0];
          selectedOption.checked = e.target.value === options[i].value;
        });
      } else {
        prevOptions = document.getElementsByName(e.target.name);
        forEach(prevOptions, i => {
          if (prevOptions[i].classList.contains('other-option')) return;
          const selectedOption = options[i].parentElement.childNodes[0];
          selectedOption.checked = prevOptions[i].checked;
        });
      }
    } else {
      const fieldVal = document.getElementById('value-' + field.id);
      if (fieldVal) {
        fieldVal.value = e.target.value;
      }
    }
    h.save.call(h);
  });
  $stage.on('input', '.fld-label', ({
    target
  }) => {
    const value = target.value || target.innerHTML;
    const label = closest(target, '.form-field').querySelector('.field-label');
    setElementContent(label, parsedHtml(value), config.opts.disableHTMLLabels);
  });
  $stage.on('keyup', 'input.error', ({
    target
  }) => $(target).removeClass('error'));
  $stage.on('keyup', 'input[name="description"]', function (e) {
    const $field = $(e.target).parents('.form-field:eq(0)');
    const closestToolTip = $('.tooltip-element', $field);
    const ttVal = $(e.target).val();
    if (ttVal !== '') {
      if (!closestToolTip.length) {
        const tt = m('span', '?', {
          className: 'tooltip-element',
          tooltip: ttVal
        });
        $('.field-label', $field).after(tt);
      } else {
        closestToolTip.attr('tooltip', ttVal).css('display', 'inline-block');
      }
    } else {
      if (closestToolTip.length) {
        closestToolTip.css('display', 'none');
      }
    }
  });
  $stage.on('change', '.fld-multiple', e => {
    const newType = e.target.checked ? 'checkbox' : 'radio';
    const $options = $('.option-selected', $(e.target).closest('.form-elements'));
    $options.each(i => $options[i].type = newType);
    return newType;
  });
  $stage.on('blur', 'input.fld-name', function (e) {
    e.target.value = safename(e.target.value);
    if (e.target.value === '') {
      $(e.target).addClass('field-error').attr('placeholder', mi18n_min_default().get('cannotBeEmpty'));
    } else {
      $(e.target).removeClass('field-error');
    }
  });
  $stage.on('blur', 'input.fld-maxlength, input.fld-rows', e => {
    e.target.value = forceNumber(e.target.value);
  });
  $stage.on('click touchstart', '.btnAddControl', function (evt) {
    const btn = $(evt.currentTarget);
    cloneControls = $cbUL.clone();
    cloneControls.hover(function () {}, function () {
      cloneControls.remove();
    });
    cloneControls.on('click', 'li', ({
      target
    }) => {
      insertTargetIsColumn = true;
      insertingNewControl = true;
      $targetInsertWrapper = btn;
      const $control = $(target).closest('li');
      h.stopIndex = undefined;
      processControl($control);
      h.save.call(h);
      cloneControls.remove();
    });
    $stage.append(cloneControls);
    if (btn.index() == 0) {
      cloneControls.css({
        position: 'fixed',
        left: btn.offset().left,
        top: btn.offset().top - $(window).scrollTop()
      });
    } else {
      cloneControls.css({
        position: 'fixed',
        left: btn.offset().left - 80,
        top: btn.offset().top - $(window).scrollTop()
      });
    }
    const bottomOfClone = cloneControls.offset().top + cloneControls.outerHeight();
    const bottomOfScreen = $(window).scrollTop() + $(window).innerHeight();
    if (bottomOfClone > bottomOfScreen) {
      cloneControls.css({
        top: parseInt(cloneControls.css('top')) - (bottomOfClone - bottomOfScreen)
      });
    }
  });
  $stage.on('click', `.${form_builder_css_prefix_text}copy`, function (evt) {
    evt.preventDefault();
    const currentItem = $(evt.target).parent().parent('li');
    const $clone = cloneItem(currentItem);
    if (enhancedBootstrapEnabled()) {
      prepareCloneWrappers($clone, currentItem);
    } else {
      $clone.insertAfter(currentItem);
    }
    UpdatePreviewAndSave($clone);
    h.tmpCleanPrevHolder($clone.find('.prev-holder'));
    if (opts.editOnAdd) {
      h.closeField(data.lastID, false);
    }
  });
  if (enhancedBootstrapEnabled()) {
    $stage.on('stageEmptied', () => {
      formRows = [];
    });
  }
  function prepareCloneWrappers($clone, currentItem) {
    const inputClassElement = $(`#className-${currentItem.attr('id')}`);
    const columnData = prepareFieldRow({});
    const rowWrapper = m('div', null, {
      id: `${$clone.attr('id')}-row`,
      className: `row row-${columnData.rowUniqueId} ${rowWrapperClass}`
    });
    const colWrapper = m('div', null, {
      id: `${$clone.attr('id')}-cont`,
      className: `${h.getBootstrapColumnClass(inputClassElement.val())} ${colWrapperClass}`
    });
    $(colWrapper).appendTo(rowWrapper);
    let insertAfterElement;
    if (currentItem.parent().is('div')) {
      insertAfterElement = currentItem.closest(rowWrapperClassSelector);
    } else if (currentItem.parent().is('ul')) {
      insertAfterElement = currentItem;
    }
    $(rowWrapper).insertAfter(insertAfterElement);
    $clone.appendTo(colWrapper);
    setupSortableRowWrapper(rowWrapper);
    ResetAllInvisibleRowPlaceholders();
    h.syncFieldWithNewRow($clone[0], $clone.closest(rowWrapperClassSelector)[0]);
  }
  $stage.on('click', '.delete-confirm', e => {
    e.preventDefault();
    const buttonPosition = e.target.getBoundingClientRect();
    const bodyRect = document.body.getBoundingClientRect();
    const coords = {
      pageX: buttonPosition.left + buttonPosition.width / 2,
      pageY: buttonPosition.top - bodyRect.top - 12
    };
    const deleteID = $(e.target).parents('.form-field:eq(0)').attr('id');
    const $field = $(document.getElementById(deleteID));
    document.addEventListener('modalClosed', function () {
      $field.removeClass('deleting');
    }, false);
    if (opts.fieldRemoveWarn) {
      const warnH3 = m('h3', mi18n_min_default().get('warning'));
      const warnMessage = m('p', mi18n_min_default().get('fieldRemoveWarning'));
      h.confirm([warnH3, warnMessage], () => h.removeField(deleteID), coords);
      $field.addClass('deleting');
    } else {
      h.removeField(deleteID);
    }
  });
  let gridMode = false;
  let gridModeTargetField;
  let gridModeStartX;
  let gridModeStartY;
  let wheelDelta = 0;
  $stage.on('click touchstart', '.grid-button', e => {
    e.preventDefault();
    const ID = $(e.target).parents('.form-field:eq(0)').attr('id');
    gridModeTargetField = $(document.getElementById(ID));
    gridModeStartX = e.pageX;
    gridModeStartY = e.pageY;
    wheelDelta = 0;
    toggleGridModeActive();
  });
  $stage.on('wheel', function (e) {
    if (e.originalEvent.deltaY === 0) {
      return;
    }
    if (gridMode) {
      e.preventDefault();
      wheelDelta += e.originalEvent.deltaY;
      const deltaPerShift = 120;
      if (wheelDelta > 0 && wheelDelta < deltaPerShift || wheelDelta < 0 && wheelDelta > -deltaPerShift) {
        return;
      }
      const parentCont = gridModeTargetField.closest('div');
      const currentColValue = h.getBootstrapColumnValue(parentCont.attr('class'));
      const change = Math.round(wheelDelta / deltaPerShift);
      wheelDelta = wheelDelta % deltaPerShift;
      const nextColSize = currentColValue + change;
      if (nextColSize > 12) {
        h.showToast('<b class="formbuilder-required">Column Size cannot exceed 12</b>');
        return;
      }
      if (nextColSize < 1) {
        h.showToast('<b class="formbuilder-required">Column Size cannot be less than 1</b>');
        return;
      }
      const rowWrapper = gridModeTargetField.closest(rowWrapperClassSelector);
      let totalRowValueCount = nextColSize;
      rowWrapper.children(`div${colWrapperClassSelector}`).each((i, elem) => {
        const colWrapper = $(`#${elem.id}`);
        const fieldID = colWrapper.find('li').attr('id');
        if (fieldID != gridModeTargetField.attr('id')) {
          totalRowValueCount += h.getBootstrapColumnValue($(`#${fieldID}-cont`).attr('class'));
        }
      });
      if (totalRowValueCount > 12) {
        h.showToast('<b class="formbuilder-required">There is a maximum of 12 columns per row</b>');
        return;
      }
      h.syncBootstrapColumnWrapperAndClassProperty(gridModeTargetField.attr('id'), nextColSize);
      gridModeTargetField.attr('manuallyChangedDefaultColumnClass', true);
      buildGridModeCurrentRowInfo();
      h.toggleHighlight(gridModeTargetField);
    }
  });
  $(document).keydown(e => {
    if (gridMode) {
      e.preventDefault();
      const rowWrapper = gridModeTargetField.closest(rowWrapperClassSelector);
      switch (event.code) {
        case 'KeyW':
        case 'ArrowUp':
          removeColumnInsertButtons(rowWrapper);
          moveFieldUp(rowWrapper);
          break;
        case 'KeyS':
        case 'ArrowDown':
          removeColumnInsertButtons(rowWrapper);
          moveFieldDown(rowWrapper);
          break;
        case 'KeyA':
        case 'ArrowLeft':
          removeColumnInsertButtons(rowWrapper);
          moveFieldLeft();
          break;
        case 'KeyD':
        case 'ArrowRight':
          removeColumnInsertButtons(rowWrapper);
          moveFieldRight();
          break;
        case 'KeyR':
          removeColumnInsertButtons(rowWrapper);
          autoSizeRowColumns(rowWrapper, true);
          setupColumnInserts(rowWrapper, true);
          break;
      }
      buildGridModeCurrentRowInfo();
      hideColumnInsertButtons(rowWrapper);
    }
  });
  function moveFieldUp(rowWrapper) {
    const previousRowSibling = rowWrapper.prevAll().not(tmpRowPlaceholderClassSelector).not('.form-prepend').first();
    if (previousRowSibling.length) {
      $(gridModeTargetField.parent().parent()).swapWith(previousRowSibling);
      h.toggleHighlight(gridModeTargetField);
    }
  }
  function moveFieldDown(rowWrapper) {
    const nextRowSibling = rowWrapper.nextAll().not(invisibleRowPlaceholderClassSelector).not('.form-append').first();
    if (nextRowSibling.length) {
      $(gridModeTargetField.parent().parent()).swapWith(nextRowSibling);
      h.toggleHighlight(gridModeTargetField);
    }
  }
  function moveFieldLeft() {
    const colSibling = gridModeTargetField.parent().prev();
    if (colSibling.length) {
      gridModeTargetField.parent().after(colSibling);
    }
    h.toggleHighlight(gridModeTargetField);
  }
  function moveFieldRight() {
    const colSibling = gridModeTargetField.parent().next();
    if (colSibling.length) {
      gridModeTargetField.parent().before(colSibling);
    }
    h.toggleHighlight(gridModeTargetField);
  }
  function autoSizeRowColumns(rowWrapper, force = false) {
    const childRowCount = rowWrapper.children(`div${colWrapperClassSelector}`).length;
    const newAutoCalcSizeValue = Math.floor(12 / childRowCount);
    rowWrapper.children(`div${colWrapperClassSelector}`).each((i, elem) => {
      const colWrapper = $(`#${elem.id}`);
      if (!force && colWrapper.find('li').attr('manuallyChangedDefaultColumnClass') == 'true') {
        h.showToast(`Preserving column size of field ${i + 1} because you had personally adjusted it`, 4000);
        return;
      }
      h.syncBootstrapColumnWrapperAndClassProperty(elem.id.replace('-cont', ''), newAutoCalcSizeValue);
    });
  }
  $(document).mousemove(e => {
    if (gridMode && h.getDistanceBetweenPoints(gridModeStartX, gridModeStartY, e.pageX, e.pageY) > config.opts.cancelGridModeDistance) {
      toggleGridModeActive(false);
    }
  });
  $(document).on('checkRowCleanup', (event, data) => {
    checkRowCleanup();
    const rowWrapper = $(`#${data.rowWrapperID}`);
    if (rowWrapper.length) {
      autoSizeRowColumns(rowWrapper, true);
    }
  });
  $(document).on('fieldOpened', (event, data) => {
    const rowWrapper = $(`#${data.rowWrapperID}`);
    if (rowWrapper.length) {
      hideColumnInsertButtons(rowWrapper);
    }
  });
  function checkRowCleanup() {
    $stage.find(colWrapperClassSelector).each((i, elem) => {
      const $colWrapper = $(elem);
      if ($colWrapper.is(':empty') && !formBuilder.preserveTempContainers.includes($colWrapper.attr('id'))) {
        $colWrapper.remove();
      }
    });
    $stage.children(rowWrapperClassSelector).not(tmpRowPlaceholderClassSelector).each((i, elem) => {
      if ($(elem).children(colWrapperClassSelector).length === 0) {
        const rowValue = h.getRowValue($(elem).attr('class'));
        formRows = formRows.filter(x => x !== rowValue);
        $(elem).remove();
      } else {
        setupColumnInserts($(elem), true);
      }
    });
  }
  function toggleGridModeActive(active = true) {
    if (active) {
      gridMode = true;
      h.showToast('Starting Grid Mode - Use the mousewheel to resize.', 1500);
      $cbUL.css('display', 'none');
      $(d.formActions).css('display', 'none');
      buildGridModeHelp();
      h.closeAllEdit();
      h.toggleHighlight(gridModeTargetField);
      hideInvisibleRowPlaceholders();
    } else {
      h.showToast('Grid Mode Finished', 1500);
      const rowWrapper = gridModeTargetField.closest(rowWrapperClassSelector);
      let totalRowValueCount = 0;
      rowWrapper.children(`div${colWrapperClassSelector}`).each((i, elem) => {
        const colWrapper = $(`#${elem.id}`);
        const fieldID = colWrapper.find('li').attr('id');
        totalRowValueCount += h.getBootstrapColumnValue($(`#${fieldID}-cont`).attr('class'));
      });
      if (totalRowValueCount > 12) {
        autoSizeRowColumns(rowWrapper, true);
      }
      gridMode = false;
      gridModeTargetField = null;
      $(gridModeHelp).empty();
      $cbUL.css('display', 'unset');
      $(d.formActions).css('display', 'unset');
    }
  }
  function buildGridModeHelp() {
    $(gridModeHelp).html(`
    <div style='padding:5px'>
      <h3 class="text text-center">Grid Mode</h3>

      <table style='border-spacing:7px;border-collapse: separate'>
        <thead>
          <tr>
            <th>Action</th>
            <th>Result</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td><kbd>MOUSEWHEEL</kbd></td>
            <td>Adjust the field column size</td>
          </tr>
          <tr>
            <td><kbd>W or &#x2191;</kbd></td>
            <td>Move entire row up</td>
          </tr>
          <tr>
              <td><kbd>S or &#x2193;</kbd></td>
              <td>Move entire row down</td>
          </tr>
          <tr>
              <td><kbd>A or &#x2190;</kbd></td>
              <td>Move field left within the row</td>
          </tr>
          <tr>
              <td><kbd>D or &#x2192;</kbd></td>
              <td>Move field right within the row</td>
          </tr>
          <tr>
            <td><kbd>R</kbd></td>
            <td>Resize all fields within the row to be maximally equal</td>
          </tr>
          <tr>
        </tbody>
      </table>

      <h5 class="text text-center" style='padding-top:10px'>Current Row Fields</h5>

      <table class='gridHelpCurrentRow'>
        <colgroup>
          <col width="100%" />
          <col width="0%" />
        </colgroup>

        <thead>
          <tr>
            <th>Field</th>
            <th>Size</th>
          </tr>
        </thead>

        <tbody>
        </tbody>
      </table>

    </div>
    `);
    buildGridModeCurrentRowInfo();
  }
  function buildGridModeCurrentRowInfo() {
    $(gridModeHelp).find('.gridHelpCurrentRow tbody').empty();
    const rowWrapper = gridModeTargetField.closest(rowWrapperClassSelector);
    rowWrapper.children(`div${colWrapperClassSelector}`).each((i, elem) => {
      const colWrapper = $(`#${elem.id}`);
      const fieldID = colWrapper.find('li').attr('id');
      const field = $(`#${fieldID}`);
      const fieldType = field.attr('type');
      let label = $(`#label-${fieldID}`).html();
      if (fieldType === 'hidden' || fieldType === 'paragraph') {
        label = $(`#name-${fieldID}`).val();
      }
      if (!label) {
        label = field.attr('id');
      }
      let currentFieldClass = '';
      if (gridModeTargetField.attr('id') === fieldID) {
        currentFieldClass = 'currentGridModeFieldHighlight';
      }
      $(gridModeHelp).find('.gridHelpCurrentRow tbody').append(`
        <tr>
          <td class='grid-mode-help-row1 ${currentFieldClass}'>${label}</td>
          <td class='grid-mode-help-row2 ${currentFieldClass}'>
            ${h.getBootstrapColumnValue($(`#${fieldID}-cont`).attr('class'))}
          </td>
        <tr>
      `);
    });
  }
  $stage.on('click', '.field-actions .sort-button', function (evt) {
    evt.preventDefault();
    const currentItem = $(evt.target).parent().parent('li');
    let swap;
    if ($(evt.target).hasClass('sort-button-higher')) {
      swap = currentItem.prev('li');
      if (swap.length && !swap.hasClass('form-prepend')) {
        currentItem.insertBefore(swap);
      }
    } else {
      swap = currentItem.next('li');
      if (swap.length && !swap.hasClass('form-append')) {
        currentItem.insertAfter(swap);
      }
    }
    h.toggleHighlight(currentItem);
    h.save.call(h);
  });
  $stage.on('click', '.style-wrap button', e => {
    const $button = $(e.target);
    const $attrsWrap = $button.closest('.form-elements');
    const styleVal = $button.val();
    const $btnStyle = $('.btn-style', $attrsWrap);
    $btnStyle.val(styleVal);
    $button.siblings('.btn').removeClass('selected');
    $button.addClass('selected');
    UpdatePreviewAndSave($btnStyle.closest('.form-field'));
  });
  $stage.on('click', '.fld-required', e => {
    $(e.target).closest('.form-field').find('.required-asterisk').toggle();
  });
  $stage.on('click', 'input.fld-access', function (e) {
    const roles = $(e.target).closest('.form-field').find('.available-roles');
    const enableRolesCB = $(e.target);
    roles.slideToggle(250, function () {
      if (!enableRolesCB.is(':checked')) {
        $('input[type=checkbox]', roles).removeAttr('checked');
      }
    });
  });
  $stage.on('click', '.add-opt', function (e) {
    e.preventDefault();
    const type = $(e.target).closest('.form-field').attr('type');
    const fieldName = $(e.target).closest('.form-group.field-options').attr('name');
    const $optionWrap = $(e.target).closest('.field-options');
    const $multiple = $('[name="multiple"]', $optionWrap);
    const $firstOption = $('.option-selected:eq(0)', $optionWrap);
    const isMultiple = $multiple.length ? $multiple.prop('checked') : $firstOption.attr('type') === 'checkbox';
    const optionTemplate = {
      selected: false,
      label: '',
      value: ''
    };
    const $sortableOptions = $('.sortable-options', $optionWrap);
    const optionData = config.opts.onAddOption(optionTemplate, {
      type,
      index: $sortableOptions.children().length,
      isMultiple,
      fieldName
    });
    $sortableOptions.append(selectFieldOptions($firstOption.attr('name'), optionData, isMultiple));
    UpdatePreviewAndSave($(e.target).parents('.form-field:eq(0)'));
  });
  $stage.on('mouseover mouseout', '.remove, .del-button', e => $(e.target).closest('li').toggleClass('delete'));
  loadFields();
  if (opts.disableInjectedStyle === true) {
    const styleTags = document.getElementsByClassName('formBuilder-injected-style');
    forEach(styleTags, i => remove(styleTags[i]));
  } else if (opts.disableInjectedStyle === 'bootstrap') {
    d.editorWrap.classList.remove('formbuilder-embedded-bootstrap');
  }
  $stage[0].dispatchEvent(new Event('loaded', {
    bubbles: true,
    cancelable: false
  }));
  formBuilder.actions = {
    getFieldTypes: activeOnly => activeOnly ? subtract(controls.getRegistered(), opts.disableFields) : controls.getRegistered(),
    clearFields: () => h.removeAllFields(d.stage),
    showData: h.showData.bind(h),
    save: minify => {
      const formData = h.save(minify);
      const formDataJS = window.JSON.parse(formData);
      config.opts.onSave(formDataJS);
      return formDataJS;
    },
    addField: (field, index) => {
      h.stopIndex = data.formData.length ? index : undefined;
      prepFieldVars(field);
    },
    removeField: h.removeField.bind(h),
    getData: h.getFormData.bind(h),
    generateAdvFields: values => generateAdvFields(control.stringifyJsonAttrs(form_builder_objectSpread({}, values))),
    getAttrVals: node => h.getAttrVals(node),
    updatePreview: $node => h.updatePreview($node),
    setData: formData => {
      h.stopIndex = undefined;
      h.removeAllFields(d.stage);
      loadFields(formData);
    },
    setLang: locale => {
      return mi18n_min_default().setCurrent.call((mi18n_min_default()), locale).then(() => {
        d.stage.dataset.content = mi18n_min_default().get('getStarted');
        controls.init();
        d.empty(d.formActions);
        h.formActionButtons().forEach(button => d.formActions.appendChild(button));
      });
    },
    showDialog: h.dialog.bind(h),
    toggleFieldEdit: fieldId => {
      const fieldIds = Array.isArray(fieldId) ? fieldId : [fieldId];
      fieldIds.forEach(fId => {
        if (!['number', 'string'].includes(typeof fId)) {
          return;
        }
        if (typeof fId === 'number') {
          fId = d.stage.children[fId].id;
        } else if (!/^frmb-/.test(fId)) {
          fId = d.stage.querySelector(fId).id;
        }
        h.toggleEdit(fId);
      });
    },
    toggleAllFieldEdit: () => {
      forEach(d.stage.children, index => {
        h.toggleEdit(d.stage.children[index].id);
      });
    },
    closeAllFieldEdit: h.closeAllEdit.bind(h),
    getCurrentFieldId: () => {
      return data.lastID;
    }
  };
  d.onRender(d.controls, () => {
    const onRenderTimeout = setTimeout(() => {
      d.stage.style.minHeight = `${d.controls.clientHeight}px`;
      clearTimeout(onRenderTimeout);
    }, 0);
  });
  return formBuilder;
}
const pluginInit = function (options, elem) {
  const _this = this;
  const _jQuery$extend = jQuery.extend({}, defaultOptions, options, true),
    {
      i18n
    } = _jQuery$extend,
    opts = form_builder_objectWithoutProperties(_jQuery$extend, _excluded6);
  this.i18nOpts = jQuery.extend({}, defaultI18n, i18n, true);
  const notInitialised = () => {
    console.error('formBuilder is still initialising');
    console.info('See https://formbuilder.online/docs/formBuilder/actions/getData/#wont-work and https://formbuilder.online/docs/formBuilder/promise/ for more information on formBuilder asynchronous loading');
  };
  const actionList = ['getFieldTypes', 'addField', 'clearFields', 'closeAllFieldEdit', 'getData', 'removeField', 'save', 'setData', 'setLang', 'showData', 'showDialog', 'toggleAllFieldEdit', 'toggleFieldEdit', 'getCurrentFieldId'];
  this.instance = {
    actions: actionList.reduce((actions, currentAction) => {
      actions[currentAction] = notInitialised;
      return actions;
    }, {}),
    markup: markup,
    get formData() {
      return _this.instance.actions.getData !== notInitialised && _this.instance.actions.getData('json');
    },
    promise: new Promise(function (resolve, reject) {
      mi18n_min_default().init(_this.i18nOpts).then(() => {
        const formBuilder = new FormBuilder(opts, elem[0], jQuery);
        jQuery(elem[0]).data('formBuilder', formBuilder);
        Object.assign(_this.instance, formBuilder.actions);
        _this.instance.actions = formBuilder.actions;
        delete _this.instance.promise;
        resolve(_this.instance);
      }).catch(err => {
        reject(err);
        opts.notify.error(err);
      });
    })
  };
};
jQuery.fn.formBuilder = function (methodOrOptions = {}, ...args) {
  const isMethod = typeof methodOrOptions === 'string';
  if (isMethod) {
    const instance = this.data('fbInstance');
    if (instance[methodOrOptions]) {
      if (typeof instance[methodOrOptions] === 'function') {
        return instance[methodOrOptions].apply(this, args);
      }
      return instance[methodOrOptions];
    }
  } else {
    const plugin = new pluginInit(methodOrOptions, this);
    this.data('fbInstance', plugin.instance);
    return plugin.instance;
  }
};
}();
/******/ })()
;
})(jQuery);